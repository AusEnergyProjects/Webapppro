import { sql } from "drizzle-orm";
import { check, index, integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";

export const tradeAccounts = sqliteTable("trade_accounts", {
  firebaseUid: text("firebase_uid").primaryKey(),
  email: text("email").notNull(),
  businessName: text("business_name").notNull(),
  abn: text("abn").notNull().default(""),
  addressLine1: text("address_line_1").notNull().default(""),
  suburb: text("suburb").notNull().default(""),
  addressState: text("address_state").notNull().default(""),
  postcode: text("postcode").notNull().default(""),
  contactName: text("contact_name").notNull(),
  phone: text("phone").notNull().default(""),
  partnerType: text("partner_type").notNull().default("installer"),
  businessWebsite: text("business_website").notNull().default(""),
  serviceStates: text("service_states").notNull().default("[]"),
  capabilities: text("capabilities").notNull().default("[]"),
  summary: text("summary").notNull().default(""),
  accountStatus: text("account_status").notNull().default("active"),
  verificationStatus: text("verification_status").notNull().default("not_started"),
  verifiedAbn: text("verified_abn").notNull().default(""),
  verificationReviewId: text("verification_review_id").notNull().default(""),
  verificationReviewedAt: text("verification_reviewed_at").notNull().default(""),
  verificationReviewedByUid: text("verification_reviewed_by_uid").notNull().default(""),
  availabilityStatus: text("availability_status").notNull().default("paused"),
  serviceBasePostcode: text("service_base_postcode").notNull().default(""),
  serviceRadiusKm: integer("service_radius_km").notNull().default(50),
  emailOpportunities: integer("email_opportunities", { mode: "boolean" }).notNull().default(true),
  emailWeeklySummary: integer("email_weekly_summary", { mode: "boolean" }).notNull().default(true),
  isSynthetic: integer("is_synthetic", { mode: "boolean" }).notNull().default(false),
  settingsUpdatedAt: text("settings_updated_at").notNull().default(""),
  consentVersion: text("consent_version").notNull(),
  consentAt: text("consent_at").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_accounts_eligibility_idx").on(table.partnerType, table.accountStatus, table.verificationStatus, table.verifiedAbn, table.firebaseUid),
  uniqueIndex("trade_accounts_verified_abn_unique_idx").on(table.verifiedAbn).where(sql`${table.verifiedAbn} <> ''`),
  index("trade_accounts_admin_type_updated_idx").on(table.partnerType, table.updatedAt, table.firebaseUid),
  index("trade_accounts_admin_status_updated_idx").on(table.accountStatus, table.updatedAt, table.firebaseUid),
  index("trade_accounts_admin_verification_updated_idx").on(table.verificationStatus, table.updatedAt, table.firebaseUid),
  index("trade_accounts_business_nocase_idx").on(sql`${table.businessName} COLLATE NOCASE`, table.firebaseUid),
]);

export const tradeSupplierLocations = sqliteTable("trade_supplier_locations", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  locationName: text("location_name").notNull(),
  locationType: text("location_type").notNull().default("warehouse"),
  addressLine1: text("address_line_1").notNull(),
  suburb: text("suburb").notNull(),
  addressState: text("address_state").notNull(),
  postcode: text("postcode").notNull(),
  salesEmail: text("sales_email").notNull().default(""),
  contactNumber: text("contact_number").notNull().default(""),
  dispatchNotes: text("dispatch_notes").notNull().default(""),
  serviceStatesJson: text("service_states_json").notNull().default("[]"),
  recordStatus: text("record_status").notNull().default("active"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_supplier_locations_owner_name_idx").on(table.firebaseUid, table.locationName),
  index("trade_supplier_locations_owner_status_idx").on(table.firebaseUid, table.recordStatus, table.addressState, table.postcode),
]);

export const leadRateLimits = sqliteTable("lead_rate_limits", {
  clientHash: text("client_hash").primaryKey(),
  timestamps: text("timestamps").notNull().default("[]"),
  version: integer("version").notNull().default(0),
  updatedAt: integer("updated_at").notNull(),
}, (table) => [
  index("lead_rate_limits_updated_idx").on(table.updatedAt),
]);

export const verificationDocuments = sqliteTable("verification_documents", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  category: text("category").notNull(),
  fileName: text("file_name").notNull(),
  contentType: text("content_type").notNull(),
  sizeBytes: integer("size_bytes").notNull(),
  objectKey: text("object_key").notNull().unique(),
  expiryDate: text("expiry_date").notNull().default(""),
  status: text("status").notNull().default("uploaded"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("verification_documents_owner_idx").on(table.firebaseUid),
]);

export const adminUsers = sqliteTable("admin_users", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  email: text("email").notNull(),
  displayName: text("display_name").notNull().default(""),
  role: text("role").notNull().default("support"),
  status: text("status").notNull().default("active"),
  invitedByUid: text("invited_by_uid").notNull().default(""),
  lastLoginAt: text("last_login_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("admin_users_firebase_uid_idx").on(table.firebaseUid),
  uniqueIndex("admin_users_email_idx").on(table.email),
  index("admin_users_status_idx").on(table.status),
]);

export const adminAuditLog = sqliteTable("admin_audit_log", {
  id: text("id").primaryKey(),
  adminUid: text("admin_uid").notNull(),
  action: text("action").notNull(),
  entityType: text("entity_type").notNull(),
  entityId: text("entity_id").notNull(),
  summary: text("summary").notNull(),
  metadata: text("metadata").notNull().default("{}"),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("admin_audit_log_created_idx").on(table.createdAt),
  index("admin_audit_log_admin_idx").on(table.adminUid),
  index("admin_audit_log_entity_idx").on(table.entityType, table.entityId),
]);

export const adminNotifications = sqliteTable("admin_notifications", {
  id: text("id").primaryKey(),
  eventKey: text("event_key").notNull(),
  eventType: text("event_type").notNull(),
  category: text("category").notNull(),
  priority: text("priority").notNull().default("normal"),
  title: text("title").notNull(),
  summary: text("summary").notNull(),
  entityType: text("entity_type").notNull(),
  entityId: text("entity_id").notNull(),
  actorType: text("actor_type").notNull().default("system"),
  actorUid: text("actor_uid").notNull().default(""),
  requiresAction: integer("requires_action", { mode: "boolean" }).notNull().default(false),
  status: text("status").notNull().default("open"),
  readAt: text("read_at").notNull().default(""),
  readByUid: text("read_by_uid").notNull().default(""),
  resolvedAt: text("resolved_at").notNull().default(""),
  resolvedByUid: text("resolved_by_uid").notNull().default(""),
  resolutionNote: text("resolution_note").notNull().default(""),
  assignedToUid: text("assigned_to_uid").notNull().default(""),
  assignedAt: text("assigned_at").notNull().default(""),
  dueAt: text("due_at").notNull().default(""),
  metadata: text("metadata").notNull().default("{}"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("admin_notifications_event_key_idx").on(table.eventKey),
  index("admin_notifications_status_idx").on(table.status, table.createdAt),
  index("admin_notifications_action_idx").on(table.requiresAction, table.status, table.createdAt),
  index("admin_notifications_category_idx").on(table.category, table.createdAt),
  index("admin_notifications_entity_idx").on(table.entityType, table.entityId),
  index("admin_notifications_assignee_idx").on(table.assignedToUid, table.status, table.dueAt),
  index("admin_notifications_due_idx").on(table.status, table.dueAt),
]);

export const adminNotificationDeliveries = sqliteTable("admin_notification_deliveries", {
  id: text("id").primaryKey(),
  notificationId: text("notification_id").notNull(),
  channel: text("channel").notNull().default("webhook"),
  status: text("status").notNull().default("pending"),
  attempts: integer("attempts").notNull().default(0),
  nextAttemptAt: text("next_attempt_at").notNull().default(""),
  lastAttemptAt: text("last_attempt_at").notNull().default(""),
  deliveredAt: text("delivered_at").notNull().default(""),
  lastError: text("last_error").notNull().default(""),
  responseCode: integer("response_code").notNull().default(0),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("admin_notification_deliveries_notification_channel_idx").on(table.notificationId, table.channel),
  index("admin_notification_deliveries_status_idx").on(table.status, table.nextAttemptAt),
  index("admin_notification_deliveries_notification_idx").on(table.notificationId, table.createdAt),
]);

export const adminUsabilityPilots = sqliteTable("admin_usability_pilots", {
  id: text("id").primaryKey(),
  name: text("name").notNull(),
  goal: text("goal").notNull(),
  targetParticipants: integer("target_participants").notNull().default(5),
  status: text("status").notNull().default("recruiting"),
  startsAt: text("starts_at").notNull().default(""),
  endsAt: text("ends_at").notNull().default(""),
  successCriteria: text("success_criteria").notNull().default("[]"),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("admin_usability_pilots_status_idx").on(table.status, table.updatedAt),
]);

export const adminUsabilityPilotParticipants = sqliteTable("admin_usability_pilot_participants", {
  id: text("id").primaryKey(),
  pilotId: text("pilot_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  slotNumber: integer("slot_number").notNull(),
  businessNameSnapshot: text("business_name_snapshot").notNull(),
  baselineSystem: text("baseline_system").notNull().default(""),
  teamSize: integer("team_size").notNull().default(1),
  primaryTrade: text("primary_trade").notNull().default(""),
  status: text("status").notNull().default("invited"),
  ownerUid: text("owner_uid").notNull().default(""),
  nextAction: text("next_action").notNull().default(""),
  invitedAt: text("invited_at").notNull(),
  completedAt: text("completed_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("admin_usability_pilot_participant_account_idx").on(table.pilotId, table.firebaseUid),
  uniqueIndex("admin_usability_pilot_participant_slot_idx").on(table.pilotId, table.slotNumber),
  index("admin_usability_pilot_participant_status_idx").on(table.pilotId, table.status, table.updatedAt),
]);

export const adminUsabilityPilotSessions = sqliteTable("admin_usability_pilot_sessions", {
  id: text("id").primaryKey(),
  pilotId: text("pilot_id").notNull(),
  participantId: text("participant_id").notNull(),
  sessionType: text("session_type").notNull(),
  status: text("status").notNull().default("scheduled"),
  scheduledAt: text("scheduled_at").notNull().default(""),
  completedAt: text("completed_at").notNull().default(""),
  durationMinutes: integer("duration_minutes").notNull().default(0),
  tasksAttempted: integer("tasks_attempted").notNull().default(0),
  tasksCompleted: integer("tasks_completed").notNull().default(0),
  easeScore: integer("ease_score").notNull().default(0),
  confidenceScore: integer("confidence_score").notNull().default(0),
  feedback: text("feedback").notNull().default(""),
  observedFrictions: text("observed_frictions").notNull().default("[]"),
  nextAction: text("next_action").notNull().default(""),
  facilitatorUid: text("facilitator_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("admin_usability_pilot_sessions_participant_idx").on(table.participantId, table.scheduledAt),
  index("admin_usability_pilot_sessions_status_idx").on(table.pilotId, table.status, table.scheduledAt),
]);

export const tradeAccountNotes = sqliteTable("trade_account_notes", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  note: text("note").notNull(),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("trade_account_notes_owner_idx").on(table.firebaseUid, table.createdAt),
]);

export const tradeAccountVerificationReviews = sqliteTable("trade_account_verification_reviews", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  abn: text("abn").notNull(),
  businessName: text("business_name").notNull(),
  partnerType: text("partner_type").notNull(),
  legalEntityName: text("legal_entity_name").notNull(),
  decision: text("decision").notNull(),
  reviewMethod: text("review_method").notNull(),
  sourceReference: text("source_reference").notNull().default(""),
  note: text("note").notNull(),
  reviewedByUid: text("reviewed_by_uid").notNull(),
  reviewedAt: text("reviewed_at").notNull(),
}, (table) => [
  index("trade_account_verification_reviews_owner_idx").on(table.firebaseUid, table.reviewedAt),
  index("trade_account_verification_reviews_reviewer_idx").on(table.reviewedByUid, table.reviewedAt),
]);

export const customerAccountNotes = sqliteTable("customer_account_notes", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  note: text("note").notNull(),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("customer_account_notes_owner_idx").on(table.firebaseUid, table.createdAt),
]);

export const tradeWorkOrders = sqliteTable("trade_work_orders", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  partnerType: text("partner_type").notNull(),
  workType: text("work_type").notNull().default("job"),
  sourceType: text("source_type").notNull().default("internal"),
  sourceReference: text("source_reference").notNull().default(""),
  workNumber: text("work_number").notNull(),
  title: text("title").notNull(),
  serviceCategory: text("service_category").notNull().default("other"),
  serviceCategories: text("service_categories").notNull().default("[]"),
  siteArea: text("site_area").notNull().default(""),
  stage: text("stage").notNull().default("backlog"),
  priority: text("priority").notNull().default("standard"),
  scheduledStart: text("scheduled_start").notNull().default(""),
  scheduledEnd: text("scheduled_end").notNull().default(""),
  assigneeMemberId: text("assignee_member_id").notNull().default(""),
  assigneeLabel: text("assignee_label").notNull().default(""),
  revision: integer("revision").notNull().default(1),
  recordStatus: text("record_status").notNull().default("active"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_work_orders_owner_number_idx").on(table.firebaseUid, table.workNumber),
  uniqueIndex("trade_work_orders_tlink_job_number_idx").on(table.workNumber).where(sql`${table.workNumber} GLOB 'TLJ-*'`),
  index("trade_work_orders_owner_stage_idx").on(table.firebaseUid, table.recordStatus, table.stage, table.updatedAt),
  index("trade_work_orders_source_idx").on(table.firebaseUid, table.sourceType, table.sourceReference),
]);

export const tradeWorkOrderComplianceIntents = sqliteTable("trade_work_order_compliance_intents", {
  id: text("id").primaryKey(),
  workOrderId: text("work_order_id").notNull(),
  installerUid: text("installer_uid").notNull(),
  complianceOrganisationId: text("compliance_organisation_id").notNull().default(""),
  programTemplateId: text("program_template_id").notNull(),
  activityTemplateId: text("activity_template_id").notNull(),
  programCode: text("program_code").notNull(),
  registryActivityCode: text("registry_activity_code").notNull().default(""),
  serviceCategory: text("service_category").notNull(),
  siteJurisdiction: text("site_jurisdiction").notNull(),
  plannedStart: text("planned_start").notNull().default(""),
  catalogueReviewedOn: text("catalogue_reviewed_on").notNull(),
  intentSnapshot: text("intent_snapshot").notNull(),
  intentSnapshotSha256: text("intent_snapshot_sha256").notNull(),
  status: text("status").notNull().default("planned"),
  complianceCaseId: text("compliance_case_id").notNull().default(""),
  revision: integer("revision").notNull().default(1),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_compliance_intent_work_revision_idx").on(table.workOrderId, table.revision),
  uniqueIndex("trade_compliance_intent_active_work_idx").on(table.workOrderId).where(sql`${table.status} = 'planned'`),
  uniqueIndex("trade_compliance_intent_case_idx").on(table.complianceCaseId).where(sql`${table.complianceCaseId} <> ''`),
  index("trade_compliance_intent_installer_status_idx").on(table.installerUid, table.status, table.updatedAt),
  index("trade_compliance_intent_creditex_queue_idx").on(table.complianceOrganisationId, table.status, table.plannedStart, table.updatedAt),
  check("trade_compliance_intent_identity_check", sql`trim(${table.workOrderId}) <> '' AND trim(${table.installerUid}) <> '' AND trim(${table.complianceOrganisationId}) <> '' AND trim(${table.programTemplateId}) <> '' AND trim(${table.activityTemplateId}) <> '' AND trim(${table.programCode}) <> '' AND trim(${table.serviceCategory}) <> '' AND ${table.siteJurisdiction} IN ('ACT', 'NSW', 'NT', 'QLD', 'SA', 'TAS', 'VIC', 'WA') AND ${table.revision} > 0`),
  check("trade_compliance_intent_status_check", sql`${table.status} IN ('planned', 'case_linked', 'superseded')`),
  check("trade_compliance_intent_case_check", sql`(${table.status} = 'case_linked' AND trim(${table.complianceCaseId}) <> '') OR (${table.status} <> 'case_linked' AND ${table.complianceCaseId} = '')`),
  check("trade_compliance_intent_snapshot_check", sql`json_valid(${table.intentSnapshot}) AND json_extract(${table.intentSnapshot}, '$.contract') = 'tlink-creditex-job-intent-v1' AND json_extract(${table.intentSnapshot}, '$.program.templateId') = ${table.programTemplateId} AND json_extract(${table.intentSnapshot}, '$.activity.templateId') = ${table.activityTemplateId} AND json_extract(${table.intentSnapshot}, '$.program.programCode') = ${table.programCode} AND json_extract(${table.intentSnapshot}, '$.activity.serviceCategory') = ${table.serviceCategory} AND json_extract(${table.intentSnapshot}, '$.siteJurisdiction') = ${table.siteJurisdiction} AND json_extract(${table.intentSnapshot}, '$.catalogueReviewedOn') = ${table.catalogueReviewedOn} AND length(${table.intentSnapshotSha256}) = 64 AND lower(${table.intentSnapshotSha256}) NOT GLOB '*[^0-9a-f]*' AND ${table.intentSnapshotSha256} = lower(${table.intentSnapshotSha256})`),
  check("trade_compliance_intent_time_check", sql`datetime(${table.createdAt}) IS NOT NULL AND datetime(${table.updatedAt}) IS NOT NULL`),
]);

export const tradeTeamMembers = sqliteTable("trade_team_members", {
  id: text("id").primaryKey(),
  ownerUid: text("owner_uid").notNull(),
  memberUid: text("member_uid").notNull().default(""),
  email: text("email").notNull(),
  displayName: text("display_name").notNull(),
  role: text("role").notNull().default("technician"),
  status: text("status").notNull().default("invited"),
  invitedAt: text("invited_at").notNull(),
  acceptedAt: text("accepted_at").notNull().default(""),
  lastActiveAt: text("last_active_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_team_members_owner_email_idx").on(table.ownerUid, table.email).where(sql`${table.email} <> ''`),
  index("trade_team_members_owner_member_idx").on(table.ownerUid, table.memberUid),
  index("trade_team_members_member_status_idx").on(table.memberUid, table.status, table.updatedAt),
  index("trade_team_members_owner_status_idx").on(table.ownerUid, table.status, table.updatedAt),
]);

export const tradeTeamInvites = sqliteTable("trade_team_invites", {
  id: text("id").primaryKey(),
  teamMemberId: text("team_member_id").notNull(),
  ownerUid: text("owner_uid").notNull(),
  tokenHash: text("token_hash").notNull(),
  expiresAt: text("expires_at").notNull(),
  consumedAt: text("consumed_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_team_invites_token_idx").on(table.tokenHash),
  index("trade_team_invites_member_idx").on(table.teamMemberId, table.expiresAt),
  index("trade_team_invites_owner_idx").on(table.ownerUid, table.expiresAt),
]);

export const tradeTeamSyncChanges = sqliteTable("trade_team_sync_changes", {
  sequence: integer("sequence").primaryKey({ autoIncrement: true }),
  ownerUid: text("owner_uid").notNull(),
  audienceMemberId: text("audience_member_id").notNull().default(""),
  entityType: text("entity_type").notNull(),
  entityId: text("entity_id").notNull(),
  operation: text("operation").notNull().default("upsert"),
  revision: integer("revision").notNull().default(1),
  changedAt: text("changed_at").notNull(),
}, (table) => [
  index("trade_team_sync_changes_owner_sequence_idx").on(table.ownerUid, table.audienceMemberId, table.sequence),
  index("trade_team_sync_changes_entity_idx").on(table.ownerUid, table.entityType, table.entityId, table.sequence),
]);

export const tradeOfflineActions = sqliteTable("trade_offline_actions", {
  id: text("id").primaryKey(),
  ownerUid: text("owner_uid").notNull(),
  actorUid: text("actor_uid").notNull(),
  memberId: text("member_id").notNull().default(""),
  deviceId: text("device_id").notNull().default(""),
  clientActionId: text("client_action_id").notNull(),
  payloadHash: text("payload_hash").notNull(),
  actionType: text("action_type").notNull(),
  entityType: text("entity_type").notNull(),
  entityId: text("entity_id").notNull(),
  baseRevision: integer("base_revision").notNull().default(0),
  resultRevision: integer("result_revision").notNull().default(0),
  status: text("status").notNull().default("applied"),
  leaseUntil: text("lease_until").notNull().default(""),
  errorCode: text("error_code").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull().default(""),
}, (table) => [
  uniqueIndex("trade_offline_actions_owner_client_idx").on(table.ownerUid, table.clientActionId),
  index("trade_offline_actions_actor_idx").on(table.ownerUid, table.actorUid, table.createdAt),
  index("trade_offline_actions_entity_idx").on(table.ownerUid, table.entityType, table.entityId, table.createdAt),
]);

export const tradeMobileDevices = sqliteTable("trade_mobile_devices", {
  id: text("id").primaryKey(),
  ownerUid: text("owner_uid").notNull(),
  actorUid: text("actor_uid").notNull(),
  memberId: text("member_id").notNull().default(""),
  deviceId: text("device_id").notNull(),
  platform: text("platform").notNull(),
  deviceName: text("device_name").notNull().default("Field device"),
  appVersion: text("app_version").notNull(),
  pushProvider: text("push_provider").notNull().default("fcm"),
  pushToken: text("push_token").notNull().default(""),
  pushTokenUpdatedAt: text("push_token_updated_at").notNull().default(""),
  status: text("status").notNull().default("active"),
  registeredAt: text("registered_at").notNull(),
  lastSeenAt: text("last_seen_at").notNull(),
  revokedAt: text("revoked_at").notNull().default(""),
  revokedByUid: text("revoked_by_uid").notNull().default(""),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_mobile_devices_owner_device_idx").on(table.ownerUid, table.deviceId),
  index("trade_mobile_devices_owner_status_idx").on(table.ownerUid, table.status, table.updatedAt),
  index("trade_mobile_devices_actor_status_idx").on(table.actorUid, table.status, table.lastSeenAt),
  index("trade_mobile_devices_member_status_idx").on(table.ownerUid, table.memberId, table.status),
]);

export const tradeMobilePushOutbox = sqliteTable("trade_mobile_push_outbox", {
  id: text("id").primaryKey(),
  ownerUid: text("owner_uid").notNull(),
  audienceMemberId: text("audience_member_id").notNull(),
  eventKey: text("event_key").notNull(),
  eventType: text("event_type").notNull(),
  entityType: text("entity_type").notNull(),
  entityId: text("entity_id").notNull(),
  payload: text("payload").notNull().default("{}"),
  status: text("status").notNull().default("pending"),
  attempts: integer("attempts").notNull().default(0),
  nextAttemptAt: text("next_attempt_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_mobile_push_outbox_event_idx").on(table.eventKey),
  index("trade_mobile_push_outbox_pending_idx").on(table.status, table.nextAttemptAt, table.createdAt),
  index("trade_mobile_push_outbox_audience_idx").on(table.ownerUid, table.audienceMemberId, table.createdAt),
]);

export const tradeMobileUploadSessions = sqliteTable("trade_mobile_upload_sessions", {
  id: text("id").primaryKey(),
  ownerUid: text("owner_uid").notNull(),
  actorUid: text("actor_uid").notNull(),
  memberId: text("member_id").notNull().default(""),
  deviceId: text("device_id").notNull(),
  clientUploadId: text("client_upload_id").notNull(),
  metadataHash: text("metadata_hash").notNull(),
  workOrderId: text("work_order_id").notNull(),
  objectKey: text("object_key").notNull(),
  uploadId: text("upload_id").notNull(),
  fileName: text("file_name").notNull(),
  contentType: text("content_type").notNull(),
  sizeBytes: integer("size_bytes").notNull(),
  category: text("category").notNull().default("progress"),
  caption: text("caption").notNull().default(""),
  evidenceEnvelope: text("evidence_envelope").notNull().default("{}"),
  originalSha256: text("original_sha256").notNull().default(""),
  partSizeBytes: integer("part_size_bytes").notNull(),
  status: text("status").notNull().default("initiated"),
  mediaId: text("media_id").notNull().default(""),
  expiresAt: text("expires_at").notNull(),
  completedAt: text("completed_at").notNull().default(""),
  lastError: text("last_error").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_mobile_upload_sessions_owner_client_idx").on(table.ownerUid, table.clientUploadId),
  uniqueIndex("trade_mobile_upload_sessions_object_idx").on(table.objectKey),
  index("trade_mobile_upload_sessions_device_idx").on(table.ownerUid, table.deviceId, table.status, table.updatedAt),
  index("trade_mobile_upload_sessions_job_idx").on(table.ownerUid, table.workOrderId, table.createdAt),
  index("trade_mobile_upload_sessions_expiry_idx").on(table.status, table.expiresAt),
  check("trade_mobile_upload_sessions_evidence_envelope_check", sql`json_valid(${table.evidenceEnvelope})`),
  check("trade_mobile_upload_sessions_original_hash_check", sql`${table.originalSha256} = '' OR (length(${table.originalSha256}) = 64 AND lower(${table.originalSha256}) NOT GLOB '*[^0-9a-f]*')`),
]);

export const tradeMobileUploadFinalisationGuards = sqliteTable("trade_mobile_upload_finalisation_guards", {
  id: text("id").primaryKey(),
  ownerUid: text("owner_uid").notNull(),
  sessionId: text("session_id").notNull(),
  stepNumber: integer("step_number").notNull(),
  verified: integer("verified").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_mobile_upload_finalisation_guards_session_step_idx").on(table.sessionId, table.stepNumber),
  index("trade_mobile_upload_finalisation_guards_owner_time_idx").on(table.ownerUid, table.createdAt, table.id),
  check("trade_mobile_upload_finalisation_guards_owner_check", sql`trim(${table.ownerUid}) <> ''`),
  check("trade_mobile_upload_finalisation_guards_session_check", sql`trim(${table.sessionId}) <> ''`),
  check("trade_mobile_upload_finalisation_guards_step_check", sql`${table.stepNumber} > 0`),
  check("trade_mobile_upload_finalisation_guards_verified_check", sql`${table.verified} = 1`),
]);

export const tradeMobileUploadParts = sqliteTable("trade_mobile_upload_parts", {
  id: text("id").primaryKey(),
  sessionId: text("session_id").notNull(),
  partNumber: integer("part_number").notNull(),
  etag: text("etag").notNull(),
  sizeBytes: integer("size_bytes").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_mobile_upload_parts_session_part_idx").on(table.sessionId, table.partNumber),
  index("trade_mobile_upload_parts_session_idx").on(table.sessionId, table.partNumber),
]);

export const tradeCrmCounters = sqliteTable("trade_crm_counters", {
  firebaseUid: text("firebase_uid").notNull(),
  counterKey: text("counter_key").notNull(),
  lastValue: integer("last_value").notNull().default(0),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_counters_owner_key_idx").on(table.firebaseUid, table.counterKey),
]);

export const tradeCrmWriteGuards = sqliteTable("trade_crm_write_guards", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  operationId: text("operation_id").notNull(),
  stepNumber: integer("step_number").notNull(),
  verified: integer("verified").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_write_guards_operation_step_idx").on(table.operationId, table.stepNumber),
  index("trade_crm_write_guards_owner_time_idx").on(table.firebaseUid, table.createdAt, table.id),
  check("trade_crm_write_guard_identity_check", sql`trim(${table.firebaseUid}) <> '' AND trim(${table.operationId}) <> '' AND ${table.stepNumber} > 0`),
  check("trade_crm_write_guard_verified_check", sql`${table.verified} = 1`),
  check("trade_crm_write_guard_time_check", sql`datetime(${table.createdAt}) IS NOT NULL`),
]);

export const tradeWorkOrderTasks = sqliteTable("trade_work_order_tasks", {
  id: text("id").primaryKey(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  title: text("title").notNull(),
  dueAt: text("due_at").notNull().default(""),
  status: text("status").notNull().default("pending"),
  completedAt: text("completed_at").notNull().default(""),
  revision: integer("revision").notNull().default(1),
  sortOrder: integer("sort_order").notNull().default(0),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_work_order_tasks_owner_idx").on(table.firebaseUid, table.status, table.dueAt),
  index("trade_work_order_tasks_order_idx").on(table.workOrderId, table.sortOrder, table.createdAt),
]);

export const tradeWorkOrderEvents = sqliteTable("trade_work_order_events", {
  id: text("id").primaryKey(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  eventType: text("event_type").notNull(),
  summary: text("summary").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("trade_work_order_events_owner_idx").on(table.firebaseUid, table.createdAt),
  index("trade_work_order_events_order_idx").on(table.workOrderId, table.createdAt),
]);

export const tradeHandoverPacks = sqliteTable("trade_handover_packs", {
  id: text("id").primaryKey(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  customerProjectId: text("customer_project_id").notNull().default(""),
  serviceCategory: text("service_category").notNull().default("other"),
  status: text("status").notNull().default("draft"),
  submittedAt: text("submitted_at").notNull().default(""),
  publishedAt: text("published_at").notNull().default(""),
  reviewNote: text("review_note").notNull().default(""),
  reviewedByUid: text("reviewed_by_uid").notNull().default(""),
  reviewedAt: text("reviewed_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_handover_packs_work_order_idx").on(table.workOrderId),
  index("trade_handover_packs_owner_idx").on(table.firebaseUid, table.status, table.updatedAt),
  index("trade_handover_packs_customer_project_idx").on(table.customerProjectId, table.status, table.publishedAt),
]);

export const tradeInstalledAssets = sqliteTable("trade_installed_assets", {
  id: text("id").primaryKey(),
  handoverPackId: text("handover_pack_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  crmCustomerId: text("crm_customer_id").notNull().default(""),
  serviceSiteId: text("service_site_id").notNull().default(""),
  sourceType: text("source_type").notNull().default("handover"),
  sourceReference: text("source_reference").notNull().default(""),
  reviewStatus: text("review_status").notNull().default("pending_review"),
  assetStatus: text("asset_status").notNull().default("active"),
  assetLabel: text("asset_label").notNull().default(""),
  commissioningReference: text("commissioning_reference").notNull().default(""),
  assetCategory: text("asset_category").notNull(),
  brand: text("brand").notNull(),
  modelNumber: text("model_number").notNull(),
  serialNumber: text("serial_number").notNull().default(""),
  quantity: integer("quantity").notNull().default(1),
  installedAt: text("installed_at").notNull().default(""),
  warrantyProvider: text("warranty_provider").notNull().default(""),
  warrantyReference: text("warranty_reference").notNull().default(""),
  warrantyStart: text("warranty_start").notNull().default(""),
  warrantyEnd: text("warranty_end").notNull().default(""),
  supplierProductId: text("supplier_product_id").notNull().default(""),
  recordStatus: text("record_status").notNull().default("active"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_installed_assets_pack_idx").on(table.handoverPackId, table.recordStatus, table.createdAt),
  index("trade_installed_assets_owner_idx").on(table.firebaseUid, table.workOrderId, table.recordStatus),
  index("trade_installed_assets_warranty_idx").on(table.firebaseUid, table.warrantyEnd),
  index("trade_installed_assets_customer_idx").on(table.firebaseUid, table.crmCustomerId, table.assetStatus, table.updatedAt),
  index("trade_installed_assets_site_idx").on(table.firebaseUid, table.serviceSiteId, table.assetStatus, table.updatedAt),
  index("trade_installed_assets_review_idx").on(table.firebaseUid, table.reviewStatus, table.updatedAt),
]);

export const tradeJobNotificationReads = sqliteTable("trade_job_notification_reads", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  notificationKey: text("notification_key").notNull(),
  readByUid: text("read_by_uid").notNull(),
  readAt: text("read_at").notNull(),
}, (table) => [
  uniqueIndex("trade_job_notification_reads_actor_key_idx").on(table.firebaseUid, table.readByUid, table.notificationKey),
  index("trade_job_notification_reads_actor_time_idx").on(table.firebaseUid, table.readByUid, table.readAt),
]);

export const tradeTeamWorkingHours = sqliteTable("trade_team_working_hours", {
  id: text("id").primaryKey(),
  ownerUid: text("owner_uid").notNull(),
  teamMemberId: text("team_member_id").notNull(),
  weekday: integer("weekday").notNull(),
  startMinute: integer("start_minute").notNull(),
  endMinute: integer("end_minute").notNull(),
  isAvailable: integer("is_available", { mode: "boolean" }).notNull().default(true),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_team_working_hours_member_day_idx").on(table.ownerUid, table.teamMemberId, table.weekday),
  index("trade_team_working_hours_owner_day_idx").on(table.ownerUid, table.weekday, table.teamMemberId),
]);

export const tradeTeamUnavailability = sqliteTable("trade_team_unavailability", {
  id: text("id").primaryKey(),
  ownerUid: text("owner_uid").notNull(),
  teamMemberId: text("team_member_id").notNull(),
  startsAt: text("starts_at").notNull(),
  endsAt: text("ends_at").notNull(),
  reason: text("reason").notNull().default("Unavailable"),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_team_unavailability_owner_range_idx").on(table.ownerUid, table.startsAt, table.endsAt),
  index("trade_team_unavailability_member_range_idx").on(table.ownerUid, table.teamMemberId, table.startsAt, table.endsAt),
]);

export const tradeComplianceItems = sqliteTable("trade_compliance_items", {
  id: text("id").primaryKey(),
  handoverPackId: text("handover_pack_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  templateKey: text("template_key").notNull(),
  label: text("label").notNull(),
  guidance: text("guidance").notNull().default(""),
  status: text("status").notNull().default("pending"),
  completedAt: text("completed_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_compliance_items_pack_key_idx").on(table.handoverPackId, table.templateKey),
  index("trade_compliance_items_owner_idx").on(table.firebaseUid, table.workOrderId, table.status),
]);

export const tradeHandoverDocuments = sqliteTable("trade_handover_documents", {
  id: text("id").primaryKey(),
  handoverPackId: text("handover_pack_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  category: text("category").notNull(),
  fileName: text("file_name").notNull(),
  contentType: text("content_type").notNull(),
  sizeBytes: integer("size_bytes").notNull(),
  objectKey: text("object_key").notNull().unique(),
  customerVisible: integer("customer_visible", { mode: "boolean" }).notNull().default(true),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_handover_documents_pack_idx").on(table.handoverPackId, table.createdAt),
  index("trade_handover_documents_owner_idx").on(table.firebaseUid, table.workOrderId, table.createdAt),
]);

export const tradeAssetServicePlans = sqliteTable("trade_asset_service_plans", {
  id: text("id").primaryKey(),
  assetId: text("asset_id").notNull(),
  handoverPackId: text("handover_pack_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  serviceType: text("service_type").notNull(),
  cadenceMonths: integer("cadence_months").notNull(),
  nextDueAt: text("next_due_at").notNull(),
  status: text("status").notNull().default("active"),
  jobTemplateId: text("job_template_id").notNull().default(""),
  autoCreateEnabled: integer("auto_create_enabled", { mode: "boolean" }).notNull().default(false),
  jobLeadDays: integer("job_lead_days").notNull().default(14),
  lastGeneratedDueAt: text("last_generated_due_at").notNull().default(""),
  lastGeneratedWorkOrderId: text("last_generated_work_order_id").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_asset_service_plans_asset_type_idx").on(table.assetId, table.serviceType),
  index("trade_asset_service_plans_owner_due_idx").on(table.firebaseUid, table.status, table.nextDueAt),
  index("trade_asset_service_plans_pack_idx").on(table.handoverPackId, table.status, table.nextDueAt),
]);

export const tradeServiceJobGenerations = sqliteTable("trade_service_job_generations", {
  id: text("id").primaryKey(),
  servicePlanId: text("service_plan_id").notNull(),
  sourceWorkOrderId: text("source_work_order_id").notNull(),
  generatedWorkOrderId: text("generated_work_order_id").notNull().default(""),
  firebaseUid: text("firebase_uid").notNull(),
  dueAt: text("due_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_service_job_generations_plan_due_idx").on(table.servicePlanId, table.dueAt),
  index("trade_service_job_generations_owner_idx").on(table.firebaseUid, table.createdAt),
  index("trade_service_job_generations_work_idx").on(table.generatedWorkOrderId),
]);

export const tradeAssetServiceEvents = sqliteTable("trade_asset_service_events", {
  id: text("id").primaryKey(),
  servicePlanId: text("service_plan_id").notNull(),
  assetId: text("asset_id").notNull(),
  handoverPackId: text("handover_pack_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  eventType: text("event_type").notNull().default("service_completed"),
  servicedAt: text("serviced_at").notNull(),
  summary: text("summary").notNull().default(""),
  providerReference: text("provider_reference").notNull().default(""),
  nextDueAt: text("next_due_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_asset_service_events_plan_idx").on(table.servicePlanId, table.servicedAt),
  index("trade_asset_service_events_asset_idx").on(table.assetId, table.servicedAt),
  index("trade_asset_service_events_owner_idx").on(table.firebaseUid, table.createdAt),
]);

export const tradeServiceFollowUps = sqliteTable("trade_service_follow_ups", {
  id: text("id").primaryKey(),
  servicePlanId: text("service_plan_id").notNull(),
  assetId: text("asset_id").notNull(),
  crmCustomerId: text("crm_customer_id").notNull(),
  serviceSiteId: text("service_site_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  dueAt: text("due_at").notNull(),
  status: text("status").notNull().default("preparing"),
  assigneeMemberId: text("assignee_member_id").notNull().default(""),
  suppressionReason: text("suppression_reason").notNull().default(""),
  internalNotes: text("internal_notes").notNull().default(""),
  reminderSubject: text("reminder_subject").notNull().default(""),
  reminderBody: text("reminder_body").notNull().default(""),
  revision: integer("revision").notNull().default(0),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_service_follow_ups_plan_due_idx").on(table.firebaseUid, table.servicePlanId, table.dueAt),
  index("trade_service_follow_ups_owner_status_due_idx").on(table.firebaseUid, table.status, table.dueAt),
  index("trade_service_follow_ups_owner_assignee_due_idx").on(table.firebaseUid, table.assigneeMemberId, table.dueAt),
  index("trade_service_follow_ups_owner_customer_site_idx").on(table.firebaseUid, table.crmCustomerId, table.serviceSiteId),
  index("trade_service_follow_ups_report_due_idx").on(table.dueAt, table.status, table.assigneeMemberId),
]);

export const tradeServiceFollowUpEvents = sqliteTable("trade_service_follow_up_events", {
  id: text("id").primaryKey(),
  followUpId: text("follow_up_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  actorUid: text("actor_uid").notNull(),
  eventType: text("event_type").notNull(),
  summary: text("summary").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("trade_service_follow_up_events_record_idx").on(table.followUpId, table.createdAt),
  index("trade_service_follow_up_events_owner_idx").on(table.firebaseUid, table.createdAt),
]);

export const serviceReminderChannelSettings = sqliteTable("service_reminder_channel_settings", {
  channel: text("channel").primaryKey(),
  provider: text("provider").notNull(),
  enabled: integer("enabled", { mode: "boolean" }).notNull().default(false),
  senderLabel: text("sender_label").notNull().default("Australian Energy Assessments"),
  dailyLimit: integer("daily_limit").notNull().default(100),
  revision: integer("revision").notNull().default(0),
  updatedByUid: text("updated_by_uid").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("service_reminder_channel_settings_enabled_idx").on(table.enabled, table.channel),
]);

export const serviceReminderDeliveries = sqliteTable("service_reminder_deliveries", {
  id: text("id").primaryKey(),
  followUpId: text("follow_up_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  customerUid: text("customer_uid").notNull(),
  assetId: text("asset_id").notNull(),
  channel: text("channel").notNull(),
  provider: text("provider").notNull(),
  contentRevision: integer("content_revision").notNull(),
  idempotencyKey: text("idempotency_key").notNull(),
  status: text("status").notNull().default("queued"),
  attempts: integer("attempts").notNull().default(0),
  providerMessageId: text("provider_message_id").notNull().default(""),
  providerStatus: text("provider_status").notNull().default(""),
  lastError: text("last_error").notNull().default(""),
  queuedAt: text("queued_at").notNull(),
  sentAt: text("sent_at").notNull().default(""),
  deliveredAt: text("delivered_at").notNull().default(""),
  failedAt: text("failed_at").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("service_reminder_deliveries_idempotency_idx").on(table.idempotencyKey),
  uniqueIndex("service_reminder_deliveries_provider_message_idx").on(table.provider, table.providerMessageId),
  index("service_reminder_deliveries_follow_up_idx").on(table.followUpId, table.createdAt),
  index("service_reminder_deliveries_owner_status_idx").on(table.firebaseUid, table.status, table.createdAt),
  index("service_reminder_deliveries_customer_channel_idx").on(table.customerUid, table.channel, table.createdAt),
  index("service_reminder_deliveries_report_time_idx").on(table.createdAt, table.channel, table.status),
]);

export const serviceReminderDeliveryEvents = sqliteTable("service_reminder_delivery_events", {
  id: text("id").primaryKey(),
  deliveryId: text("delivery_id").notNull(),
  providerEventKey: text("provider_event_key").notNull(),
  eventType: text("event_type").notNull(),
  providerStatus: text("provider_status").notNull().default(""),
  summary: text("summary").notNull(),
  occurredAt: text("occurred_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("service_reminder_delivery_events_provider_idx").on(table.providerEventKey),
  index("service_reminder_delivery_events_delivery_idx").on(table.deliveryId, table.occurredAt),
]);

export const appointmentNotificationEvents = sqliteTable("appointment_notification_events", {
  id: text("id").primaryKey(),
  eventKey: text("event_key").notNull(),
  appointmentId: text("appointment_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  proposalId: text("proposal_id").notNull(),
  projectId: text("project_id").notNull(),
  installerUid: text("installer_uid").notNull(),
  customerUid: text("customer_uid").notNull(),
  eventType: text("event_type").notNull(),
  appointmentRevision: integer("appointment_revision").notNull(),
  startsAt: text("starts_at").notNull(),
  endsAt: text("ends_at").notNull(),
  summary: text("summary").notNull(),
  occurredAt: text("occurred_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("appointment_notification_events_key_idx").on(table.eventKey),
  index("appointment_notification_events_appointment_idx").on(table.appointmentId, table.appointmentRevision),
  index("appointment_notification_events_project_idx").on(table.customerUid, table.projectId, table.occurredAt),
]);

export const appointmentNotificationDeliveries = sqliteTable("appointment_notification_deliveries", {
  id: text("id").primaryKey(),
  eventId: text("event_id").notNull(),
  appointmentId: text("appointment_id").notNull(),
  audience: text("audience").notNull(),
  recipientUid: text("recipient_uid").notNull(),
  channel: text("channel").notNull(),
  provider: text("provider").notNull(),
  contentRevision: integer("content_revision").notNull(),
  subject: text("subject").notNull(),
  body: text("body").notNull(),
  idempotencyKey: text("idempotency_key").notNull(),
  status: text("status").notNull().default("queued"),
  eligibilityReason: text("eligibility_reason").notNull().default(""),
  attempts: integer("attempts").notNull().default(0),
  providerMessageId: text("provider_message_id").notNull().default(""),
  providerStatus: text("provider_status").notNull().default(""),
  lastError: text("last_error").notNull().default(""),
  queuedAt: text("queued_at").notNull(),
  sentAt: text("sent_at").notNull().default(""),
  deliveredAt: text("delivered_at").notNull().default(""),
  failedAt: text("failed_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("appointment_notification_deliveries_idempotency_idx").on(table.idempotencyKey),
  uniqueIndex("appointment_notification_deliveries_provider_message_idx").on(table.provider, table.providerMessageId),
  index("appointment_notification_deliveries_event_idx").on(table.eventId, table.audience, table.channel),
  index("appointment_notification_deliveries_status_idx").on(table.status, table.updatedAt),
  index("appointment_notification_deliveries_recipient_idx").on(table.recipientUid, table.channel, table.createdAt),
]);

export const appointmentNotificationDeliveryEvents = sqliteTable("appointment_notification_delivery_events", {
  id: text("id").primaryKey(),
  deliveryId: text("delivery_id").notNull(),
  providerEventKey: text("provider_event_key").notNull(),
  eventType: text("event_type").notNull(),
  providerStatus: text("provider_status").notNull().default(""),
  summary: text("summary").notNull(),
  occurredAt: text("occurred_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("appointment_notification_delivery_events_provider_idx").on(table.providerEventKey),
  index("appointment_notification_delivery_events_delivery_idx").on(table.deliveryId, table.occurredAt),
]);

export const customerServiceReminderContacts = sqliteTable("customer_service_reminder_contacts", {
  customerUid: text("customer_uid").primaryKey(),
  mobileE164: text("mobile_e164").notNull().default(""),
  mobileVerifiedAt: text("mobile_verified_at").notNull().default(""),
  pendingMobileE164: text("pending_mobile_e164").notNull().default(""),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("customer_service_reminder_contacts_mobile_idx").on(table.mobileE164),
]);

export const customerServiceReminderOptOuts = sqliteTable("customer_service_reminder_opt_outs", {
  id: text("id").primaryKey(),
  customerUid: text("customer_uid").notNull(),
  channel: text("channel").notNull(),
  source: text("source").notNull(),
  providerReference: text("provider_reference").notNull().default(""),
  optedOutAt: text("opted_out_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("customer_service_reminder_opt_outs_customer_channel_idx").on(table.customerUid, table.channel),
  index("customer_service_reminder_opt_outs_channel_idx").on(table.channel, table.optedOutAt),
  index("customer_service_reminder_opt_outs_report_time_idx").on(table.optedOutAt, table.channel),
]);

export const customerAssetLifecyclePreferences = sqliteTable("customer_asset_lifecycle_preferences", {
  id: text("id").primaryKey(),
  customerUid: text("customer_uid").notNull(),
  assetId: text("asset_id").notNull(),
  remindersEnabled: integer("reminders_enabled", { mode: "boolean" }).notNull().default(true),
  emailEnabled: integer("email_enabled", { mode: "boolean" }).notNull().default(false),
  smsEnabled: integer("sms_enabled", { mode: "boolean" }).notNull().default(false),
  reminderLeadDays: integer("reminder_lead_days").notNull().default(30),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("customer_asset_lifecycle_preferences_owner_asset_idx").on(table.customerUid, table.assetId),
  index("customer_asset_lifecycle_preferences_owner_idx").on(table.customerUid, table.updatedAt),
]);

export const assetSafetyNotices = sqliteTable("asset_safety_notices", {
  id: text("id").primaryKey(),
  createdByUid: text("created_by_uid").notNull(),
  title: text("title").notNull(),
  summary: text("summary").notNull(),
  severity: text("severity").notNull().default("advisory"),
  assetCategory: text("asset_category").notNull().default(""),
  brand: text("brand").notNull().default(""),
  modelNumber: text("model_number").notNull().default(""),
  sourceUrl: text("source_url").notNull(),
  sourceLabel: text("source_label").notNull().default("Official safety source"),
  effectiveAt: text("effective_at").notNull().default(""),
  expiresAt: text("expires_at").notNull().default(""),
  status: text("status").notNull().default("draft"),
  publishedAt: text("published_at").notNull().default(""),
  withdrawnAt: text("withdrawn_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("asset_safety_notices_status_idx").on(table.status, table.publishedAt),
  index("asset_safety_notices_scope_idx").on(table.assetCategory, table.brand, table.modelNumber),
]);

export const assetSafetyAcknowledgements = sqliteTable("asset_safety_acknowledgements", {
  id: text("id").primaryKey(),
  noticeId: text("notice_id").notNull(),
  assetId: text("asset_id").notNull(),
  customerUid: text("customer_uid").notNull(),
  status: text("status").notNull().default("acknowledged"),
  acknowledgedAt: text("acknowledged_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("asset_safety_acknowledgements_owner_notice_asset_idx").on(table.customerUid, table.noticeId, table.assetId),
  index("asset_safety_acknowledgements_notice_idx").on(table.noticeId, table.acknowledgedAt),
  index("asset_safety_acknowledgements_owner_idx").on(table.customerUid, table.updatedAt),
]);

export const customerAssetOwnerships = sqliteTable("customer_asset_ownerships", {
  id: text("id").primaryKey(),
  handoverPackId: text("handover_pack_id").notNull(),
  customerUid: text("customer_uid").notNull(),
  activeKey: text("active_key"),
  status: text("status").notNull().default("active"),
  sourceType: text("source_type").notNull().default("original"),
  transferId: text("transfer_id").notNull().default(""),
  startedAt: text("started_at").notNull(),
  endedAt: text("ended_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("customer_asset_ownerships_active_key_idx").on(table.activeKey),
  index("customer_asset_ownerships_owner_idx").on(table.customerUid, table.status, table.updatedAt),
  index("customer_asset_ownerships_pack_idx").on(table.handoverPackId, table.startedAt),
]);

export const customerAssetTransferRequests = sqliteTable("customer_asset_transfer_requests", {
  id: text("id").primaryKey(),
  handoverPackId: text("handover_pack_id").notNull(),
  fromCustomerUid: text("from_customer_uid").notNull(),
  toCustomerUid: text("to_customer_uid").notNull().default(""),
  claimCodeHash: text("claim_code_hash").notNull(),
  status: text("status").notNull().default("awaiting_recipient"),
  senderConsentAt: text("sender_consent_at").notNull(),
  recipientConsentAt: text("recipient_consent_at").notNull().default(""),
  expiresAt: text("expires_at").notNull(),
  reviewNote: text("review_note").notNull().default(""),
  reviewedByUid: text("reviewed_by_uid").notNull().default(""),
  reviewedAt: text("reviewed_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("customer_asset_transfer_requests_code_idx").on(table.claimCodeHash),
  index("customer_asset_transfer_requests_pack_idx").on(table.handoverPackId, table.status, table.updatedAt),
  index("customer_asset_transfer_requests_sender_idx").on(table.fromCustomerUid, table.status, table.updatedAt),
  index("customer_asset_transfer_requests_recipient_idx").on(table.toCustomerUid, table.status, table.updatedAt),
  index("customer_asset_transfer_requests_expiry_idx").on(table.status, table.expiresAt),
]);

export const customerAssetTransferEvents = sqliteTable("customer_asset_transfer_events", {
  id: text("id").primaryKey(),
  transferId: text("transfer_id").notNull(),
  eventType: text("event_type").notNull(),
  actorType: text("actor_type").notNull(),
  actorUid: text("actor_uid").notNull().default(""),
  summary: text("summary").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("customer_asset_transfer_events_transfer_idx").on(table.transferId, table.createdAt),
  index("customer_asset_transfer_events_actor_idx").on(table.actorUid, table.createdAt),
]);

export const tradeHandoverCorrections = sqliteTable("trade_handover_corrections", {
  id: text("id").primaryKey(),
  handoverPackId: text("handover_pack_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  assetId: text("asset_id").notNull(),
  versionNumber: integer("version_number").notNull(),
  fieldKey: text("field_key").notNull(),
  previousValue: text("previous_value").notNull().default(""),
  proposedValue: text("proposed_value").notNull().default(""),
  reason: text("reason").notNull(),
  status: text("status").notNull().default("submitted"),
  submittedAt: text("submitted_at").notNull(),
  publishedAt: text("published_at").notNull().default(""),
  reviewNote: text("review_note").notNull().default(""),
  reviewedByUid: text("reviewed_by_uid").notNull().default(""),
  reviewedAt: text("reviewed_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_handover_corrections_pack_version_idx").on(table.handoverPackId, table.versionNumber),
  index("trade_handover_corrections_owner_idx").on(table.firebaseUid, table.status, table.updatedAt),
  index("trade_handover_corrections_pack_idx").on(table.handoverPackId, table.status, table.versionNumber),
  index("trade_handover_corrections_asset_idx").on(table.assetId, table.versionNumber),
]);

export const tradeOpportunities = sqliteTable("trade_opportunities", {
  id: text("id").primaryKey(),
  title: text("title").notNull(),
  projectType: text("project_type").notNull(),
  suburb: text("suburb").notNull().default(""),
  postcode: text("postcode").notNull().default(""),
  state: text("state").notNull(),
  serviceCategories: text("service_categories").notNull().default("[]"),
  priority: text("priority").notNull().default("standard"),
  timing: text("timing").notNull().default("planning"),
  summary: text("summary").notNull(),
  status: text("status").notNull().default("draft"),
  sourceReference: text("source_reference").notNull().default(""),
  contactLimit: integer("contact_limit").notNull().default(2),
  maximumConnectedInstallers: integer("maximum_connected_installers").notNull().default(3),
  expiresAt: text("expires_at").notNull().default(""),
  expiredAt: text("expired_at").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  isSynthetic: integer("is_synthetic", { mode: "boolean" }).notNull().default(false),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_opportunities_status_idx").on(table.status, table.updatedAt, table.id),
  index("trade_opportunities_state_idx").on(table.state, table.status, table.updatedAt, table.id),
  index("trade_opportunities_title_nocase_idx").on(sql`${table.title} COLLATE NOCASE`, table.updatedAt, table.id),
  index("trade_opportunities_expiry_idx").on(table.status, table.expiresAt, table.id),
]);

export const tradeOpportunityMatches = sqliteTable("trade_opportunity_matches", {
  id: text("id").primaryKey(),
  opportunityId: text("opportunity_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  status: text("status").notNull().default("offered"),
  adminNote: text("admin_note").notNull().default(""),
  partnerNote: text("partner_note").notNull().default(""),
  matchedCategories: text("matched_categories").notNull().default("[]"),
  distanceMetres: integer("distance_metres").notNull().default(0),
  allocationRank: integer("allocation_rank").notNull().default(0),
  matchSource: text("match_source").notNull().default("manual"),
  contactAttemptCount: integer("contact_attempt_count").notNull().default(0),
  lastContactAt: text("last_contact_at").notNull().default(""),
  connectedAt: text("connected_at").notNull().default(""),
  matchedByUid: text("matched_by_uid").notNull(),
  matchedAt: text("matched_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_opportunity_matches_unique_idx").on(table.opportunityId, table.firebaseUid),
  index("trade_opportunity_matches_owner_idx").on(table.firebaseUid, table.updatedAt),
  index("trade_opportunity_matches_opportunity_idx").on(table.opportunityId),
  index("trade_opportunity_matches_status_idx").on(table.status),
]);

export const customerOpportunityDispatchJobs = sqliteTable("customer_opportunity_dispatch_jobs", {
  id: text("id").primaryKey(),
  opportunityId: text("opportunity_id").notNull(),
  adminNotificationId: text("admin_notification_id").notNull(),
  status: text("status").notNull().default("pending"),
  attempts: integer("attempts").notNull().default(0),
  nextAttemptAt: text("next_attempt_at").notNull().default(""),
  claimedAt: text("claimed_at").notNull().default(""),
  completedAt: text("completed_at").notNull().default(""),
  failedAt: text("failed_at").notNull().default(""),
  lastError: text("last_error").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("customer_opportunity_dispatch_jobs_opportunity_idx").on(table.opportunityId),
  index("customer_opportunity_dispatch_jobs_status_idx").on(table.status, table.nextAttemptAt, table.createdAt),
]);

export const tradeOpportunityNotificationDeliveries = sqliteTable("trade_opportunity_notification_deliveries", {
  id: text("id").primaryKey(),
  matchId: text("match_id").notNull(),
  status: text("status").notNull().default("pending"),
  eligibilityReason: text("eligibility_reason").notNull().default(""),
  attempts: integer("attempts").notNull().default(0),
  nextAttemptAt: text("next_attempt_at").notNull().default(""),
  provider: text("provider").notNull().default("resend"),
  providerMessageId: text("provider_message_id").notNull().default(""),
  providerStatus: text("provider_status").notNull().default(""),
  recipientEmailHash: text("recipient_email_hash").notNull().default(""),
  idempotencyKey: text("idempotency_key").notNull().default(""),
  subject: text("subject").notNull().default(""),
  body: text("body").notNull().default(""),
  enqueuedAt: text("enqueued_at").notNull(),
  lastAttemptAt: text("last_attempt_at").notNull().default(""),
  sentAt: text("sent_at").notNull().default(""),
  deliveredAt: text("delivered_at").notNull().default(""),
  failedAt: text("failed_at").notNull().default(""),
  lastError: text("last_error").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_opportunity_notification_deliveries_match_idx").on(table.matchId),
  uniqueIndex("trade_opportunity_notification_deliveries_idempotency_idx")
    .on(table.idempotencyKey).where(sql`${table.idempotencyKey} <> ''`),
  uniqueIndex("trade_opportunity_notification_deliveries_provider_message_idx")
    .on(table.provider, table.providerMessageId).where(sql`${table.providerMessageId} <> ''`),
  index("trade_opportunity_notification_deliveries_status_idx").on(table.status, table.nextAttemptAt, table.enqueuedAt),
  index("trade_opportunity_notification_deliveries_recipient_idx").on(table.recipientEmailHash, table.createdAt),
]);

export const tradeOpportunityNotificationDeliveryEvents = sqliteTable("trade_opportunity_notification_delivery_events", {
  id: text("id").primaryKey(),
  deliveryId: text("delivery_id").notNull(),
  providerEventKey: text("provider_event_key").notNull(),
  eventType: text("event_type").notNull(),
  providerStatus: text("provider_status").notNull().default(""),
  summary: text("summary").notNull(),
  occurredAt: text("occurred_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_opportunity_notification_delivery_events_provider_idx").on(table.providerEventKey),
  index("trade_opportunity_notification_delivery_events_delivery_idx").on(table.deliveryId, table.occurredAt),
]);

export const tradeOpportunityEmailSuppressions = sqliteTable("trade_opportunity_email_suppressions", {
  emailHash: text("email_hash").primaryKey(),
  reason: text("reason").notNull(),
  provider: text("provider").notNull().default("resend"),
  providerStatus: text("provider_status").notNull(),
  providerMessageId: text("provider_message_id").notNull().default(""),
  suppressedAt: text("suppressed_at").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_opportunity_email_suppressions_time_idx").on(table.suppressedAt),
]);

export const tradeCrmCustomers = sqliteTable("trade_crm_customers", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  customerNumber: text("customer_number").notNull(),
  customerType: text("customer_type").notNull().default("residential"),
  firstName: text("first_name").notNull().default(""),
  lastName: text("last_name").notNull().default(""),
  businessName: text("business_name").notNull().default(""),
  businessNumber: text("business_number").notNull().default(""),
  email: text("email").notNull().default(""),
  phone: text("phone").notNull().default(""),
  addressLine1: text("address_line_1").notNull().default(""),
  addressLine2: text("address_line_2").notNull().default(""),
  suburb: text("suburb").notNull().default(""),
  addressState: text("address_state").notNull().default(""),
  postcode: text("postcode").notNull().default(""),
  tags: text("tags").notNull().default("[]"),
  privateNotes: text("private_notes").notNull().default(""),
  recordStatus: text("record_status").notNull().default("active"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_customers_owner_number_idx").on(table.firebaseUid, table.customerNumber),
  index("trade_crm_customers_owner_status_idx").on(table.firebaseUid, table.recordStatus, table.updatedAt),
  index("trade_crm_customers_owner_name_idx").on(table.firebaseUid, table.lastName, table.businessName),
]);

export const tradeCrmEnquiries = sqliteTable("trade_crm_enquiries", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  sourceType: text("source_type").notNull().default("direct"),
  sourceReference: text("source_reference").notNull().default(""),
  externalRecordId: text("external_record_id").notNull().default(""),
  opportunityMatchId: text("opportunity_match_id").notNull().default(""),
  status: text("status").notNull().default("new"),
  customerId: text("customer_id").notNull().default(""),
  customerContactId: text("customer_contact_id").notNull().default(""),
  serviceSiteId: text("service_site_id").notNull().default(""),
  customerType: text("customer_type").notNull().default("residential"),
  firstName: text("first_name").notNull().default(""),
  lastName: text("last_name").notNull().default(""),
  businessName: text("business_name").notNull().default(""),
  businessNumber: text("business_number").notNull().default(""),
  email: text("email").notNull().default(""),
  phone: text("phone").notNull().default(""),
  addressLine1: text("address_line_1").notNull().default(""),
  addressLine2: text("address_line_2").notNull().default(""),
  suburb: text("suburb").notNull().default(""),
  addressState: text("address_state").notNull().default(""),
  postcode: text("postcode").notNull().default(""),
  serviceCategory: text("service_category").notNull().default("other"),
  serviceCategories: text("service_categories").notNull().default("[]"),
  description: text("description").notNull().default(""),
  urgency: text("urgency").notNull().default("standard"),
  preferredDate: text("preferred_date").notNull().default(""),
  serviceRegion: text("service_region").notNull().default(""),
  assignedLabel: text("assigned_label").notNull().default(""),
  nextFollowUpAt: text("next_follow_up_at").notNull().default(""),
  lostReason: text("lost_reason").notNull().default(""),
  protectedSource: integer("protected_source", { mode: "boolean" }).notNull().default(false),
  duplicateDecision: text("duplicate_decision").notNull().default("unchecked"),
  recordStatus: text("record_status").notNull().default("active"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_enquiries_owner_source_idx").on(table.firebaseUid, table.sourceType, table.sourceReference),
  index("trade_crm_enquiries_owner_status_idx").on(table.firebaseUid, table.recordStatus, table.status, table.updatedAt),
  index("trade_crm_enquiries_owner_external_idx").on(table.firebaseUid, table.externalRecordId),
  index("trade_crm_enquiries_customer_idx").on(table.firebaseUid, table.customerId, table.updatedAt),
]);

export const tradeCrmEnquiryMessages = sqliteTable("trade_crm_enquiry_messages", {
  id: text("id").primaryKey(),
  enquiryId: text("enquiry_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  channel: text("channel").notNull().default("note"),
  direction: text("direction").notNull().default("internal"),
  subject: text("subject").notNull().default(""),
  body: text("body").notNull(),
  occurredAt: text("occurred_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("trade_crm_enquiry_messages_owner_idx").on(table.firebaseUid, table.enquiryId, table.occurredAt),
]);

export const tradeCrmEnquiryAttachments = sqliteTable("trade_crm_enquiry_attachments", {
  id: text("id").primaryKey(),
  enquiryId: text("enquiry_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  fileName: text("file_name").notNull(),
  contentType: text("content_type").notNull().default("application/octet-stream"),
  sizeBytes: integer("size_bytes").notNull().default(0),
  objectKey: text("object_key").notNull().default(""),
  status: text("status").notNull().default("metadata_only"),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("trade_crm_enquiry_attachments_owner_idx").on(table.firebaseUid, table.enquiryId, table.createdAt),
]);

export const tradeCrmEnquiryEvents = sqliteTable("trade_crm_enquiry_events", {
  id: text("id").primaryKey(),
  enquiryId: text("enquiry_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  eventType: text("event_type").notNull(),
  summary: text("summary").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("trade_crm_enquiry_events_owner_idx").on(table.firebaseUid, table.enquiryId, table.createdAt),
]);

export const tradeCrmCustomerContacts = sqliteTable("trade_crm_customer_contacts", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  customerId: text("customer_id").notNull(),
  firstName: text("first_name").notNull().default(""),
  lastName: text("last_name").notNull().default(""),
  roleLabel: text("role_label").notNull().default(""),
  email: text("email").notNull().default(""),
  phone: text("phone").notNull().default(""),
  isPrimary: integer("is_primary").notNull().default(0),
  recordStatus: text("record_status").notNull().default("active"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_crm_customer_contacts_owner_customer_idx").on(table.firebaseUid, table.customerId, table.recordStatus),
  index("trade_crm_customer_contacts_owner_email_idx").on(table.firebaseUid, table.email),
  index("trade_crm_customer_contacts_owner_phone_idx").on(table.firebaseUid, table.phone),
]);

export const tradeCrmServiceSites = sqliteTable("trade_crm_service_sites", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  customerId: text("customer_id").notNull(),
  siteLabel: text("site_label").notNull().default("Primary site"),
  addressLine1: text("address_line_1").notNull().default(""),
  addressLine2: text("address_line_2").notNull().default(""),
  suburb: text("suburb").notNull().default(""),
  addressState: text("address_state").notNull().default(""),
  postcode: text("postcode").notNull().default(""),
  addressEntryMode: text("address_entry_mode").notNull().default("manual_pending_review"),
  addressProvider: text("address_provider").notNull().default(""),
  addressProviderReference: text("address_provider_reference").notNull().default(""),
  addressFormatted: text("address_formatted").notNull().default(""),
  addressVerifiedAt: text("address_verified_at").notNull().default(""),
  accessInstructions: text("access_instructions").notNull().default(""),
  parkingInstructions: text("parking_instructions").notNull().default(""),
  hazardNotes: text("hazard_notes").notNull().default(""),
  isPrimary: integer("is_primary").notNull().default(0),
  recordStatus: text("record_status").notNull().default("active"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_crm_service_sites_owner_customer_idx").on(table.firebaseUid, table.customerId, table.recordStatus),
  index("trade_crm_service_sites_owner_postcode_idx").on(table.firebaseUid, table.postcode),
  index("trade_crm_service_sites_address_verification_idx").on(table.firebaseUid, table.addressEntryMode, table.addressVerifiedAt),
]);

export const tradeCrmSiteContacts = sqliteTable("trade_crm_site_contacts", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  serviceSiteId: text("service_site_id").notNull(),
  customerContactId: text("customer_contact_id").notNull(),
  roleLabel: text("role_label").notNull().default("Service contact"),
  isPrimary: integer("is_primary").notNull().default(0),
  recordStatus: text("record_status").notNull().default("active"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_site_contacts_owner_site_contact_idx").on(table.firebaseUid, table.serviceSiteId, table.customerContactId),
  index("trade_crm_site_contacts_owner_contact_idx").on(table.firebaseUid, table.customerContactId, table.recordStatus),
]);

export const tradeCrmJobDetails = sqliteTable("trade_crm_job_details", {
  id: text("id").primaryKey(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  crmCustomerId: text("crm_customer_id").notNull().default(""),
  serviceSiteId: text("service_site_id").notNull().default(""),
  customerSource: text("customer_source").notNull().default("internal"),
  pipelineStage: text("pipeline_stage").notNull().default("enquiry"),
  buildingType: text("building_type").notNull().default("not_sure"),
  description: text("description").notNull().default(""),
  customerReference: text("customer_reference").notNull().default(""),
  nextAction: text("next_action").notNull().default(""),
  tags: text("tags").notNull().default("[]"),
  estimatedValueCents: integer("estimated_value_cents").notNull().default(0),
  quotedValueCents: integer("quoted_value_cents").notNull().default(0),
  invoicedValueCents: integer("invoiced_value_cents").notNull().default(0),
  paidValueCents: integer("paid_value_cents").notNull().default(0),
  quoteStatus: text("quote_status").notNull().default("not_started"),
  invoiceStatus: text("invoice_status").notNull().default("not_started"),
  paymentDueAt: text("payment_due_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_job_details_work_order_idx").on(table.workOrderId),
  index("trade_crm_job_details_owner_pipeline_idx").on(table.firebaseUid, table.pipelineStage, table.updatedAt),
  index("trade_crm_job_details_customer_idx").on(table.crmCustomerId, table.updatedAt),
]);

export const tradePriceBookItems = sqliteTable("trade_price_book_items", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  itemCode: text("item_code").notNull(),
  name: text("name").notNull(),
  description: text("description").notNull().default(""),
  itemType: text("item_type").notNull(),
  unitLabel: text("unit_label").notNull().default("each"),
  supplierCostCentsExGst: integer("supplier_cost_cents_ex_gst").notNull().default(0),
  sellPriceCentsExGst: integer("sell_price_cents_ex_gst").notNull(),
  taxCode: text("tax_code").notNull().default("gst"),
  markupBasisPoints: integer("markup_basis_points").notNull().default(0),
  marginBasisPoints: integer("margin_basis_points").notNull().default(0),
  expectedDurationMinutes: integer("expected_duration_minutes").notNull().default(0),
  requiredSkill: text("required_skill").notNull().default(""),
  supplierName: text("supplier_name").notNull().default(""),
  supplierSku: text("supplier_sku").notNull().default(""),
  supplierProductId: text("supplier_product_id").notNull().default(""),
  recordStatus: text("record_status").notNull().default("active"),
  priceRevision: integer("price_revision").notNull().default(1),
  createdByUid: text("created_by_uid").notNull(),
  updatedByUid: text("updated_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_price_book_items_owner_code_idx").on(table.firebaseUid, table.itemCode),
  index("trade_price_book_items_owner_status_name_idx").on(table.firebaseUid, table.recordStatus, table.name),
  index("trade_price_book_items_owner_type_idx").on(table.firebaseUid, table.recordStatus, table.itemType, table.updatedAt),
  index("trade_price_book_items_supplier_product_idx").on(table.supplierProductId, table.recordStatus),
]);

export const tradePriceBookPriceHistory = sqliteTable("trade_price_book_price_history", {
  id: text("id").primaryKey(),
  priceBookItemId: text("price_book_item_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  priceRevision: integer("price_revision").notNull(),
  supplierCostCentsExGst: integer("supplier_cost_cents_ex_gst").notNull(),
  sellPriceCentsExGst: integer("sell_price_cents_ex_gst").notNull(),
  taxCode: text("tax_code").notNull(),
  markupBasisPoints: integer("markup_basis_points").notNull(),
  marginBasisPoints: integer("margin_basis_points").notNull(),
  changeType: text("change_type").notNull(),
  changedByUid: text("changed_by_uid").notNull(),
  changedAt: text("changed_at").notNull(),
}, (table) => [
  uniqueIndex("trade_price_book_price_history_revision_idx").on(table.priceBookItemId, table.priceRevision),
  index("trade_price_book_price_history_owner_changed_idx").on(table.firebaseUid, table.changedAt),
]);

export const tradeJobPackets = sqliteTable("trade_job_packets", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  packetCode: text("packet_code").notNull(),
  name: text("name").notNull(),
  serviceCategory: text("service_category").notNull().default("other"),
  jobTemplateId: text("job_template_id").notNull().default(""),
  suggestedCrewSize: integer("suggested_crew_size").notNull().default(1),
  recordStatus: text("record_status").notNull().default("draft"),
  revision: integer("revision").notNull().default(1),
  createdByUid: text("created_by_uid").notNull(),
  updatedByUid: text("updated_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_job_packets_owner_code_idx").on(table.firebaseUid, table.packetCode),
  uniqueIndex("trade_job_packets_owner_name_idx").on(table.firebaseUid, table.name),
  index("trade_job_packets_owner_status_idx").on(table.firebaseUid, table.recordStatus, table.updatedAt),
]);

export const tradeJobPacketItems = sqliteTable("trade_job_packet_items", {
  id: text("id").primaryKey(),
  packetId: text("packet_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  position: integer("position").notNull(),
  priceBookItemId: text("price_book_item_id").notNull(),
  quantityMilli: integer("quantity_milli").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_job_packet_items_position_idx").on(table.packetId, table.position),
  uniqueIndex("trade_job_packet_items_price_idx").on(table.packetId, table.priceBookItemId),
  index("trade_job_packet_items_owner_idx").on(table.firebaseUid, table.packetId),
]);

export const tradeJobPacketForms = sqliteTable("trade_job_packet_forms", {
  id: text("id").primaryKey(),
  packetId: text("packet_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  position: integer("position").notNull(),
  templateKey: text("template_key").notNull(),
  templateVersion: integer("template_version").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_job_packet_forms_position_idx").on(table.packetId, table.position),
  uniqueIndex("trade_job_packet_forms_template_idx").on(table.packetId, table.templateKey, table.templateVersion),
  index("trade_job_packet_forms_owner_idx").on(table.firebaseUid, table.packetId),
]);

export const tradeCrmQuotes = sqliteTable("trade_crm_quotes", {
  id: text("id").primaryKey(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  crmCustomerId: text("crm_customer_id").notNull(),
  serviceSiteId: text("service_site_id").notNull(),
  quoteNumber: text("quote_number").notNull(),
  currentVersionNumber: integer("current_version_number").notNull().default(1),
  status: text("status").notNull().default("draft"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_quotes_owner_work_idx").on(table.firebaseUid, table.workOrderId),
  uniqueIndex("trade_crm_quotes_owner_number_idx").on(table.firebaseUid, table.quoteNumber),
  index("trade_crm_quotes_customer_idx").on(table.firebaseUid, table.crmCustomerId, table.updatedAt),
]);

export const tradeCrmQuoteVersions = sqliteTable("trade_crm_quote_versions", {
  id: text("id").primaryKey(),
  quoteId: text("quote_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  versionNumber: integer("version_number").notNull(),
  status: text("status").notNull().default("draft"),
  acceptanceEmail: text("acceptance_email").notNull().default(""),
  subtotalCents: integer("subtotal_cents").notNull().default(0),
  taxCents: integer("tax_cents").notNull().default(0),
  totalCents: integer("total_cents").notNull().default(0),
  terms: text("terms").notNull().default(""),
  validUntil: text("valid_until").notNull().default(""),
  consentStatement: text("consent_statement").notNull().default(""),
  issuedAt: text("issued_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_quote_versions_quote_version_idx").on(table.quoteId, table.versionNumber),
  index("trade_crm_quote_versions_owner_idx").on(table.firebaseUid, table.status, table.updatedAt),
  index("trade_crm_quote_versions_acceptance_email_idx").on(table.acceptanceEmail, table.status, table.issuedAt),
]);

export const tradeCrmQuoteItems = sqliteTable("trade_crm_quote_items", {
  id: text("id").primaryKey(),
  quoteVersionId: text("quote_version_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  position: integer("position").notNull(),
  lineType: text("line_type").notNull(),
  description: text("description").notNull(),
  quantityMilli: integer("quantity_milli").notNull(),
  unitPriceCents: integer("unit_price_cents").notNull(),
  taxCode: text("tax_code").notNull(),
  subtotalCents: integer("subtotal_cents").notNull(),
  taxCents: integer("tax_cents").notNull(),
  totalCents: integer("total_cents").notNull(),
  priceBookItemId: text("price_book_item_id").notNull().default(""),
  priceBookItemType: text("price_book_item_type").notNull().default(""),
  unitCostCentsExGst: integer("unit_cost_cents_ex_gst").notNull().default(0),
  markupBasisPoints: integer("markup_basis_points").notNull().default(0),
  marginBasisPoints: integer("margin_basis_points").notNull().default(0),
  jobPacketId: text("job_packet_id").notNull().default(""),
  jobPacketRevision: integer("job_packet_revision").notNull().default(0),
  jobPacketLineId: text("job_packet_line_id").notNull().default(""),
  sectionHeading: text("section_heading").notNull().default("Included work"),
  quoteChoiceId: text("quote_choice_id").notNull().default(""),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_quote_items_version_position_idx").on(table.quoteVersionId, table.position),
  index("trade_crm_quote_items_owner_idx").on(table.firebaseUid, table.quoteVersionId),
]);

export const tradeCrmQuoteExecutionSnapshots = sqliteTable("trade_crm_quote_execution_snapshots", {
  id: text("id").primaryKey(), quoteVersionId: text("quote_version_id").notNull(), firebaseUid: text("firebase_uid").notNull(),
  sourceKind: text("source_kind").notNull().default("manual_quote"), packetsJson: text("packets_json").notNull().default("[]"),
  expectedDurationMinutes: integer("expected_duration_minutes").notNull().default(0), suggestedCrewSize: integer("suggested_crew_size").notNull().default(1),
  requiredCapabilitiesJson: text("required_capabilities_json").notNull().default("[]"), createdAt: text("created_at").notNull(),
}, (table) => [uniqueIndex("trade_crm_quote_execution_snapshots_version_idx").on(table.quoteVersionId), index("trade_crm_quote_execution_snapshots_owner_idx").on(table.firebaseUid, table.createdAt)]);

export const tradeCrmQuoteChoices = sqliteTable("trade_crm_quote_choices", {
  id: text("id").primaryKey(),
  quoteVersionId: text("quote_version_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  position: integer("position").notNull(),
  choiceKey: text("choice_key").notNull(),
  choiceKind: text("choice_kind").notNull(),
  groupKey: text("group_key").notNull(),
  name: text("name").notNull(),
  summary: text("summary").notNull().default(""),
  recommended: integer("recommended", { mode: "boolean" }).notNull().default(false),
  subtotalCents: integer("subtotal_cents").notNull().default(0),
  taxCents: integer("tax_cents").notNull().default(0),
  totalCents: integer("total_cents").notNull().default(0),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_quote_choices_version_key_idx").on(table.quoteVersionId, table.choiceKey),
  uniqueIndex("trade_crm_quote_choices_version_position_idx").on(table.quoteVersionId, table.position),
  index("trade_crm_quote_choices_owner_version_idx").on(table.firebaseUid, table.quoteVersionId),
]);

export const tradeCrmQuoteAcceptances = sqliteTable("trade_crm_quote_acceptances", {
  id: text("id").primaryKey(),
  quoteId: text("quote_id").notNull(),
  quoteVersionId: text("quote_version_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  crmCustomerId: text("crm_customer_id").notNull(),
  customerFirebaseUid: text("customer_firebase_uid").notNull(),
  actorEmail: text("actor_email").notNull(),
  actorEmailVerified: integer("actor_email_verified", { mode: "boolean" }).notNull().default(false),
  actorAuthTime: integer("actor_auth_time").notNull().default(0),
  actorSignInProvider: text("actor_sign_in_provider").notNull().default(""),
  decision: text("decision").notNull(),
  consentStatement: text("consent_statement").notNull(),
  selectedChoiceIdsJson: text("selected_choice_ids_json").notNull().default("[]"),
  selectedSubtotalCents: integer("selected_subtotal_cents").notNull().default(0),
  selectedTaxCents: integer("selected_tax_cents").notNull().default(0),
  selectedTotalCents: integer("selected_total_cents").notNull().default(0),
  selectionSummary: text("selection_summary").notNull().default(""),
  signerName: text("signer_name").notNull().default(""),
  actorType: text("actor_type").notNull().default("verified_account"),
  quoteLinkId: text("quote_link_id").notNull().default(""),
  tokenIssue: integer("token_issue").notNull().default(0),
  commercialReference: text("commercial_reference").notNull().default(""),
  currency: text("currency").notNull().default("AUD"),
  decidedAt: text("decided_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_quote_acceptances_version_idx").on(table.quoteVersionId),
  index("trade_crm_quote_acceptances_owner_idx").on(table.firebaseUid, table.workOrderId, table.decidedAt),
  index("trade_crm_quote_acceptances_customer_idx").on(table.customerFirebaseUid, table.decidedAt),
]);

export const tradeCrmCommercialHandovers = sqliteTable("trade_crm_commercial_handovers", {
  id: text("id").primaryKey(),
  acceptanceId: text("acceptance_id").notNull(),
  quoteId: text("quote_id").notNull(),
  quoteVersionId: text("quote_version_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  crmCustomerId: text("crm_customer_id").notNull(),
  commercialReference: text("commercial_reference").notNull(),
  currency: text("currency").notNull().default("AUD"),
  scopeSnapshotJson: text("scope_snapshot_json").notNull().default("[]"),
  termsSnapshot: text("terms_snapshot").notNull().default(""),
  subtotalCents: integer("subtotal_cents").notNull(),
  taxCents: integer("tax_cents").notNull(),
  totalCents: integer("total_cents").notNull(),
  depositKind: text("deposit_kind").notNull().default("percentage"),
  depositBasisPoints: integer("deposit_basis_points").notNull().default(1000),
  depositFixedCents: integer("deposit_fixed_cents").notNull().default(0),
  depositAmountCents: integer("deposit_amount_cents").notNull().default(0),
  status: text("status").notNull().default("accepted"),
  acceptedAt: text("accepted_at").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_commercial_handovers_acceptance_idx").on(table.acceptanceId),
  uniqueIndex("trade_crm_commercial_handovers_reference_idx").on(table.firebaseUid, table.commercialReference),
  index("trade_crm_commercial_handovers_work_idx").on(table.firebaseUid, table.workOrderId, table.acceptedAt),
]);

export const tradeCrmJobPlans = sqliteTable("trade_crm_job_plans", {
  id: text("id").primaryKey(),
  commercialHandoffId: text("commercial_handoff_id").notNull(),
  quoteVersionId: text("quote_version_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  commercialReference: text("commercial_reference").notNull(),
  sourceKind: text("source_kind").notNull().default("manual_quote"),
  status: text("status").notNull().default("planning"),
  acceptedSubtotalCents: integer("accepted_subtotal_cents").notNull(),
  acceptedTaxCents: integer("accepted_tax_cents").notNull(),
  acceptedTotalCents: integer("accepted_total_cents").notNull(),
  budgetCostCents: integer("budget_cost_cents").notNull().default(0),
  budgetMarginCents: integer("budget_margin_cents").notNull().default(0),
  expectedDurationMinutes: integer("expected_duration_minutes").notNull().default(0),
  suggestedCrewSize: integer("suggested_crew_size").notNull().default(1),
  depositRequirement: text("deposit_requirement").notNull().default("optional"),
  readyAt: text("ready_at").notNull().default(""),
  completedAt: text("completed_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_job_plans_handoff_idx").on(table.commercialHandoffId),
  index("trade_crm_job_plans_work_idx").on(table.firebaseUid, table.workOrderId, table.updatedAt),
]);

export const tradeCrmJobPlanPhases = sqliteTable("trade_crm_job_plan_phases", {
  id: text("id").primaryKey(),
  jobPlanId: text("job_plan_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  position: integer("position").notNull(),
  title: text("title").notNull(),
  customerDescription: text("customer_description").notNull(),
  sourcePacketId: text("source_packet_id").notNull().default(""),
  sourcePacketRevision: integer("source_packet_revision").notNull().default(0),
  expectedDurationMinutes: integer("expected_duration_minutes").notNull().default(0),
  status: text("status").notNull().default("pending"), completedAt: text("completed_at").notNull().default(""),
  createdAt: text("created_at").notNull(), updatedAt: text("updated_at").notNull().default(""),
}, (table) => [
  uniqueIndex("trade_crm_job_plan_phases_position_idx").on(table.jobPlanId, table.position),
  index("trade_crm_job_plan_phases_owner_idx").on(table.firebaseUid, table.jobPlanId),
]);

export const tradeCrmJobPlanRequirements = sqliteTable("trade_crm_job_plan_requirements", {
  id: text("id").primaryKey(),
  jobPlanId: text("job_plan_id").notNull(),
  jobPlanPhaseId: text("job_plan_phase_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  position: integer("position").notNull(),
  requirementType: text("requirement_type").notNull(),
  sourceId: text("source_id").notNull().default(""),
  description: text("description").notNull(),
  quantityMilli: integer("quantity_milli").notNull().default(1000),
  unitCostCents: integer("unit_cost_cents").notNull().default(0),
  totalCostCents: integer("total_cost_cents").notNull().default(0),
  expectedDurationMinutes: integer("expected_duration_minutes").notNull().default(0),
  requiredCapability: text("required_capability").notNull().default(""),
  status: text("status").notNull().default("required"),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_job_plan_requirements_position_idx").on(table.jobPlanId, table.position),
  index("trade_crm_job_plan_requirements_owner_idx").on(table.firebaseUid, table.jobPlanId, table.requirementType),
]);

export const tradeCrmJobActuals = sqliteTable("trade_crm_job_actuals", {
  id: text("id").primaryKey(), jobPlanId: text("job_plan_id").notNull(), jobPlanPhaseId: text("job_plan_phase_id").notNull(),
  jobPlanRequirementId: text("job_plan_requirement_id").notNull(), workOrderId: text("work_order_id").notNull(), firebaseUid: text("firebase_uid").notNull(),
  actualType: text("actual_type").notNull(), quantityMilli: integer("quantity_milli").notNull().default(0), durationMinutes: integer("duration_minutes").notNull().default(0),
  totalCostCents: integer("total_cost_cents").notNull().default(0), note: text("note").notNull().default(""), recordedByUid: text("recorded_by_uid").notNull(),
  createdAt: text("created_at").notNull(), updatedAt: text("updated_at").notNull(),
}, (table) => [uniqueIndex("trade_crm_job_actuals_requirement_idx").on(table.jobPlanRequirementId), index("trade_crm_job_actuals_work_idx").on(table.firebaseUid, table.workOrderId, table.updatedAt), index("trade_crm_job_actuals_plan_idx").on(table.jobPlanId, table.actualType)]);

export const tradeCrmQuoteLinks = sqliteTable("trade_crm_quote_links", {
  id: text("id").primaryKey(), quoteId: text("quote_id").notNull(), quoteVersionId: text("quote_version_id").notNull(),
  workOrderId: text("work_order_id").notNull(), firebaseUid: text("firebase_uid").notNull(), crmCustomerId: text("crm_customer_id").notNull(),
  tokenHash: text("token_hash").notNull(), encryptedToken: text("encrypted_token").notNull().default(""), tokenIssue: integer("token_issue").notNull().default(1),
  status: text("status").notNull().default("active"), expiresAt: text("expires_at").notNull(), revokedAt: text("revoked_at").notNull().default(""),
  createdAt: text("created_at").notNull(), updatedAt: text("updated_at").notNull(),
}, (table) => [uniqueIndex("trade_crm_quote_links_version_idx").on(table.quoteVersionId), index("trade_crm_quote_links_owner_idx").on(table.firebaseUid, table.workOrderId, table.updatedAt), index("trade_crm_quote_links_expiry_idx").on(table.status, table.expiresAt)]);

export const tradeCrmQuoteEvents = sqliteTable("trade_crm_quote_events", {
  id: text("id").primaryKey(), quoteLinkId: text("quote_link_id").notNull().default(""), quoteId: text("quote_id").notNull(), quoteVersionId: text("quote_version_id").notNull(),
  workOrderId: text("work_order_id").notNull(), firebaseUid: text("firebase_uid").notNull(), eventType: text("event_type").notNull(), actorType: text("actor_type").notNull().default("system"),
  summary: text("summary").notNull().default(""), evidenceKey: text("evidence_key").notNull(), occurredAt: text("occurred_at").notNull(),
}, (table) => [uniqueIndex("trade_crm_quote_events_evidence_idx").on(table.evidenceKey), index("trade_crm_quote_events_version_idx").on(table.quoteVersionId, table.occurredAt), index("trade_crm_quote_events_owner_idx").on(table.firebaseUid, table.workOrderId, table.occurredAt)]);

export const tradeCrmQuoteQuestions = sqliteTable("trade_crm_quote_questions", {
  id: text("id").primaryKey(), quoteLinkId: text("quote_link_id").notNull(), quoteId: text("quote_id").notNull(), quoteVersionId: text("quote_version_id").notNull(),
  workOrderId: text("work_order_id").notNull(), firebaseUid: text("firebase_uid").notNull(), question: text("question").notNull(), answer: text("answer").notNull().default(""),
  status: text("status").notNull().default("open"), askedAt: text("asked_at").notNull(), answeredAt: text("answered_at").notNull().default(""), answeredByUid: text("answered_by_uid").notNull().default(""),
}, (table) => [index("trade_crm_quote_questions_version_idx").on(table.quoteVersionId, table.askedAt), index("trade_crm_quote_questions_owner_idx").on(table.firebaseUid, table.status, table.askedAt)]);

export const tradeCrmQuoteDeliveries = sqliteTable("trade_crm_quote_deliveries", {
  id: text("id").primaryKey(), quoteLinkId: text("quote_link_id").notNull(), quoteVersionId: text("quote_version_id").notNull(), workOrderId: text("work_order_id").notNull(), firebaseUid: text("firebase_uid").notNull(), crmCustomerId: text("crm_customer_id").notNull(),
  channel: text("channel").notNull(), provider: text("provider").notNull(), status: text("status").notNull().default("queued"), recipientPreview: text("recipient_preview").notNull().default(""),
  consentBasis: text("consent_basis").notNull().default(""), idempotencyKey: text("idempotency_key").notNull(), providerMessageId: text("provider_message_id").notNull().default(""), providerStatus: text("provider_status").notNull().default(""),
  attempts: integer("attempts").notNull().default(0), lastError: text("last_error").notNull().default(""), sentAt: text("sent_at").notNull().default(""), deliveredAt: text("delivered_at").notNull().default(""),
  createdAt: text("created_at").notNull(), updatedAt: text("updated_at").notNull(),
}, (table) => [uniqueIndex("trade_crm_quote_deliveries_idempotency_idx").on(table.idempotencyKey), index("trade_crm_quote_deliveries_version_idx").on(table.quoteVersionId, table.createdAt)]);

export const tradeCrmAppointments = sqliteTable("trade_crm_appointments", {
  id: text("id").primaryKey(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  appointmentType: text("appointment_type").notNull().default("site_visit"),
  title: text("title").notNull(),
  startsAt: text("starts_at").notNull(),
  endsAt: text("ends_at").notNull().default(""),
  assigneeMemberId: text("assignee_member_id").notNull().default(""),
  assigneeLabel: text("assignee_label").notNull().default(""),
  status: text("status").notNull().default("scheduled"),
  travelStartedAt: text("travel_started_at").notNull().default(""),
  arrivedAt: text("arrived_at").notNull().default(""),
  workStartedAt: text("work_started_at").notNull().default(""),
  completedAt: text("completed_at").notNull().default(""),
  lastTransitionByUid: text("last_transition_by_uid").notNull().default(""),
  notes: text("notes").notNull().default(""),
  revision: integer("revision").notNull().default(1),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_crm_appointments_owner_start_idx").on(table.firebaseUid, table.status, table.startsAt),
  index("trade_crm_appointments_work_order_idx").on(table.workOrderId, table.startsAt),
  index("trade_crm_appointments_assignee_start_idx").on(table.firebaseUid, table.assigneeMemberId, table.status, table.startsAt),
  index("trade_crm_appointments_register_latest_idx").on(table.workOrderId, table.firebaseUid, table.startsAt, table.id),
]);

export const tradeCrmAppointmentRevisions = sqliteTable("trade_crm_appointment_revisions", {
  id: text("id").primaryKey(),
  appointmentId: text("appointment_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  revision: integer("revision").notNull(),
  startsAt: text("starts_at").notNull(),
  endsAt: text("ends_at").notNull().default(""),
  assigneeMemberId: text("assignee_member_id").notNull().default(""),
  assigneeLabel: text("assignee_label").notNull().default(""),
  changeSource: text("change_source").notNull(),
  sourceReference: text("source_reference").notNull().default(""),
  changedByUid: text("changed_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_appointment_revisions_item_revision_idx").on(table.appointmentId, table.revision),
  index("trade_crm_appointment_revisions_owner_idx").on(table.firebaseUid, table.appointmentId, table.createdAt),
]);

export const tradeCrmAppointmentRescheduleRequests = sqliteTable("trade_crm_appointment_reschedule_requests", {
  id: text("id").primaryKey(),
  appointmentId: text("appointment_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  crmCustomerId: text("crm_customer_id").notNull(),
  customerFirebaseUid: text("customer_firebase_uid").notNull(),
  actorEmail: text("actor_email").notNull(),
  status: text("status").notNull().default("pending"),
  activeKey: text("active_key").notNull().default(""),
  preferredWindows: text("preferred_windows").notNull().default("[]"),
  reason: text("reason").notNull().default(""),
  accessNotes: text("access_notes").notNull().default(""),
  requestedAppointmentRevision: integer("requested_appointment_revision").notNull(),
  originalStartsAt: text("original_starts_at").notNull(),
  originalEndsAt: text("original_ends_at").notNull().default(""),
  originalAssigneeMemberId: text("original_assignee_member_id").notNull().default(""),
  originalAssigneeLabel: text("original_assignee_label").notNull().default(""),
  proposedStartsAt: text("proposed_starts_at").notNull().default(""),
  proposedEndsAt: text("proposed_ends_at").notNull().default(""),
  proposedAssigneeMemberId: text("proposed_assignee_member_id").notNull().default(""),
  proposedAssigneeLabel: text("proposed_assignee_label").notNull().default(""),
  decisionNote: text("decision_note").notNull().default(""),
  revision: integer("revision").notNull().default(1),
  requestedAt: text("requested_at").notNull(),
  decidedByUid: text("decided_by_uid").notNull().default(""),
  decidedAt: text("decided_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_appointment_reschedule_active_idx").on(table.appointmentId, table.activeKey),
  index("trade_crm_appointment_reschedule_owner_idx").on(table.firebaseUid, table.status, table.updatedAt),
  index("trade_crm_appointment_reschedule_customer_idx").on(table.customerFirebaseUid, table.updatedAt),
]);

export const tradeCrmAppointmentRescheduleEvents = sqliteTable("trade_crm_appointment_reschedule_events", {
  id: text("id").primaryKey(),
  requestId: text("request_id").notNull(),
  appointmentId: text("appointment_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  actorType: text("actor_type").notNull(),
  actorUid: text("actor_uid").notNull(),
  eventType: text("event_type").notNull(),
  requestRevision: integer("request_revision").notNull(),
  fromStartsAt: text("from_starts_at").notNull().default(""),
  fromEndsAt: text("from_ends_at").notNull().default(""),
  toStartsAt: text("to_starts_at").notNull().default(""),
  toEndsAt: text("to_ends_at").notNull().default(""),
  summary: text("summary").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("trade_crm_appointment_reschedule_events_request_idx").on(table.requestId, table.createdAt),
  index("trade_crm_appointment_reschedule_events_owner_idx").on(table.firebaseUid, table.createdAt),
]);

export const tradeCrmJobTemplates = sqliteTable("trade_crm_job_templates", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  name: text("name").notNull(),
  title: text("title").notNull().default(""),
  serviceCategory: text("service_category").notNull().default("other"),
  priority: text("priority").notNull().default("standard"),
  description: text("description").notNull().default(""),
  taskTitles: text("task_titles").notNull().default("[]"),
  recordStatus: text("record_status").notNull().default("active"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_crm_job_templates_owner_idx").on(table.firebaseUid, table.recordStatus, table.updatedAt),
  uniqueIndex("trade_crm_job_templates_owner_name_idx").on(table.firebaseUid, table.name),
]);

export const tradeDataImportBatches = sqliteTable("trade_data_import_batches", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  partnerType: text("partner_type").notNull(),
  importType: text("import_type").notNull(),
  fileName: text("file_name").notNull(),
  fileSizeBytes: integer("file_size_bytes").notNull().default(0),
  rowCount: integer("row_count").notNull().default(0),
  readyCount: integer("ready_count").notNull().default(0),
  warningCount: integer("warning_count").notNull().default(0),
  duplicateCount: integer("duplicate_count").notNull().default(0),
  errorCount: integer("error_count").notNull().default(0),
  importedCount: integer("imported_count").notNull().default(0),
  skippedCount: integer("skipped_count").notNull().default(0),
  failedCount: integer("failed_count").notNull().default(0),
  status: text("status").notNull().default("preview"),
  committedAt: text("committed_at").notNull().default(""),
  rollbackUntil: text("rollback_until").notNull().default(""),
  rolledBackAt: text("rolled_back_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_data_import_batches_owner_idx").on(table.firebaseUid, table.createdAt),
  index("trade_data_import_batches_status_idx").on(table.firebaseUid, table.status, table.updatedAt),
]);

export const tradeDataImportRows = sqliteTable("trade_data_import_rows", {
  id: text("id").primaryKey(),
  batchId: text("batch_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  rowNumber: integer("row_number").notNull(),
  rowKey: text("row_key").notNull().default(""),
  normalizedData: text("normalized_data").notNull(),
  validationStatus: text("validation_status").notNull(),
  issues: text("issues").notNull().default("[]"),
  resolution: text("resolution").notNull().default("import"),
  resultStatus: text("result_status").notNull().default("pending"),
  targetEntityType: text("target_entity_type").notNull().default(""),
  targetEntityId: text("target_entity_id").notNull().default(""),
  error: text("error").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_data_import_rows_batch_row_idx").on(table.batchId, table.rowNumber),
  index("trade_data_import_rows_batch_status_idx").on(table.batchId, table.validationStatus, table.resultStatus),
  index("trade_data_import_rows_target_idx").on(table.firebaseUid, table.targetEntityType, table.targetEntityId),
]);

export const tradeJobForms = sqliteTable("trade_job_forms", {
  id: text("id").primaryKey(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  templateKey: text("template_key").notNull(),
  templateVersion: integer("template_version").notNull(),
  templateName: text("template_name").notNull(),
  jurisdiction: text("jurisdiction").notNull().default("AU"),
  templateSnapshot: text("template_snapshot").notNull(),
  answers: text("answers").notNull().default("{}"),
  status: text("status").notNull().default("draft"),
  revision: integer("revision").notNull().default(1),
  completedByUid: text("completed_by_uid").notNull().default(""),
  completedAt: text("completed_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_job_forms_work_template_idx").on(table.workOrderId, table.templateKey, table.templateVersion),
  index("trade_job_forms_owner_status_idx").on(table.firebaseUid, table.status, table.updatedAt),
  index("trade_job_forms_work_idx").on(table.workOrderId, table.updatedAt),
]);

export const tradeFormTemplates = sqliteTable("trade_form_templates", {
  id: text("id").primaryKey(),
  templateKey: text("template_key").notNull(),
  version: integer("version").notNull(),
  name: text("name").notNull(),
  jurisdiction: text("jurisdiction").notNull().default("AU"),
  categories: text("categories").notNull().default("[]"),
  description: text("description").notNull().default(""),
  guidance: text("guidance").notNull().default(""),
  fields: text("fields").notNull().default("[]"),
  sourceNotes: text("source_notes").notNull().default(""),
  status: text("status").notNull().default("draft"),
  createdByUid: text("created_by_uid").notNull(),
  publishedByUid: text("published_by_uid").notNull().default(""),
  publishedAt: text("published_at").notNull().default(""),
  withdrawnAt: text("withdrawn_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_form_templates_key_version_idx").on(table.templateKey, table.version),
  index("trade_form_templates_status_idx").on(table.status, table.updatedAt),
]);

export const tradeCrmJobNotes = sqliteTable("trade_crm_job_notes", {
  id: text("id").primaryKey(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  noteType: text("note_type").notNull().default("internal"),
  body: text("body").notNull(),
  issueStatus: text("issue_status").notNull().default("not_applicable"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_crm_job_notes_owner_idx").on(table.firebaseUid, table.noteType, table.updatedAt),
  index("trade_crm_job_notes_work_order_idx").on(table.workOrderId, table.createdAt),
]);

export const tradeCrmTimeEntries = sqliteTable("trade_crm_time_entries", {
  id: text("id").primaryKey(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  staffLabel: text("staff_label").notNull().default(""),
  workDate: text("work_date").notNull(),
  durationMinutes: integer("duration_minutes").notNull(),
  notes: text("notes").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_crm_time_entries_owner_date_idx").on(table.firebaseUid, table.workDate),
  index("trade_crm_time_entries_work_order_idx").on(table.workOrderId, table.workDate),
]);

export const tradeCrmJobMedia = sqliteTable("trade_crm_job_media", {
  id: text("id").primaryKey(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  category: text("category").notNull().default("progress"),
  fileName: text("file_name").notNull(),
  contentType: text("content_type").notNull(),
  sizeBytes: integer("size_bytes").notNull(),
  objectKey: text("object_key").notNull(),
  caption: text("caption").notNull().default(""),
  source: text("source").notNull().default("installer"),
  photoRequestId: text("photo_request_id").notNull().default(""),
  photoRequirementId: text("photo_requirement_id").notNull().default(""),
  requestRevision: integer("request_revision").notNull().default(0),
  checklistVersion: text("checklist_version").notNull().default(""),
  customerAcknowledgedAt: text("customer_acknowledged_at").notNull().default(""),
  evidenceEnvelope: text("evidence_envelope").notNull().default("{}"),
  originalSha256: text("original_sha256").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_crm_job_media_owner_idx").on(table.firebaseUid, table.createdAt),
  index("trade_crm_job_media_work_order_idx").on(table.workOrderId, table.createdAt),
  index("trade_crm_job_media_photo_request_idx").on(table.photoRequestId, table.createdAt),
  check("trade_crm_job_media_evidence_envelope_check", sql`json_valid(${table.evidenceEnvelope})`),
  check("trade_crm_job_media_original_hash_check", sql`${table.originalSha256} = '' OR (length(${table.originalSha256}) = 64 AND lower(${table.originalSha256}) NOT GLOB '*[^0-9a-f]*')`),
]);

export const tradeCrmPhotoRequests = sqliteTable("trade_crm_photo_requests", {
  id: text("id").primaryKey(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  crmCustomerId: text("crm_customer_id").notNull(),
  tokenHash: text("token_hash").notNull(),
  encryptedToken: text("encrypted_token").notNull().default(""),
  tokenIssue: integer("token_issue").notNull().default(0),
  status: text("status").notNull().default("active"),
  requirements: text("requirements").notNull().default("[]"),
  revision: integer("revision").notNull().default(1),
  expiresAt: text("expires_at").notNull(),
  lastSharedAt: text("last_shared_at").notNull().default(""),
  sourceTemplateId: text("source_template_id").notNull().default(""),
  sourceTemplateVersionId: text("source_template_version_id").notNull().default(""),
  sourceTemplateVersion: integer("source_template_version").notNull().default(0),
  sourceTemplateEdited: integer("source_template_edited").notNull().default(0),
  templateFeedback: text("template_feedback").notNull().default("{}"),
  templateMissingFeedback: integer("template_missing_feedback").notNull().default(0),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_photo_requests_work_order_idx").on(table.workOrderId),
  index("trade_crm_photo_requests_owner_idx").on(table.firebaseUid, table.status, table.updatedAt),
  index("trade_crm_photo_requests_expiry_idx").on(table.status, table.expiresAt),
  index("trade_crm_photo_requests_template_version_idx").on(table.firebaseUid, table.sourceTemplateVersionId, table.createdAt),
]);

export const tradeCrmPhotoTemplates = sqliteTable("trade_crm_photo_templates", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  name: text("name").notNull(),
  serviceCategory: text("service_category").notNull().default("other"),
  status: text("status").notNull().default("draft"),
  draftRequirements: text("draft_requirements").notNull().default("[]"),
  publishedVersion: integer("published_version").notNull().default(0),
  createdByUid: text("created_by_uid").notNull(),
  updatedByUid: text("updated_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("trade_crm_photo_templates_owner_status_idx").on(table.firebaseUid, table.status, table.updatedAt),
  index("trade_crm_photo_templates_owner_service_idx").on(table.firebaseUid, table.serviceCategory, table.updatedAt),
]);

export const tradeCrmPhotoTemplateVersions = sqliteTable("trade_crm_photo_template_versions", {
  id: text("id").primaryKey(),
  templateId: text("template_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  version: integer("version").notNull(),
  name: text("name").notNull(),
  serviceCategory: text("service_category").notNull(),
  requirements: text("requirements").notNull(),
  requirementCount: integer("requirement_count").notNull(),
  publishedByUid: text("published_by_uid").notNull(),
  publishedAt: text("published_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_photo_template_versions_template_version_idx").on(table.templateId, table.version),
  index("trade_crm_photo_template_versions_owner_idx").on(table.firebaseUid, table.templateId, table.publishedAt),
]);

export const tradeCrmPhotoRequestEvents = sqliteTable("trade_crm_photo_request_events", {
  id: text("id").primaryKey(),
  photoRequestId: text("photo_request_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  actorType: text("actor_type").notNull(),
  actorUid: text("actor_uid").notNull().default(""),
  eventType: text("event_type").notNull(),
  requestRevision: integer("request_revision").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("trade_crm_photo_request_events_request_idx").on(table.photoRequestId, table.createdAt),
  index("trade_crm_photo_request_events_job_idx").on(table.firebaseUid, table.workOrderId, table.createdAt),
]);

export const tradeCrmPhotoRequestCompletions = sqliteTable("trade_crm_photo_request_completions", {
  id: text("id").primaryKey(),
  photoRequestId: text("photo_request_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  requestRevision: integer("request_revision").notNull(),
  completionRevision: integer("completion_revision").notNull(),
  checklistVersion: text("checklist_version").notNull(),
  evidenceKey: text("evidence_key").notNull(),
  requiredCount: integer("required_count").notNull(),
  suppliedCount: integer("supplied_count").notNull(),
  completedAt: text("completed_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_photo_request_completions_evidence_idx").on(table.evidenceKey),
  uniqueIndex("trade_crm_photo_request_completions_revision_idx").on(table.photoRequestId, table.completionRevision),
  index("trade_crm_photo_request_completions_request_idx").on(table.photoRequestId, table.completedAt),
  index("trade_crm_photo_request_completions_job_idx").on(table.firebaseUid, table.workOrderId, table.completedAt),
]);

export const tradeCrmPhotoRequirementReviews = sqliteTable("trade_crm_photo_requirement_reviews", {
  id: text("id").primaryKey(),
  photoRequestId: text("photo_request_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  requestRevision: integer("request_revision").notNull(),
  reviewRevision: integer("review_revision").notNull(),
  photoRequirementId: text("photo_requirement_id").notNull(),
  status: text("status").notNull(),
  reasonCode: text("reason_code").notNull().default(""),
  guidance: text("guidance").notNull().default(""),
  reviewedUploadCount: integer("reviewed_upload_count").notNull().default(0),
  actorUid: text("actor_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_photo_requirement_reviews_revision_idx").on(table.photoRequestId, table.reviewRevision),
  index("trade_crm_photo_requirement_reviews_requirement_idx").on(table.photoRequestId, table.photoRequirementId, table.reviewRevision),
  index("trade_crm_photo_requirement_reviews_job_idx").on(table.firebaseUid, table.workOrderId, table.createdAt),
]);

export const tradeCrmPhotoRequestDeliveries = sqliteTable("trade_crm_photo_request_deliveries", {
  id: text("id").primaryKey(),
  photoRequestId: text("photo_request_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  crmCustomerId: text("crm_customer_id").notNull(),
  customerUid: text("customer_uid").notNull().default(""),
  channel: text("channel").notNull(),
  provider: text("provider").notNull(),
  requestRevision: integer("request_revision").notNull(),
  tokenIssue: integer("token_issue").notNull(),
  reviewRevision: integer("review_revision").notNull().default(0),
  photoRequirementId: text("photo_requirement_id").notNull().default(""),
  intent: text("intent").notNull(),
  idempotencyKey: text("idempotency_key").notNull(),
  consentBasis: text("consent_basis").notNull(),
  consentConfirmedAt: text("consent_confirmed_at").notNull(),
  status: text("status").notNull().default("queued"),
  eligibilityReason: text("eligibility_reason").notNull().default(""),
  attempts: integer("attempts").notNull().default(0),
  providerMessageId: text("provider_message_id").notNull().default(""),
  providerStatus: text("provider_status").notNull().default(""),
  lastError: text("last_error").notNull().default(""),
  queuedAt: text("queued_at").notNull(),
  sentAt: text("sent_at").notNull().default(""),
  deliveredAt: text("delivered_at").notNull().default(""),
  failedAt: text("failed_at").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_photo_request_deliveries_idempotency_idx").on(table.idempotencyKey),
  uniqueIndex("trade_crm_photo_request_deliveries_provider_message_idx").on(table.provider, table.providerMessageId),
  index("trade_crm_photo_request_deliveries_request_idx").on(table.photoRequestId, table.createdAt),
  index("trade_crm_photo_request_deliveries_owner_status_idx").on(table.firebaseUid, table.status, table.updatedAt),
  index("trade_crm_photo_request_deliveries_customer_channel_idx").on(table.firebaseUid, table.crmCustomerId, table.channel, table.createdAt),
]);

export const tradeCrmPhotoRequestDeliveryEvents = sqliteTable("trade_crm_photo_request_delivery_events", {
  id: text("id").primaryKey(),
  deliveryId: text("delivery_id").notNull(),
  providerEventKey: text("provider_event_key").notNull(),
  eventType: text("event_type").notNull(),
  providerStatus: text("provider_status").notNull().default(""),
  summary: text("summary").notNull(),
  occurredAt: text("occurred_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_photo_request_delivery_events_provider_idx").on(table.providerEventKey),
  index("trade_crm_photo_request_delivery_events_delivery_idx").on(table.deliveryId, table.occurredAt),
]);

export const tradeCrmSignoffs = sqliteTable("trade_crm_signoffs", {
  id: text("id").primaryKey(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  signerRole: text("signer_role").notNull(),
  signerName: text("signer_name").notNull(),
  confirmationText: text("confirmation_text").notNull(),
  method: text("method").notNull().default("typed"),
  signedAt: text("signed_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("trade_crm_signoffs_owner_idx").on(table.firebaseUid, table.signedAt),
  index("trade_crm_signoffs_work_order_idx").on(table.workOrderId, table.signedAt),
]);

export const tradeCrmQuickInvoices = sqliteTable("trade_crm_quick_invoices", {
  id: text("id").primaryKey(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  crmCustomerId: text("crm_customer_id").notNull(),
  invoiceNumber: text("invoice_number").notNull(),
  currency: text("currency").notNull().default("AUD"),
  lineItemsJson: text("line_items_json").notNull().default("[]"),
  subtotalCents: integer("subtotal_cents").notNull().default(0),
  taxCents: integer("tax_cents").notNull().default(0),
  totalCents: integer("total_cents").notNull().default(0),
  dueAt: text("due_at").notNull(),
  status: text("status").notNull().default("draft"),
  deliveryStatus: text("delivery_status").notNull().default("queued"),
  deliveryProvider: text("delivery_provider").notNull().default("resend"),
  providerMessageId: text("provider_message_id").notNull().default(""),
  consentConfirmedAt: text("consent_confirmed_at").notNull(),
  attempts: integer("attempts").notNull().default(0),
  lastError: text("last_error").notNull().default(""),
  sentAt: text("sent_at").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
  revision: integer("revision").notNull().default(1),
}, (table) => [
  uniqueIndex("trade_crm_quick_invoices_owner_job_idx").on(table.firebaseUid, table.workOrderId),
  uniqueIndex("trade_crm_quick_invoices_number_idx").on(table.invoiceNumber),
  index("trade_crm_quick_invoices_owner_status_idx").on(table.firebaseUid, table.status, table.updatedAt),
]);

export const tradeCrmQuickInvoiceRevisions = sqliteTable("trade_crm_quick_invoice_revisions", {
  id: text("id").primaryKey(),
  invoiceId: text("invoice_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  revision: integer("revision").notNull(),
  lineItemsJson: text("line_items_json").notNull(),
  subtotalCents: integer("subtotal_cents").notNull(),
  taxCents: integer("tax_cents").notNull(),
  totalCents: integer("total_cents").notNull(),
  dueAt: text("due_at").notNull(),
  changeReason: text("change_reason").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_quick_invoice_revisions_invoice_revision_idx").on(table.invoiceId, table.revision),
  index("trade_crm_quick_invoice_revisions_owner_idx").on(table.firebaseUid, table.createdAt),
]);

export const tradeCrmQuickInvoiceCredits = sqliteTable("trade_crm_quick_invoice_credits", {
  id: text("id").primaryKey(),
  invoiceId: text("invoice_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  creditNumber: text("credit_number").notNull(),
  description: text("description").notNull(),
  subtotalCents: integer("subtotal_cents").notNull(),
  taxCents: integer("tax_cents").notNull(),
  totalCents: integer("total_cents").notNull(),
  status: text("status").notNull().default("issued"),
  reason: text("reason").notNull(),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_quick_invoice_credits_number_idx").on(table.creditNumber),
  index("trade_crm_quick_invoice_credits_invoice_idx").on(table.invoiceId, table.createdAt),
  index("trade_crm_quick_invoice_credits_owner_idx").on(table.firebaseUid, table.createdAt),
]);

export const tradeCrmIntegrations = sqliteTable("trade_crm_integrations", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  provider: text("provider").notNull(),
  status: text("status").notNull().default("connected"),
  externalAccountId: text("external_account_id").notNull().default(""),
  externalAccountLabel: text("external_account_label").notNull().default(""),
  encryptedCredentials: text("encrypted_credentials").notNull(),
  scopes: text("scopes").notNull().default("[]"),
  tokenExpiresAt: text("token_expires_at").notNull().default(""),
  lastSyncAt: text("last_sync_at").notNull().default(""),
  lastError: text("last_error").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_integrations_owner_provider_idx").on(table.firebaseUid, table.provider),
  index("trade_crm_integrations_owner_status_idx").on(table.firebaseUid, table.status, table.updatedAt),
]);

export const tradeCrmOauthStates = sqliteTable("trade_crm_oauth_states", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  provider: text("provider").notNull(),
  stateHash: text("state_hash").notNull(),
  redirectUri: text("redirect_uri").notNull(),
  expiresAt: text("expires_at").notNull(),
  consumedAt: text("consumed_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_oauth_states_hash_idx").on(table.stateHash),
  index("trade_crm_oauth_states_owner_expiry_idx").on(table.firebaseUid, table.expiresAt),
]);

export const tradeCrmAccountingDocuments = sqliteTable("trade_crm_accounting_documents", {
  id: text("id").primaryKey(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  commercialHandoffId: text("commercial_handoff_id").notNull().default(""),
  commercialReference: text("commercial_reference").notNull().default(""),
  scopeSnapshotJson: text("scope_snapshot_json").notNull().default("[]"),
  subtotalCents: integer("subtotal_cents").notNull().default(0),
  taxCents: integer("tax_cents").notNull().default(0),
  provider: text("provider").notNull(),
  documentType: text("document_type").notNull().default("invoice"),
  externalContactId: text("external_contact_id").notNull().default(""),
  externalDocumentId: text("external_document_id").notNull().default(""),
  externalNumber: text("external_number").notNull().default(""),
  externalUrl: text("external_url").notNull().default(""),
  accountReference: text("account_reference").notNull().default(""),
  amountCents: integer("amount_cents").notNull().default(0),
  paidAmountCents: integer("paid_amount_cents").notNull().default(0),
  currency: text("currency").notNull().default("AUD"),
  status: text("status").notNull().default("exporting"),
  providerStatus: text("provider_status").notNull().default(""),
  dueAt: text("due_at").notNull().default(""),
  lastSyncedAt: text("last_synced_at").notNull().default(""),
  lastError: text("last_error").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_accounting_documents_job_type_idx").on(table.firebaseUid, table.workOrderId, table.documentType),
  index("trade_crm_accounting_documents_provider_external_idx").on(table.provider, table.externalDocumentId),
  index("trade_crm_accounting_documents_owner_idx").on(table.firebaseUid, table.updatedAt),
  index("trade_crm_accounting_documents_status_idx").on(table.status, table.lastSyncedAt),
]);

export const tradeCrmAccountingEvents = sqliteTable("trade_crm_accounting_events", {
  id: text("id").primaryKey(),
  accountingDocumentId: text("accounting_document_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  provider: text("provider").notNull(),
  action: text("action").notNull(),
  status: text("status").notNull(),
  providerStatus: text("provider_status").notNull().default(""),
  amountCents: integer("amount_cents").notNull().default(0),
  paidAmountCents: integer("paid_amount_cents").notNull().default(0),
  detail: text("detail").notNull().default(""),
  occurredAt: text("occurred_at").notNull(),
}, (table) => [
  index("trade_crm_accounting_events_document_idx").on(table.accountingDocumentId, table.occurredAt),
  index("trade_crm_accounting_events_owner_idx").on(table.firebaseUid, table.occurredAt),
]);

export const customerAccounts = sqliteTable("customer_accounts", {
  firebaseUid: text("firebase_uid").primaryKey(),
  email: text("email").notNull(),
  displayName: text("display_name").notNull(),
  phone: text("phone").notNull().default(""),
  addressLine1: text("address_line_1").notNull().default(""),
  addressLine2: text("address_line_2").notNull().default(""),
  suburb: text("suburb").notNull().default(""),
  postcode: text("postcode").notNull().default(""),
  addressState: text("address_state").notNull().default(""),
  propertyType: text("property_type").notNull().default("house"),
  householdSituation: text("household_situation").notNull().default("owner"),
  accountUpdates: integer("account_updates", { mode: "boolean" }).notNull().default(false),
  accountStatus: text("account_status").notNull().default("active"),
  isSynthetic: integer("is_synthetic", { mode: "boolean" }).notNull().default(false),
  consentVersion: text("consent_version").notNull(),
  consentAt: text("consent_at").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("customer_accounts_email_idx").on(table.email),
  index("customer_accounts_status_idx").on(table.accountStatus, table.updatedAt),
]);

export const customerProjects = sqliteTable("customer_projects", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  title: text("title").notNull(),
  homeNickname: text("home_nickname").notNull().default("My home"),
  postcode: text("postcode").notNull(),
  addressState: text("address_state").notNull(),
  propertyType: text("property_type").notNull(),
  householdSituation: text("household_situation").notNull(),
  goal: text("goal").notNull(),
  goals: text("goals").notNull().default("[]"),
  pace: text("pace").notNull(),
  existingFeatures: text("existing_features").notNull().default("[]"),
  serviceCategories: text("service_categories").notNull().default("[]"),
  priorities: text("priorities").notNull().default("[]"),
  projectStage: text("project_stage").notNull().default("exploring"),
  timing: text("timing").notNull().default("planning"),
  budgetRange: text("budget_range").notNull().default("not_set"),
  propertyContext: text("property_context").notNull().default("{}"),
  privateNotes: text("private_notes").notNull().default(""),
  advisorProfile: text("advisor_profile").notNull().default("{}"),
  planSnapshot: text("plan_snapshot").notNull().default("{}"),
  planRevision: integer("plan_revision").notNull().default(1),
  completedPlanItems: text("completed_plan_items").notNull().default("[]"),
  status: text("status").notNull().default("draft"),
  opportunityId: text("opportunity_id").notNull().default(""),
  submittedAt: text("submitted_at").notNull().default(""),
  archivedAt: text("archived_at").notNull().default(""),
  isSynthetic: integer("is_synthetic", { mode: "boolean" }).notNull().default(false),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("customer_projects_owner_idx").on(table.firebaseUid, table.status, table.updatedAt),
  index("customer_projects_opportunity_idx").on(table.opportunityId),
]);

export const customerConsentReceipts = sqliteTable("customer_consent_receipts", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  projectId: text("project_id").notNull().default(""),
  purpose: text("purpose").notNull(),
  noticeVersion: text("notice_version").notNull(),
  grantedAt: text("granted_at").notNull(),
  withdrawnAt: text("withdrawn_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("customer_consent_receipts_owner_idx").on(table.firebaseUid, table.createdAt),
  index("customer_consent_receipts_project_idx").on(table.projectId, table.createdAt),
]);

export const customerProjectQuotes = sqliteTable("customer_project_quotes", {
  id: text("id").primaryKey(),
  projectId: text("project_id").notNull(),
  opportunityId: text("opportunity_id").notNull(),
  opportunityMatchId: text("opportunity_match_id").notNull(),
  installerUid: text("installer_uid").notNull(),
  submissionRequestId: text("submission_request_id").notNull().default(""),
  submissionRevision: integer("submission_revision").notNull().default(0),
  productListId: text("product_list_id").notNull().default(""),
  inclusions: text("inclusions").notNull().default("[]"),
  productSnapshot: text("product_snapshot").notNull().default("[]"),
  productSubtotalCentsExGst: integer("product_subtotal_cents_ex_gst").notNull().default(0),
  labourCentsExGst: integer("labour_cents_ex_gst").notNull().default(0),
  otherCentsExGst: integer("other_cents_ex_gst").notNull().default(0),
  totalCentsExGst: integer("total_cents_ex_gst").notNull().default(0),
  quoteType: text("quote_type").notNull().default("indicative"),
  startWindow: text("start_window").notNull().default("to_confirm"),
  durationWeeks: integer("duration_weeks").notNull().default(0),
  workmanshipWarrantyYears: integer("workmanship_warranty_years").notNull().default(0),
  status: text("status").notNull().default("submitted"),
  customerDecision: text("customer_decision").notNull().default("reviewing"),
  submittedAt: text("submitted_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("customer_project_quotes_match_idx").on(table.opportunityMatchId),
  index("customer_project_quotes_project_idx").on(table.projectId, table.status, table.updatedAt),
  index("customer_project_quotes_installer_idx").on(table.installerUid, table.updatedAt),
]);

export const customerProjectQuoteSubmissions = sqliteTable("customer_project_quote_submissions", {
  id: text("id").primaryKey(),
  opportunityMatchId: text("opportunity_match_id").notNull(),
  installerUid: text("installer_uid").notNull(),
  submissionRequestId: text("submission_request_id").notNull(),
  quoteId: text("quote_id").notNull(),
  submissionRevision: integer("submission_revision").notNull(),
  quoteSnapshot: text("quote_snapshot").notNull(),
  submittedAt: text("submitted_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("customer_project_quote_submissions_request_idx").on(
    table.installerUid,
    table.opportunityMatchId,
    table.submissionRequestId,
  ),
  uniqueIndex("customer_project_quote_submissions_revision_idx").on(
    table.installerUid,
    table.opportunityMatchId,
    table.submissionRevision,
  ),
  index("customer_project_quote_submissions_quote_idx").on(table.quoteId, table.submittedAt),
]);

export const customerProjectQuoteAcceptanceClaims = sqliteTable("customer_project_quote_acceptance_claims", {
  projectId: text("project_id").primaryKey(),
  customerUid: text("customer_uid").notNull(),
  quoteId: text("quote_id").notNull(),
  opportunityMatchId: text("opportunity_match_id").notNull(),
  contactReleaseId: text("contact_release_id").notNull(),
  acceptedAt: text("accepted_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("customer_project_quote_acceptance_claims_quote_idx").on(table.quoteId),
  index("customer_project_quote_acceptance_claims_owner_idx").on(table.customerUid, table.acceptedAt),
]);

export const customerProjectActivityEvents = sqliteTable("customer_project_activity_events", {
  id: text("id").primaryKey(),
  eventKey: text("event_key").notNull(),
  projectId: text("project_id").notNull(),
  quoteId: text("quote_id").notNull(),
  opportunityMatchId: text("opportunity_match_id").notNull(),
  customerUid: text("customer_uid").notNull(),
  installerUid: text("installer_uid").notNull(),
  eventType: text("event_type").notNull(),
  actorType: text("actor_type").notNull(),
  actorUid: text("actor_uid").notNull(),
  summary: text("summary").notNull(),
  occurredAt: text("occurred_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("customer_project_activity_events_key_idx").on(table.eventKey),
  index("customer_project_activity_events_customer_idx").on(table.customerUid, table.occurredAt, table.id),
  index("customer_project_activity_events_installer_idx").on(table.installerUid, table.occurredAt, table.id),
]);

export const customerProjectActivityDeliveries = sqliteTable("customer_project_activity_deliveries", {
  id: text("id").primaryKey(),
  eventId: text("event_id").notNull(),
  audience: text("audience").notNull(),
  recipientUid: text("recipient_uid").notNull(),
  channel: text("channel").notNull().default("email"),
  provider: text("provider").notNull().default("resend"),
  status: text("status").notNull().default("pending"),
  eligibilityReason: text("eligibility_reason").notNull().default(""),
  attempts: integer("attempts").notNull().default(0),
  nextAttemptAt: text("next_attempt_at").notNull().default(""),
  recipientEmailHash: text("recipient_email_hash").notNull().default(""),
  idempotencyKey: text("idempotency_key").notNull(),
  subject: text("subject").notNull().default(""),
  body: text("body").notNull().default(""),
  html: text("html").notNull().default(""),
  providerMessageId: text("provider_message_id").notNull().default(""),
  providerStatus: text("provider_status").notNull().default(""),
  queuedAt: text("queued_at").notNull(),
  lastAttemptAt: text("last_attempt_at").notNull().default(""),
  sentAt: text("sent_at").notNull().default(""),
  deliveredAt: text("delivered_at").notNull().default(""),
  failedAt: text("failed_at").notNull().default(""),
  lastError: text("last_error").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("customer_project_activity_deliveries_event_idx").on(table.eventId, table.audience, table.channel),
  uniqueIndex("customer_project_activity_deliveries_idempotency_idx").on(table.idempotencyKey),
  uniqueIndex("customer_project_activity_deliveries_provider_message_idx")
    .on(table.provider, table.providerMessageId).where(sql`${table.providerMessageId} <> ''`),
  index("customer_project_activity_deliveries_status_idx").on(table.status, table.nextAttemptAt, table.queuedAt),
  index("customer_project_activity_deliveries_recipient_idx").on(table.recipientUid, table.audience, table.createdAt),
]);

export const customerProjectActivityDeliveryEvents = sqliteTable("customer_project_activity_delivery_events", {
  id: text("id").primaryKey(),
  deliveryId: text("delivery_id").notNull(),
  providerEventKey: text("provider_event_key").notNull(),
  eventType: text("event_type").notNull(),
  providerStatus: text("provider_status").notNull().default(""),
  summary: text("summary").notNull(),
  occurredAt: text("occurred_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("customer_project_activity_delivery_events_provider_idx").on(table.providerEventKey),
  index("customer_project_activity_delivery_events_delivery_idx").on(table.deliveryId, table.occurredAt),
]);

export const tradeCrmCalendarEvents = sqliteTable("trade_crm_calendar_events", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  appointmentId: text("appointment_id").notNull(),
  provider: text("provider").notNull(),
  externalEventId: text("external_event_id").notNull().default(""),
  externalUrl: text("external_url").notNull().default(""),
  appointmentRevision: integer("appointment_revision").notNull().default(0),
  status: text("status").notNull().default("pending"),
  lastError: text("last_error").notNull().default(""),
  lastSyncedAt: text("last_synced_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_crm_calendar_events_owner_appointment_provider_idx").on(table.firebaseUid, table.appointmentId, table.provider),
  index("trade_crm_calendar_events_owner_status_idx").on(table.firebaseUid, table.status, table.updatedAt),
]);

export const customerProjectContactReleases = sqliteTable("customer_project_contact_releases", {
  id: text("id").primaryKey(),
  projectId: text("project_id").notNull(),
  opportunityId: text("opportunity_id").notNull(),
  opportunityMatchId: text("opportunity_match_id").notNull(),
  quoteId: text("quote_id").notNull(),
  customerUid: text("customer_uid").notNull(),
  installerUid: text("installer_uid").notNull(),
  status: text("status").notNull().default("active"),
  noticeVersion: text("notice_version").notNull(),
  disclosedFields: text("disclosed_fields").notNull().default("[]"),
  customerName: text("customer_name").notNull(),
  customerEmail: text("customer_email").notNull(),
  customerPhone: text("customer_phone").notNull(),
  addressLine1: text("address_line_1").notNull(),
  addressLine2: text("address_line_2").notNull().default(""),
  suburb: text("suburb").notNull(),
  addressState: text("address_state").notNull(),
  postcode: text("postcode").notNull(),
  grantedAt: text("granted_at").notNull(),
  withdrawnAt: text("withdrawn_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("customer_project_contact_releases_match_idx").on(table.opportunityMatchId),
  index("customer_project_contact_releases_customer_idx").on(table.customerUid, table.projectId, table.updatedAt),
  index("customer_project_contact_releases_installer_idx").on(table.installerUid, table.status, table.updatedAt),
]);

export const customerProjectContactReleaseEvents = sqliteTable("customer_project_contact_release_events", {
  id: text("id").primaryKey(),
  releaseId: text("release_id").notNull(),
  projectId: text("project_id").notNull(),
  opportunityMatchId: text("opportunity_match_id").notNull(),
  customerUid: text("customer_uid").notNull(),
  installerUid: text("installer_uid").notNull(),
  actorType: text("actor_type").notNull(),
  actorUid: text("actor_uid").notNull(),
  eventType: text("event_type").notNull(),
  noticeVersion: text("notice_version").notNull(),
  disclosedFields: text("disclosed_fields").notNull().default("[]"),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("customer_project_contact_release_events_release_idx").on(table.releaseId, table.createdAt),
  index("customer_project_contact_release_events_project_idx").on(table.customerUid, table.projectId, table.createdAt),
]);

export const customerProjectEvidence = sqliteTable("customer_project_evidence", {
  id: text("id").primaryKey(),
  projectId: text("project_id").notNull(),
  customerUid: text("customer_uid").notNull(),
  clientUploadId: text("client_upload_id").notNull(),
  category: text("category").notNull(),
  captureSlot: text("capture_slot").notNull().default(""),
  factKeys: text("fact_keys").notNull().default("[]"),
  sharingScope: text("sharing_scope").notNull().default("private-plan"),
  fileName: text("file_name").notNull(),
  contentType: text("content_type").notNull(),
  sizeBytes: integer("size_bytes").notNull(),
  objectKey: text("object_key").notNull(),
  privacyStatus: text("privacy_status").notNull().default("not-recorded"),
  revision: integer("revision").notNull().default(1),
  status: text("status").notNull().default("active"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("customer_project_evidence_client_idx").on(table.customerUid, table.projectId, table.clientUploadId),
  index("customer_project_evidence_project_idx").on(table.customerUid, table.projectId, table.status, table.createdAt),
]);

export const customerProjectEvidenceUploadSessions = sqliteTable("customer_project_evidence_upload_sessions", {
  id: text("id").primaryKey(),
  projectId: text("project_id").notNull(),
  customerUid: text("customer_uid").notNull(),
  clientUploadId: text("client_upload_id").notNull(),
  metadataHash: text("metadata_hash").notNull(),
  captureSlot: text("capture_slot").notNull().default(""),
  replacementEvidenceId: text("replacement_evidence_id").notNull().default(""),
  replacementObjectKey: text("replacement_object_key").notNull().default(""),
  expectedEvidenceRevision: integer("expected_evidence_revision").notNull().default(0),
  stagingObjectKey: text("staging_object_key").notNull(),
  uploadId: text("upload_id").notNull(),
  contentType: text("content_type").notNull(),
  sizeBytes: integer("size_bytes").notNull(),
  category: text("category").notNull(),
  factKeys: text("fact_keys").notNull().default("[]"),
  sharingScope: text("sharing_scope").notNull().default("private-plan"),
  partSizeBytes: integer("part_size_bytes").notNull(),
  status: text("status").notNull().default("initiated"),
  evidenceId: text("evidence_id").notNull(),
  privacyStatus: text("privacy_status").notNull().default("pending"),
  expiresAt: text("expires_at").notNull(),
  completedAt: text("completed_at").notNull().default(""),
  lastError: text("last_error").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("customer_project_evidence_upload_owner_client_idx")
    .on(table.customerUid, table.projectId, table.clientUploadId),
  uniqueIndex("customer_project_evidence_upload_staging_idx").on(table.stagingObjectKey),
  uniqueIndex("customer_project_evidence_upload_replacement_idx")
    .on(table.customerUid, table.projectId, table.replacementEvidenceId)
    .where(sql`${table.replacementEvidenceId} <> '' AND ${table.status} IN ('initiated','uploading','completing')`),
  index("customer_project_evidence_upload_project_idx")
    .on(table.customerUid, table.projectId, table.status, table.updatedAt),
  index("customer_project_evidence_upload_expiry_idx").on(table.status, table.expiresAt),
]);

export const customerProjectEvidenceUploadParts = sqliteTable("customer_project_evidence_upload_parts", {
  id: text("id").primaryKey(),
  sessionId: text("session_id").notNull(),
  partNumber: integer("part_number").notNull(),
  etag: text("etag").notNull(),
  sizeBytes: integer("size_bytes").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("customer_project_evidence_upload_parts_session_part_idx")
    .on(table.sessionId, table.partNumber),
  index("customer_project_evidence_upload_parts_session_idx")
    .on(table.sessionId, table.partNumber),
]);

export const customerProjectEvidenceEvents = sqliteTable("customer_project_evidence_events", {
  id: text("id").primaryKey(),
  evidenceId: text("evidence_id").notNull(),
  projectId: text("project_id").notNull(),
  customerUid: text("customer_uid").notNull(),
  installerUid: text("installer_uid").notNull().default(""),
  actorType: text("actor_type").notNull(),
  actorUid: text("actor_uid").notNull(),
  eventType: text("event_type").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("customer_project_evidence_events_item_idx").on(table.evidenceId, table.createdAt),
  index("customer_project_evidence_events_project_idx").on(table.customerUid, table.projectId, table.createdAt),
]);

export const customerProjectPlanRevisions = sqliteTable("customer_project_plan_revisions", {
  id: text("id").primaryKey(),
  projectId: text("project_id").notNull(),
  customerUid: text("customer_uid").notNull(),
  revisionNumber: integer("revision_number").notNull(),
  eventType: text("event_type").notNull().default("saved"),
  planVersion: text("plan_version").notNull().default(""),
  goals: text("goals").notNull().default("[]"),
  homeFeatures: text("home_features").notNull().default("[]"),
  pace: text("pace").notNull().default(""),
  budgetRange: text("budget_range").notNull().default(""),
  planSnapshot: text("plan_snapshot").notNull().default("{}"),
  restoredFromRevision: integer("restored_from_revision").notNull().default(0),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("customer_project_plan_revisions_number_idx").on(table.projectId, table.revisionNumber),
  index("customer_project_plan_revisions_owner_idx").on(table.customerUid, table.projectId, table.createdAt),
]);

export const customerProjectOutcomeCheckins = sqliteTable("customer_project_outcome_checkins", {
  id: text("id").primaryKey(),
  projectId: text("project_id").notNull(),
  customerUid: text("customer_uid").notNull(),
  comfortOutcome: text("comfort_outcome").notNull(),
  energyOutcome: text("energy_outcome").notNull(),
  completedItemIds: text("completed_item_ids").notNull().default("[]"),
  note: text("note").notNull().default(""),
  recordedAt: text("recorded_at").notNull(),
}, (table) => [
  index("customer_project_outcome_checkins_owner_idx").on(table.customerUid, table.projectId, table.recordedAt),
]);

export const customerProjectArrivalProposals = sqliteTable("customer_project_arrival_proposals", {
  id: text("id").primaryKey(),
  projectId: text("project_id").notNull(),
  quoteId: text("quote_id").notNull(),
  opportunityMatchId: text("opportunity_match_id").notNull(),
  customerUid: text("customer_uid").notNull(),
  installerUid: text("installer_uid").notNull(),
  status: text("status").notNull().default("proposed"),
  windows: text("windows").notNull().default("[]"),
  installerNote: text("installer_note").notNull().default(""),
  selectedWindow: text("selected_window").notNull().default("{}"),
  revision: integer("revision").notNull().default(1),
  proposedAt: text("proposed_at").notNull(),
  selectedAt: text("selected_at").notNull().default(""),
  directContactSnapshot: text("direct_contact_snapshot").notNull().default("{}"),
  directContactSelectedAt: text("direct_contact_selected_at").notNull().default(""),
  crmWorkOrderId: text("crm_work_order_id").notNull().default(""),
  crmAppointmentId: text("crm_appointment_id").notNull().default(""),
  preparationAcknowledgedAt: text("preparation_acknowledged_at").notNull().default(""),
  withdrawnAt: text("withdrawn_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("customer_project_arrival_proposals_match_idx").on(table.opportunityMatchId),
  index("customer_project_arrival_proposals_customer_idx").on(table.customerUid, table.projectId, table.updatedAt),
  index("customer_project_arrival_proposals_installer_idx").on(table.installerUid, table.status, table.updatedAt),
]);

export const customerProjectArrivalEvents = sqliteTable("customer_project_arrival_events", {
  id: text("id").primaryKey(),
  proposalId: text("proposal_id").notNull(),
  projectId: text("project_id").notNull(),
  opportunityMatchId: text("opportunity_match_id").notNull(),
  customerUid: text("customer_uid").notNull(),
  installerUid: text("installer_uid").notNull(),
  actorType: text("actor_type").notNull(),
  actorUid: text("actor_uid").notNull(),
  eventType: text("event_type").notNull(),
  proposalRevision: integer("proposal_revision").notNull(),
  windows: text("windows").notNull().default("[]"),
  selectedWindow: text("selected_window").notNull().default("{}"),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("customer_project_arrival_events_proposal_idx").on(table.proposalId, table.createdAt),
  index("customer_project_arrival_events_project_idx").on(table.customerUid, table.projectId, table.createdAt),
]);

export const supplierProducts = sqliteTable("supplier_products", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  modelNumber: text("model_number").notNull(),
  brand: text("brand").notNull(),
  name: text("name").notNull(),
  category: text("category").notNull(),
  description: text("description").notNull(),
  unitPriceCentsExGst: integer("unit_price_cents_ex_gst").notNull(),
  minOrderQty: integer("min_order_qty").notNull().default(1),
  orderIncrement: integer("order_increment").notNull().default(1),
  unitLabel: text("unit_label").notNull().default("each"),
  stockStatus: text("stock_status").notNull().default("order_in"),
  leadTimeDays: integer("lead_time_days").notNull().default(0),
  warrantyYears: integer("warranty_years").notNull().default(0),
  datasheetUrl: text("datasheet_url").notNull().default(""),
  listingStatus: text("listing_status").notNull().default("draft"),
  reviewStatus: text("review_status").notNull().default("pending"),
  reviewNote: text("review_note").notNull().default(""),
  isSynthetic: integer("is_synthetic", { mode: "boolean" }).notNull().default(false),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("supplier_products_owner_model_idx").on(table.firebaseUid, table.modelNumber),
  index("supplier_products_owner_idx").on(table.firebaseUid, table.updatedAt),
  index("supplier_products_listing_idx").on(table.listingStatus, table.reviewStatus),
  index("supplier_products_category_idx").on(table.category),
  index("supplier_products_marketplace_name_idx").on(table.listingStatus, table.reviewStatus, sql`${table.name} COLLATE NOCASE`, sql`${table.brand} COLLATE NOCASE`, sql`${table.modelNumber} COLLATE NOCASE`, table.id),
  index("supplier_products_marketplace_brand_idx").on(table.listingStatus, table.reviewStatus, sql`${table.brand} COLLATE NOCASE`, sql`${table.name} COLLATE NOCASE`, sql`${table.modelNumber} COLLATE NOCASE`, table.id),
  index("supplier_products_marketplace_model_idx").on(table.listingStatus, table.reviewStatus, sql`${table.modelNumber} COLLATE NOCASE`, sql`${table.name} COLLATE NOCASE`, table.id),
  index("supplier_products_marketplace_price_idx").on(table.listingStatus, table.reviewStatus, table.unitPriceCentsExGst, sql`${table.name} COLLATE NOCASE`, table.id),
  index("supplier_products_marketplace_lead_idx").on(table.listingStatus, table.reviewStatus, table.leadTimeDays, sql`${table.name} COLLATE NOCASE`, table.id),
  index("supplier_products_marketplace_filter_idx").on(table.listingStatus, table.reviewStatus, table.category, table.stockStatus, table.unitPriceCentsExGst, table.id),
]);

export const supplierProductLinks = sqliteTable("supplier_product_links", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  productId: text("product_id").notNull(),
  linkedProductId: text("linked_product_id").notNull(),
  relationship: text("relationship").notNull().default("recommended"),
  defaultQty: integer("default_qty").notNull().default(1),
  note: text("note").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("supplier_product_links_unique_idx").on(table.productId, table.linkedProductId, table.relationship),
  index("supplier_product_links_owner_idx").on(table.firebaseUid),
  index("supplier_product_links_product_idx").on(table.productId),
]);

export const installerCataloguePreferences = sqliteTable("installer_catalogue_preferences", {
  firebaseUid: text("firebase_uid").primaryKey(),
  search: text("search").notNull().default(""),
  modelSearch: text("model_search").notNull().default(""),
  category: text("category").notNull().default(""),
  supplierUid: text("supplier_uid").notNull().default(""),
  brand: text("brand").notNull().default(""),
  serviceState: text("service_state").notNull().default(""),
  stockStatus: text("stock_status").notNull().default(""),
  minimumPriceCents: integer("minimum_price_cents").notNull().default(0),
  maximumPriceCents: integer("maximum_price_cents").notNull().default(0),
  maximumLeadDays: integer("maximum_lead_days").notNull().default(-1),
  minimumWarrantyYears: integer("minimum_warranty_years").notNull().default(0),
  sortKey: text("sort_key").notNull().default("name-asc"),
  pageSize: integer("page_size").notNull().default(25),
  visibleColumns: text("visible_columns").notNull().default('["supplier","brand","model","name","category","price","ordering","stock","lead","warranty","states","kit","actions"]'),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("installer_catalogue_preferences_updated_idx").on(table.updatedAt),
]);

export const workspaceListViews = sqliteTable("workspace_list_views", {
  id: text("id").primaryKey(),
  ownerUid: text("owner_uid").notNull(),
  ownerScope: text("owner_scope").notNull(),
  viewKey: text("view_key").notNull(),
  preferences: text("preferences").notNull().default("{}"),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("workspace_list_views_owner_view_idx").on(table.ownerUid, table.ownerScope, table.viewKey),
  index("workspace_list_views_owner_idx").on(table.ownerUid, table.ownerScope, table.updatedAt),
]);

export const installerProductLists = sqliteTable("installer_product_lists", {
  id: text("id").primaryKey(),
  firebaseUid: text("firebase_uid").notNull(),
  name: text("name").notNull(),
  projectPostcode: text("project_postcode").notNull().default(""),
  notes: text("notes").notNull().default(""),
  status: text("status").notNull().default("draft"),
  submittedAt: text("submitted_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("installer_product_lists_owner_idx").on(table.firebaseUid, table.status, table.updatedAt),
]);

export const installerProductListItems = sqliteTable("installer_product_list_items", {
  id: text("id").primaryKey(),
  listId: text("list_id").notNull(),
  productId: text("product_id").notNull(),
  supplierUid: text("supplier_uid").notNull(),
  quantity: integer("quantity").notNull().default(1),
  unitPriceCentsExGst: integer("unit_price_cents_ex_gst").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("installer_product_list_items_unique_idx").on(table.listId, table.productId),
  index("installer_product_list_items_list_idx").on(table.listId),
  index("installer_product_list_items_supplier_idx").on(table.supplierUid, table.updatedAt),
]);

export const supplierProductEnquiries = sqliteTable("supplier_product_enquiries", {
  id: text("id").primaryKey(),
  listId: text("list_id").notNull(),
  installerUid: text("installer_uid").notNull(),
  supplierUid: text("supplier_uid").notNull(),
  status: text("status").notNull().default("new"),
  message: text("message").notNull().default(""),
  supplierNote: text("supplier_note").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("supplier_product_enquiries_list_supplier_idx").on(table.listId, table.supplierUid),
  index("supplier_product_enquiries_supplier_idx").on(table.supplierUid, table.status, table.updatedAt),
  index("supplier_product_enquiries_installer_idx").on(table.installerUid, table.updatedAt),
]);

export const tradePurchaseOrders = sqliteTable("trade_purchase_orders", {
  id: text("id").primaryKey(),
  orderNumber: text("order_number").notNull(),
  enquiryId: text("enquiry_id").notNull(),
  listId: text("list_id").notNull(),
  installerUid: text("installer_uid").notNull(),
  supplierUid: text("supplier_uid").notNull(),
  status: text("status").notNull().default("submitted"),
  installerReference: text("installer_reference").notNull().default(""),
  supplierReference: text("supplier_reference").notNull().default(""),
  deliveryMethod: text("delivery_method").notNull().default("confirm_with_supplier"),
  deliveryNotes: text("delivery_notes").notNull().default(""),
  supplierNote: text("supplier_note").notNull().default(""),
  expectedAt: text("expected_at").notNull().default(""),
  subtotalCentsExGst: integer("subtotal_cents_ex_gst").notNull().default(0),
  gstCents: integer("gst_cents").notNull().default(0),
  totalCentsIncGst: integer("total_cents_inc_gst").notNull().default(0),
  submittedAt: text("submitted_at").notNull(),
  confirmedAt: text("confirmed_at").notNull().default(""),
  fulfilledAt: text("fulfilled_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_purchase_orders_number_idx").on(table.orderNumber),
  uniqueIndex("trade_purchase_orders_enquiry_idx").on(table.enquiryId),
  index("trade_purchase_orders_installer_idx").on(table.installerUid, table.status, table.updatedAt),
  index("trade_purchase_orders_supplier_idx").on(table.supplierUid, table.status, table.updatedAt),
]);

export const tradePurchaseOrderItems = sqliteTable("trade_purchase_order_items", {
  id: text("id").primaryKey(),
  purchaseOrderId: text("purchase_order_id").notNull(),
  supplierProductId: text("supplier_product_id").notNull(),
  modelNumber: text("model_number").notNull(),
  brand: text("brand").notNull(),
  productName: text("product_name").notNull(),
  unitLabel: text("unit_label").notNull().default("each"),
  quantity: integer("quantity").notNull().default(1),
  fulfilledQuantity: integer("fulfilled_quantity").notNull().default(0),
  unitPriceCentsExGst: integer("unit_price_cents_ex_gst").notNull(),
  warrantyYears: integer("warranty_years").notNull().default(0),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_purchase_order_items_product_idx").on(table.purchaseOrderId, table.supplierProductId),
  index("trade_purchase_order_items_order_idx").on(table.purchaseOrderId),
]);

export const tradePurchaseOrderEvents = sqliteTable("trade_purchase_order_events", {
  id: text("id").primaryKey(),
  purchaseOrderId: text("purchase_order_id").notNull(),
  eventType: text("event_type").notNull(),
  status: text("status").notNull(),
  summary: text("summary").notNull(),
  actorType: text("actor_type").notNull(),
  actorUid: text("actor_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("trade_purchase_order_events_order_idx").on(table.purchaseOrderId, table.createdAt),
]);

export const tradeWarrantyClaims = sqliteTable("trade_warranty_claims", {
  id: text("id").primaryKey(),
  claimNumber: text("claim_number").notNull(),
  purchaseOrderId: text("purchase_order_id").notNull(),
  purchaseOrderItemId: text("purchase_order_item_id").notNull(),
  installerUid: text("installer_uid").notNull(),
  supplierUid: text("supplier_uid").notNull(),
  status: text("status").notNull().default("submitted"),
  issueCategory: text("issue_category").notNull(),
  summary: text("summary").notNull(),
  serialNumber: text("serial_number").notNull().default(""),
  supplierResponse: text("supplier_response").notNull().default(""),
  resolution: text("resolution").notNull().default(""),
  submittedAt: text("submitted_at").notNull(),
  resolvedAt: text("resolved_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("trade_warranty_claims_number_idx").on(table.claimNumber),
  index("trade_warranty_claims_installer_idx").on(table.installerUid, table.status, table.updatedAt),
  index("trade_warranty_claims_supplier_idx").on(table.supplierUid, table.status, table.updatedAt),
  index("trade_warranty_claims_order_idx").on(table.purchaseOrderId, table.updatedAt),
]);

export const complianceOrganisations = sqliteTable("compliance_organisations", {
  id: text("id").primaryKey(),
  organisationCode: text("organisation_code").notNull(),
  legalName: text("legal_name").notNull(),
  tradingName: text("trading_name").notNull().default(""),
  abn: text("abn").notNull().default(""),
  status: text("status").notNull().default("active"),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_organisations_code_idx").on(table.organisationCode),
  index("compliance_organisations_status_name_idx").on(table.status, table.tradingName, table.legalName),
  check("compliance_organisations_status_check", sql`${table.status} IN ('active', 'suspended', 'retired')`),
]);

export const complianceUsers = sqliteTable("compliance_users", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  email: text("email").notNull(),
  displayName: text("display_name").notNull().default(""),
  role: text("role").notNull(),
  status: text("status").notNull().default("active"),
  governanceIdentityVerified: integer("governance_identity_verified").notNull().default(0),
  governanceIdentityVerifiedByUid: text("governance_identity_verified_by_uid").notNull().default(""),
  governanceIdentityVerifiedAt: text("governance_identity_verified_at").notNull().default(""),
  governanceIdentityVerificationBasis: text("governance_identity_verification_basis").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  lastLoginAt: text("last_login_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_users_org_uid_idx").on(table.organisationId, table.firebaseUid),
  uniqueIndex("compliance_users_org_email_idx").on(table.organisationId, sql`${table.email} COLLATE NOCASE`),
  index("compliance_users_uid_status_idx").on(table.firebaseUid, table.status),
  index("compliance_users_org_role_status_idx").on(table.organisationId, table.role, table.status),
  index("compliance_users_org_governance_identity_idx").on(table.organisationId, table.governanceIdentityVerified, table.role, table.status),
  check("compliance_users_role_check", sql`${table.role} IN ('admin', 'case_manager', 'reviewer', 'auditor')`),
  check("compliance_users_status_check", sql`${table.status} IN ('active', 'suspended', 'revoked')`),
  check("compliance_users_governance_identity_check", sql`(${table.governanceIdentityVerified} = 0 AND ${table.governanceIdentityVerifiedByUid} = '' AND ${table.governanceIdentityVerifiedAt} = '' AND ${table.governanceIdentityVerificationBasis} = '') OR (${table.governanceIdentityVerified} = 1 AND trim(${table.governanceIdentityVerifiedByUid}) <> '' AND ${table.governanceIdentityVerifiedByUid} <> ${table.firebaseUid} AND trim(${table.governanceIdentityVerifiedAt}) <> '' AND trim(${table.governanceIdentityVerificationBasis}) <> '')`),
]);

export const compliancePrograms = sqliteTable("compliance_programs", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  programCode: text("program_code").notNull(),
  name: text("name").notNull(),
  schemeKind: text("scheme_kind").notNull(),
  jurisdiction: text("jurisdiction").notNull(),
  administeringBody: text("administering_body").notNull(),
  officialSourceUrl: text("official_source_url").notNull(),
  officialSourceTitle: text("official_source_title").notNull(),
  officialSourceVersion: text("official_source_version").notNull().default(""),
  officialSourceSha256: text("official_source_sha256").notNull().default(""),
  officialSourceCheckedAt: text("official_source_checked_at").notNull(),
  publishState: text("publish_state").notNull().default("draft"),
  publicationRequestId: text("publication_request_id").notNull().default(""),
  publicationSnapshotSha256: text("publication_snapshot_sha256").notNull().default(""),
  publishedByUid: text("published_by_uid").notNull().default(""),
  publishedAt: text("published_at").notNull().default(""),
  withdrawnByUid: text("withdrawn_by_uid").notNull().default(""),
  withdrawnAt: text("withdrawn_at").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_programs_org_code_idx").on(table.organisationId, table.programCode),
  index("compliance_programs_org_state_idx").on(table.organisationId, table.publishState, table.name),
  index("compliance_programs_scheme_jurisdiction_idx").on(table.schemeKind, table.jurisdiction, table.publishState),
  check("compliance_programs_publish_state_check", sql`${table.publishState} IN ('draft', 'published', 'withdrawn')`),
  check("compliance_programs_jurisdiction_check", sql`${table.jurisdiction} IN ('AU', 'ACT', 'NSW', 'NT', 'QLD', 'SA', 'TAS', 'VIC', 'WA')`),
  check("compliance_programs_source_hash_check", sql`(${table.officialSourceSha256} = '' OR (length(${table.officialSourceSha256}) = 64 AND ${table.officialSourceSha256} NOT GLOB '*[^0-9a-fA-F]*')) AND (${table.publishState} = 'draft' OR length(${table.officialSourceSha256}) = 64)`),
  check("compliance_programs_publication_hash_check", sql`${table.publicationSnapshotSha256} = '' OR (length(${table.publicationSnapshotSha256}) = 64 AND lower(${table.publicationSnapshotSha256}) NOT GLOB '*[^0-9a-f]*')`),
  check("compliance_programs_provenance_check", sql`(${table.publishState} = 'draft' AND ${table.publishedByUid} = '' AND ${table.publishedAt} = '' AND ${table.withdrawnByUid} = '' AND ${table.withdrawnAt} = '') OR (${table.publishState} = 'published' AND trim(${table.publishedByUid}) <> '' AND trim(${table.publishedAt}) <> '' AND ${table.withdrawnByUid} = '' AND ${table.withdrawnAt} = '') OR (${table.publishState} = 'withdrawn' AND trim(${table.publishedByUid}) <> '' AND trim(${table.publishedAt}) <> '' AND trim(${table.withdrawnByUid}) <> '' AND trim(${table.withdrawnAt}) <> '')`),
]);

export const complianceActivityVersions = sqliteTable("compliance_activity_versions", {
  id: text("id").primaryKey(),
  programId: text("program_id").notNull(),
  activityKey: text("activity_key").notNull(),
  version: integer("version").notNull(),
  title: text("title").notNull(),
  serviceCategory: text("service_category").notNull(),
  registryActivityCode: text("registry_activity_code").notNull().default(""),
  specificationPart: text("specification_part").notNull().default(""),
  productCategory: text("product_category").notNull(),
  scenarioCode: text("scenario_code").notNull().default(""),
  scenario: text("scenario").notNull(),
  jurisdiction: text("jurisdiction").notNull(),
  effectiveFrom: text("effective_from").notNull(),
  effectiveTo: text("effective_to").notNull().default(""),
  officialSourceUrl: text("official_source_url").notNull(),
  officialSourceTitle: text("official_source_title").notNull(),
  officialSourceVersion: text("official_source_version").notNull().default(""),
  officialSourceSha256: text("official_source_sha256").notNull().default(""),
  officialSourceCheckedAt: text("official_source_checked_at").notNull(),
  requirementsSnapshot: text("requirements_snapshot").notNull().default("{}"),
  publishState: text("publish_state").notNull().default("draft"),
  publicationRequestId: text("publication_request_id").notNull().default(""),
  publicationSnapshotSha256: text("publication_snapshot_sha256").notNull().default(""),
  calculationApprovalState: text("calculation_approval_state").notNull().default("not_assessed"),
  publishedByUid: text("published_by_uid").notNull().default(""),
  publishedAt: text("published_at").notNull().default(""),
  withdrawnByUid: text("withdrawn_by_uid").notNull().default(""),
  withdrawnAt: text("withdrawn_at").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_activity_versions_key_version_idx").on(table.programId, table.activityKey, table.version),
  index("compliance_activity_versions_program_state_idx").on(table.programId, table.publishState, table.effectiveFrom, table.effectiveTo),
  index("compliance_activity_versions_installer_idx").on(table.serviceCategory, table.jurisdiction, table.publishState, table.effectiveFrom),
  index("compliance_activity_versions_registry_idx").on(table.registryActivityCode, table.specificationPart, table.scenarioCode),
  check("compliance_activity_versions_version_check", sql`${table.version} > 0`),
  check("compliance_activity_versions_service_category_check", sql`${table.serviceCategory} IN ('assessment', 'solar', 'battery', 'heating-cooling', 'hot-water', 'draught-proofing', 'insulation', 'glazing', 'window-coverings', 'ev-charging', 'electrical', 'plumbing', 'mounting-hardware', 'controls', 'other')`),
  check("compliance_activity_versions_publish_state_check", sql`${table.publishState} IN ('draft', 'published', 'withdrawn')`),
  check("compliance_activity_versions_jurisdiction_check", sql`${table.jurisdiction} IN ('AU', 'ACT', 'NSW', 'NT', 'QLD', 'SA', 'TAS', 'VIC', 'WA')`),
  check("compliance_activity_versions_calculation_state_check", sql`${table.calculationApprovalState} IN ('not_assessed', 'approved', 'rejected', 'not_applicable')`),
  check("compliance_activity_versions_date_check", sql`date(${table.effectiveFrom}) = ${table.effectiveFrom} AND (${table.effectiveTo} = '' OR (date(${table.effectiveTo}) = ${table.effectiveTo} AND ${table.effectiveTo} >= ${table.effectiveFrom}))`),
  check("compliance_activity_versions_source_hash_check", sql`(${table.officialSourceSha256} = '' OR (length(${table.officialSourceSha256}) = 64 AND ${table.officialSourceSha256} NOT GLOB '*[^0-9a-fA-F]*')) AND (${table.publishState} = 'draft' OR length(${table.officialSourceSha256}) = 64)`),
  check("compliance_activity_versions_publication_hash_check", sql`${table.publicationSnapshotSha256} = '' OR (length(${table.publicationSnapshotSha256}) = 64 AND lower(${table.publicationSnapshotSha256}) NOT GLOB '*[^0-9a-f]*')`),
  check("compliance_activity_versions_requirements_check", sql`json_valid(${table.requirementsSnapshot})`),
  check("compliance_activity_versions_provenance_check", sql`(${table.publishState} = 'draft' AND ${table.publishedByUid} = '' AND ${table.publishedAt} = '' AND ${table.withdrawnByUid} = '' AND ${table.withdrawnAt} = '') OR (${table.publishState} = 'published' AND trim(${table.publishedByUid}) <> '' AND trim(${table.publishedAt}) <> '' AND ${table.withdrawnByUid} = '' AND ${table.withdrawnAt} = '') OR (${table.publishState} = 'withdrawn' AND trim(${table.publishedByUid}) <> '' AND trim(${table.publishedAt}) <> '' AND trim(${table.withdrawnByUid}) <> '' AND trim(${table.withdrawnAt}) <> '')`),
]);

export const complianceCases = sqliteTable("compliance_cases", {
  id: text("id").primaryKey(),
  caseNumber: text("case_number").notNull(),
  organisationId: text("organisation_id").notNull(),
  programId: text("program_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  commercialHandoffId: text("commercial_handoff_id").notNull().default(""),
  acceptedQuoteVersionId: text("accepted_quote_version_id").notNull().default(""),
  acceptedScopeSha256: text("accepted_scope_sha256").notNull().default(""),
  installerUid: text("installer_uid").notNull(),
  activityVersionId: text("activity_version_id").notNull(),
  evidencePolicyVersionId: text("evidence_policy_version_id").notNull(),
  activityDate: text("activity_date").notNull(),
  siteJurisdiction: text("site_jurisdiction").notNull(),
  activitySnapshot: text("activity_snapshot").notNull(),
  status: text("status").notNull().default("draft"),
  evidenceStatus: text("evidence_status").notNull().default("not_started"),
  revision: integer("revision").notNull().default(1),
  createdByType: text("created_by_type").notNull(),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_cases_number_idx").on(table.caseNumber),
  uniqueIndex("compliance_cases_active_work_order_idx")
    .on(table.workOrderId)
    .where(sql`${table.status} <> 'closed'`),
  index("compliance_cases_org_status_idx").on(table.organisationId, table.status, table.evidenceStatus, table.updatedAt),
  index("compliance_cases_work_order_idx").on(table.workOrderId, table.createdAt),
  index("compliance_cases_handoff_idx").on(table.installerUid, table.commercialHandoffId, table.createdAt),
  index("compliance_cases_installer_idx").on(table.installerUid, table.status, table.updatedAt),
  index("compliance_cases_activity_idx").on(table.activityVersionId, table.createdAt),
  index("compliance_cases_evidence_policy_idx").on(table.evidencePolicyVersionId, table.createdAt),
  check("compliance_cases_status_check", sql`${table.status} IN ('draft', 'ready_for_submission', 'submitted', 'in_review', 'changes_requested', 'accepted', 'rejected', 'closed')`),
  check("compliance_cases_evidence_status_check", sql`${table.evidenceStatus} IN ('not_started', 'in_progress', 'complete', 'changes_required', 'verified')`),
  check("compliance_cases_revision_check", sql`${table.revision} > 0`),
  check("compliance_cases_activity_date_check", sql`length(${table.activityDate}) = 10 AND ${table.activityDate} GLOB '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]' AND date(${table.activityDate}) = ${table.activityDate}`),
  check("compliance_cases_site_jurisdiction_check", sql`${table.siteJurisdiction} IN ('ACT', 'NSW', 'NT', 'QLD', 'SA', 'TAS', 'VIC', 'WA')`),
  check("compliance_cases_actor_type_check", sql`${table.createdByType} IN ('installer', 'compliance', 'platform')`),
  check("compliance_cases_snapshot_check", sql`json_valid(${table.activitySnapshot})`),
  check("compliance_cases_evidence_policy_check", sql`trim(${table.evidencePolicyVersionId}) <> ''`),
  check("compliance_cases_handoff_hash_check", sql`${table.acceptedScopeSha256} = '' OR (length(${table.acceptedScopeSha256}) = 64 AND lower(${table.acceptedScopeSha256}) NOT GLOB '*[^0-9a-f]*')`),
]);

export const complianceCaseEvents = sqliteTable("compliance_case_events", {
  id: text("id").primaryKey(),
  caseId: text("case_id").notNull(),
  organisationId: text("organisation_id").notNull(),
  eventType: text("event_type").notNull(),
  actorType: text("actor_type").notNull(),
  actorUid: text("actor_uid").notNull(),
  summary: text("summary").notNull(),
  metadata: text("metadata").notNull().default("{}"),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("compliance_case_events_case_time_idx").on(table.caseId, table.createdAt),
  index("compliance_case_events_org_time_idx").on(table.organisationId, table.createdAt),
  check("compliance_case_events_actor_type_check", sql`${table.actorType} IN ('installer', 'compliance', 'platform')`),
  check("compliance_case_events_metadata_check", sql`json_valid(${table.metadata})`),
]);

export const certificatePriceHistory = sqliteTable("certificate_price_history", {
  id: text("id").primaryKey(),
  certificateCode: text("certificate_code").notNull(),
  tradedOn: text("traded_on").notNull(),
  priceCents: integer("price_cents").notNull(),
  sourceUrl: text("source_url").notNull(),
  capturedAt: text("captured_at").notNull(),
}, (table) => [
  uniqueIndex("certificate_price_history_code_date_idx").on(table.certificateCode, table.tradedOn),
  index("certificate_price_history_date_idx").on(table.tradedOn),
]);

export const certificatePriceSyncRuns = sqliteTable("certificate_price_sync_runs", {
  id: text("id").primaryKey(),
  sourceName: text("source_name").notNull(),
  status: text("status").notNull(),
  recordCount: integer("record_count").notNull().default(0),
  message: text("message").notNull().default(""),
  fetchedAt: text("fetched_at").notNull(),
}, (table) => [
  index("certificate_price_sync_runs_status_date_idx").on(table.status, table.fetchedAt),
]);

export const apiPerformanceSamples = sqliteTable("api_performance_samples", {
  id: text("id").primaryKey(),
  routeKey: text("route_key").notNull(),
  method: text("method").notNull(),
  statusCode: integer("status_code").notNull(),
  outcome: text("outcome").notNull(),
  durationMs: integer("duration_ms").notNull(),
  dbDurationMs: integer("db_duration_ms").notNull().default(0),
  resultCount: integer("result_count").notNull().default(0),
  cursorUsed: integer("cursor_used", { mode: "boolean" }).notNull().default(false),
  sampledAt: text("sampled_at").notNull(),
}, (table) => [
  index("api_performance_samples_route_time_idx").on(table.routeKey, table.sampledAt),
  index("api_performance_samples_time_idx").on(table.sampledAt),
  index("api_performance_samples_duration_idx").on(table.durationMs, table.sampledAt),
]);

export const complianceInvitations = sqliteTable("compliance_invitations", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  email: text("email").notNull(),
  displayName: text("display_name").notNull().default(""),
  role: text("role").notNull(),
  status: text("status").notNull().default("pending"),
  invitedByUid: text("invited_by_uid").notNull(),
  expiresAt: text("expires_at").notNull(),
  claimedByUid: text("claimed_by_uid").notNull().default(""),
  claimedAt: text("claimed_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_invitations_org_email_idx")
    .on(table.organisationId, sql`${table.email} COLLATE NOCASE`)
    .where(sql`${table.status} IN ('pending', 'claimed')`),
  index("compliance_invitations_email_status_idx").on(sql`${table.email} COLLATE NOCASE`, table.status, table.expiresAt),
  check("compliance_invitations_role_check", sql`${table.role} IN ('admin', 'case_manager', 'reviewer', 'auditor')`),
  check("compliance_invitations_status_check", sql`${table.status} IN ('pending', 'claimed', 'revoked', 'expired')`),
  check("compliance_invitations_email_check", sql`length(${table.email}) BETWEEN 3 AND 320 AND ${table.email} = lower(trim(${table.email}))`),
  check("compliance_invitations_claimed_state_check", sql`(${table.status} = 'claimed' AND ${table.claimedByUid} <> '' AND ${table.claimedAt} <> '') OR (${table.status} <> 'claimed' AND ${table.claimedByUid} = '' AND ${table.claimedAt} = '')`),
]);

export const complianceAuditEvents = sqliteTable("compliance_audit_events", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  actorType: text("actor_type").notNull(),
  actorUid: text("actor_uid").notNull(),
  eventType: text("event_type").notNull(),
  targetType: text("target_type").notNull(),
  targetId: text("target_id").notNull(),
  summary: text("summary").notNull(),
  metadata: text("metadata").notNull().default("{}"),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("compliance_audit_events_org_time_idx").on(table.organisationId, table.createdAt, table.id),
  index("compliance_audit_events_target_idx").on(table.organisationId, table.targetType, table.targetId, table.createdAt),
  check("compliance_audit_events_actor_check", sql`${table.actorType} IN ('installer', 'compliance', 'platform')`),
  check("compliance_audit_events_metadata_check", sql`json_valid(${table.metadata})`),
]);

export const complianceWriteGuards = sqliteTable("compliance_write_guards", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  operationId: text("operation_id").notNull(),
  stepNumber: integer("step_number").notNull(),
  verified: integer("verified").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_write_guards_operation_step_idx").on(table.operationId, table.stepNumber),
  index("compliance_write_guards_org_time_idx").on(table.organisationId, table.createdAt, table.id),
  check("compliance_write_guards_step_check", sql`${table.stepNumber} > 0`),
  check("compliance_write_guards_verified_check", sql`${table.verified} = 1`),
]);

export const complianceGovernanceRequests = sqliteTable("compliance_governance_requests", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  targetType: text("target_type").notNull(),
  targetId: text("target_id").notNull(),
  action: text("action").notNull(),
  sealedSnapshot: text("sealed_snapshot").notNull(),
  sealedSnapshotSha256: text("sealed_snapshot_sha256").notNull(),
  status: text("status").notNull().default("pending"),
  requestReason: text("request_reason").notNull(),
  requestedByUid: text("requested_by_uid").notNull(),
  requestedAt: text("requested_at").notNull(),
  reviewedByUid: text("reviewed_by_uid").notNull().default(""),
  reviewedAt: text("reviewed_at").notNull().default(""),
  reviewNote: text("review_note").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_governance_requests_pending_idx")
    .on(table.organisationId, table.targetType, table.targetId, table.action)
    .where(sql`${table.status} = 'pending'`),
  index("compliance_governance_requests_org_status_idx")
    .on(table.organisationId, table.status, table.requestedAt, table.id),
  index("compliance_governance_requests_target_idx")
    .on(table.organisationId, table.targetType, table.targetId, table.requestedAt, table.id),
  check("compliance_governance_requests_target_check", sql`${table.targetType} IN ('program', 'activity', 'evidence_policy')`),
  check("compliance_governance_requests_action_check", sql`${table.action} = 'publish'`),
  check("compliance_governance_requests_snapshot_check", sql`json_valid(${table.sealedSnapshot}) AND length(${table.sealedSnapshotSha256}) = 64 AND lower(${table.sealedSnapshotSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_governance_requests_status_check", sql`${table.status} IN ('pending', 'approved', 'rejected', 'superseded')`),
  check("compliance_governance_requests_reason_check", sql`trim(${table.requestReason}) <> '' AND trim(${table.requestedByUid}) <> ''`),
  check("compliance_governance_requests_review_check", sql`(${table.status} = 'pending' AND ${table.reviewedByUid} = '' AND ${table.reviewedAt} = '' AND ${table.reviewNote} = '') OR (${table.status} IN ('approved', 'rejected') AND trim(${table.reviewedByUid}) <> '' AND ${table.reviewedByUid} <> ${table.requestedByUid} AND trim(${table.reviewedAt}) <> '' AND trim(${table.reviewNote}) <> '') OR (${table.status} = 'superseded' AND ${table.reviewedByUid} = '' AND trim(${table.reviewedAt}) <> '' AND trim(${table.reviewNote}) <> '')`),
]);

export const complianceEvidencePolicyVersions = sqliteTable("compliance_evidence_policy_versions", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  activityVersionId: text("activity_version_id").notNull(),
  version: integer("version").notNull(),
  title: text("title").notNull(),
  officialSourceUrl: text("official_source_url").notNull(),
  officialSourceTitle: text("official_source_title").notNull(),
  officialSourceVersion: text("official_source_version").notNull(),
  officialSourceSha256: text("official_source_sha256").notNull(),
  officialSourceCheckedAt: text("official_source_checked_at").notNull(),
  requirementsComplete: integer("requirements_complete", { mode: "boolean" }).notNull().default(false),
  publishState: text("publish_state").notNull().default("draft"),
  publicationRequestId: text("publication_request_id").notNull().default(""),
  publicationSnapshotSha256: text("publication_snapshot_sha256").notNull().default(""),
  contentRevision: integer("content_revision").notNull().default(1),
  publishedByUid: text("published_by_uid").notNull().default(""),
  publishedAt: text("published_at").notNull().default(""),
  withdrawnByUid: text("withdrawn_by_uid").notNull().default(""),
  withdrawnAt: text("withdrawn_at").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_evidence_policies_activity_version_idx").on(table.activityVersionId, table.version),
  index("compliance_evidence_policies_org_state_idx").on(table.organisationId, table.publishState, table.updatedAt),
  check("compliance_evidence_policies_version_check", sql`${table.version} > 0`),
  check("compliance_evidence_policies_content_revision_check", sql`${table.contentRevision} > 0`),
  check("compliance_evidence_policies_state_check", sql`${table.publishState} IN ('draft', 'published', 'withdrawn')`),
  check("compliance_evidence_policies_source_hash_check", sql`length(${table.officialSourceSha256}) = 64 AND lower(${table.officialSourceSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_evidence_policies_publication_hash_check", sql`${table.publicationSnapshotSha256} = '' OR (length(${table.publicationSnapshotSha256}) = 64 AND lower(${table.publicationSnapshotSha256}) NOT GLOB '*[^0-9a-f]*')`),
  check("compliance_evidence_policies_complete_check", sql`${table.requirementsComplete} IN (0, 1)`),
  check("compliance_evidence_policies_provenance_check", sql`(${table.publishState} = 'draft' AND ${table.publishedByUid} = '' AND ${table.publishedAt} = '' AND ${table.withdrawnByUid} = '' AND ${table.withdrawnAt} = '') OR (${table.publishState} = 'published' AND ${table.publishedByUid} <> '' AND ${table.publishedAt} <> '' AND ${table.withdrawnByUid} = '' AND ${table.withdrawnAt} = '') OR (${table.publishState} = 'withdrawn' AND ${table.publishedByUid} <> '' AND ${table.publishedAt} <> '' AND ${table.withdrawnByUid} <> '' AND ${table.withdrawnAt} <> '')`),
]);

export const complianceEvidenceRequirements = sqliteTable("compliance_evidence_requirements", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  policyVersionId: text("policy_version_id").notNull(),
  requirementCode: text("requirement_code").notNull(),
  title: text("title").notNull(),
  description: text("description").notNull().default(""),
  evidenceType: text("evidence_type").notNull(),
  captureTiming: text("capture_timing").notNull(),
  minimumCount: integer("minimum_count").notNull().default(1),
  maximumCount: integer("maximum_count").notNull().default(1),
  originalRequired: integer("original_required", { mode: "boolean" }).notNull().default(false),
  metadataRequired: integer("metadata_required", { mode: "boolean" }).notNull().default(false),
  gpsRequired: integer("gps_required", { mode: "boolean" }).notNull().default(false),
  dateStampRequired: integer("date_stamp_required", { mode: "boolean" }).notNull().default(false),
  installerSignatureRequired: integer("installer_signature_required", { mode: "boolean" }).notNull().default(false),
  customerSignatureRequired: integer("customer_signature_required", { mode: "boolean" }).notNull().default(false),
  allowedContentTypes: text("allowed_content_types").notNull().default("[]"),
  conditionSnapshot: text("condition_snapshot").notNull().default("{}"),
  fieldSchema: text("field_schema").notNull().default("{}"),
  sourceCitation: text("source_citation").notNull(),
  sortOrder: integer("sort_order").notNull().default(0),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_evidence_requirements_policy_code_idx").on(table.policyVersionId, table.requirementCode),
  index("compliance_evidence_requirements_policy_order_idx").on(table.policyVersionId, table.sortOrder, table.requirementCode),
  check("compliance_evidence_requirements_types_check", sql`${table.evidenceType} IN ('photo', 'document', 'declaration', 'signature', 'licence', 'invoice', 'payment', 'product', 'serial', 'decommission', 'location', 'other')`),
  check("compliance_evidence_requirements_timing_check", sql`${table.captureTiming} IN ('pre_install', 'during_install', 'post_install', 'any', 'periodic')`),
  check("compliance_evidence_requirements_json_check", sql`json_valid(${table.allowedContentTypes}) AND json_valid(${table.conditionSnapshot}) AND json_valid(${table.fieldSchema})`),
  check("compliance_evidence_requirements_cardinality_check", sql`${table.minimumCount} >= 0 AND (${table.maximumCount} = 0 OR ${table.maximumCount} >= ${table.minimumCount})`),
  check("compliance_evidence_requirements_flags_check", sql`${table.originalRequired} IN (0, 1) AND ${table.metadataRequired} IN (0, 1) AND ${table.gpsRequired} IN (0, 1) AND ${table.dateStampRequired} IN (0, 1) AND ${table.installerSignatureRequired} IN (0, 1) AND ${table.customerSignatureRequired} IN (0, 1)`),
]);

export const complianceParticipants = sqliteTable("compliance_participants", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  participantType: text("participant_type").notNull(),
  externalReference: text("external_reference").notNull().default(""),
  legalName: text("legal_name").notNull(),
  tradingName: text("trading_name").notNull().default(""),
  abn: text("abn").notNull().default(""),
  contactEmail: text("contact_email").notNull().default(""),
  status: text("status").notNull().default("pending"),
  effectiveFrom: text("effective_from").notNull().default(""),
  effectiveTo: text("effective_to").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("compliance_participants_org_status_idx").on(table.organisationId, table.participantType, table.status, table.tradingName),
  uniqueIndex("compliance_participants_external_idx").on(table.organisationId, table.externalReference).where(sql`${table.externalReference} <> ''`),
  check("compliance_participants_type_check", sql`${table.participantType} IN ('installer', 'retailer', 'aggregator', 'auditor', 'supplier', 'agent')`),
  check("compliance_participants_status_check", sql`${table.status} IN ('pending', 'active', 'suspended', 'expired', 'retired')`),
]);

export const complianceParticipantAbilities = sqliteTable("compliance_participant_abilities", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  participantId: text("participant_id").notNull(),
  programId: text("program_id").notNull().default(""),
  activityVersionId: text("activity_version_id").notNull().default(""),
  abilityCode: text("ability_code").notNull(),
  abilityRole: text("ability_role").notNull(),
  status: text("status").notNull().default("pending"),
  effectiveFrom: text("effective_from").notNull(),
  effectiveTo: text("effective_to").notNull().default(""),
  evidenceSnapshot: text("evidence_snapshot").notNull().default("{}"),
  approvedByUid: text("approved_by_uid").notNull().default(""),
  approvedAt: text("approved_at").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_participant_abilities_key_idx").on(table.participantId, table.abilityCode, table.effectiveFrom),
  index("compliance_participant_abilities_expiry_idx").on(table.organisationId, table.status, table.effectiveTo, table.participantId),
  check("compliance_participant_abilities_status_check", sql`${table.status} IN ('pending', 'active', 'suspended', 'expired', 'revoked')`),
  check("compliance_participant_abilities_evidence_check", sql`json_valid(${table.evidenceSnapshot})`),
]);

export const complianceCaseAssignments = sqliteTable("compliance_case_assignments", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  caseId: text("case_id").notNull(),
  complianceUserId: text("compliance_user_id").notNull(),
  assignmentRole: text("assignment_role").notNull(),
  status: text("status").notNull().default("assigned"),
  assignedByUid: text("assigned_by_uid").notNull(),
  assignedAt: text("assigned_at").notNull(),
  releasedAt: text("released_at").notNull().default(""),
  completedAt: text("completed_at").notNull().default(""),
}, (table) => [
  uniqueIndex("compliance_case_assignments_active_role_idx").on(table.caseId, table.complianceUserId, table.assignmentRole),
  index("compliance_case_assignments_user_status_idx").on(table.organisationId, table.complianceUserId, table.status, table.assignedAt),
  check("compliance_case_assignments_role_check", sql`${table.assignmentRole} IN ('case_manager', 'primary_reviewer', 'secondary_reviewer', 'auditor')`),
  check("compliance_case_assignments_status_check", sql`${table.status} IN ('assigned', 'released', 'completed')`),
]);

export const complianceCaseTasks = sqliteTable("compliance_case_tasks", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  caseId: text("case_id").notNull(),
  taskType: text("task_type").notNull(),
  title: text("title").notNull(),
  detail: text("detail").notNull().default(""),
  priority: text("priority").notNull().default("normal"),
  status: text("status").notNull().default("open"),
  assigneeUserId: text("assignee_user_id").notNull().default(""),
  dueAt: text("due_at").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  completedByUid: text("completed_by_uid").notNull().default(""),
  completedAt: text("completed_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("compliance_case_tasks_queue_idx").on(table.organisationId, table.status, table.priority, table.dueAt, table.createdAt),
  index("compliance_case_tasks_case_idx").on(table.caseId, table.status, table.createdAt),
  check("compliance_case_tasks_type_check", sql`${table.taskType} IN ('evidence', 'review', 'correction', 'submission', 'reconciliation', 'participant', 'general')`),
  check("compliance_case_tasks_priority_check", sql`${table.priority} IN ('low', 'normal', 'high', 'urgent')`),
  check("compliance_case_tasks_status_check", sql`${table.status} IN ('open', 'in_progress', 'blocked', 'completed', 'cancelled')`),
]);

export const complianceCaseEvidence = sqliteTable("compliance_case_evidence", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  caseId: text("case_id").notNull(),
  requirementId: text("requirement_id").notNull(),
  jobMediaId: text("job_media_id").notNull().default(""),
  supersedesEvidenceId: text("supersedes_evidence_id").notNull().default(""),
  sourceType: text("source_type").notNull(),
  status: text("status").notNull().default("received"),
  objectKey: text("object_key").notNull(),
  fileName: text("file_name").notNull(),
  contentType: text("content_type").notNull(),
  sizeBytes: integer("size_bytes").notNull(),
  originalSha256: text("original_sha256").notNull(),
  evidenceEnvelope: text("evidence_envelope").notNull(),
  receivedByType: text("received_by_type").notNull(),
  receivedByUid: text("received_by_uid").notNull(),
  receivedAt: text("received_at").notNull(),
  reviewedByUid: text("reviewed_by_uid").notNull().default(""),
  reviewedAt: text("reviewed_at").notNull().default(""),
  retentionUntil: text("retention_until").notNull().default(""),
  legalHold: integer("legal_hold", { mode: "boolean" }).notNull().default(false),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_case_evidence_job_media_idx").on(table.jobMediaId).where(sql`${table.jobMediaId} <> ''`),
  uniqueIndex("compliance_case_evidence_active_original_idx")
    .on(table.organisationId, table.caseId, table.requirementId, table.originalSha256)
    .where(sql`${table.originalSha256} <> '' AND ${table.status} IN ('received', 'under_review', 'accepted')`),
  index("compliance_case_evidence_case_requirement_idx").on(table.caseId, table.requirementId, table.status, table.receivedAt),
  index("compliance_case_evidence_review_idx").on(table.organisationId, table.status, table.receivedAt),
  check("compliance_case_evidence_source_check", sql`${table.sourceType} IN ('field_app', 'installer_upload', 'customer_upload', 'import', 'registry')`),
  check("compliance_case_evidence_status_check", sql`${table.status} IN ('received', 'under_review', 'accepted', 'rejected', 'superseded', 'withdrawn')`),
  check("compliance_case_evidence_envelope_check", sql`json_valid(${table.evidenceEnvelope})`),
  check("compliance_case_evidence_original_hash_check", sql`length(${table.originalSha256}) = 64 AND lower(${table.originalSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_case_evidence_size_check", sql`${table.sizeBytes} > 0`),
]);

export const complianceCaseFindings = sqliteTable("compliance_case_findings", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  caseId: text("case_id").notNull(),
  evidenceId: text("evidence_id").notNull().default(""),
  requirementId: text("requirement_id").notNull().default(""),
  findingCode: text("finding_code").notNull(),
  severity: text("severity").notNull(),
  description: text("description").notNull(),
  status: text("status").notNull().default("open"),
  raisedByUid: text("raised_by_uid").notNull(),
  raisedAt: text("raised_at").notNull(),
  resolvedByUid: text("resolved_by_uid").notNull().default(""),
  resolvedAt: text("resolved_at").notNull().default(""),
  resolutionNote: text("resolution_note").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("compliance_case_findings_case_status_idx").on(table.caseId, table.status, table.severity, table.raisedAt),
  index("compliance_case_findings_org_status_idx").on(table.organisationId, table.status, table.severity, table.raisedAt),
  check("compliance_case_findings_severity_check", sql`${table.severity} IN ('information', 'minor', 'major', 'critical')`),
  check("compliance_case_findings_status_check", sql`${table.status} IN ('open', 'resolved', 'waived')`),
]);

export const complianceCaseDecisions = sqliteTable("compliance_case_decisions", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  caseId: text("case_id").notNull(),
  caseRevision: integer("case_revision").notNull(),
  decisionType: text("decision_type").notNull(),
  outcome: text("outcome").notNull(),
  basisSnapshot: text("basis_snapshot").notNull(),
  primaryReviewerUid: text("primary_reviewer_uid").notNull(),
  secondaryReviewerUid: text("secondary_reviewer_uid").notNull().default(""),
  decidedAt: text("decided_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("compliance_case_decisions_case_time_idx").on(table.caseId, table.decidedAt, table.id),
  index("compliance_case_decisions_case_revision_idx").on(table.caseId, table.caseRevision, table.decisionType, table.decidedAt, table.id),
  check("compliance_case_decisions_revision_check", sql`${table.caseRevision} > 0`),
  check("compliance_case_decisions_type_check", sql`${table.decisionType} IN ('evidence_complete', 'eligibility', 'ready_to_submit', 'submission_outcome', 'case_closure')`),
  check("compliance_case_decisions_outcome_check", sql`${table.outcome} IN ('approved', 'rejected', 'changes_required', 'withdrawn')`),
  check("compliance_case_decisions_basis_check", sql`json_valid(${table.basisSnapshot})`),
  check("compliance_case_decisions_dual_control_check", sql`${table.outcome} <> 'approved' OR ${table.decisionType} NOT IN ('eligibility', 'ready_to_submit') OR (${table.secondaryReviewerUid} <> '' AND ${table.secondaryReviewerUid} <> ${table.primaryReviewerUid})`),
]);

export const complianceDecisionRequests = sqliteTable("compliance_decision_requests", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  caseId: text("case_id").notNull(),
  caseRevision: integer("case_revision").notNull(),
  decisionType: text("decision_type").notNull(),
  outcome: text("outcome").notNull(),
  basisSnapshot: text("basis_snapshot").notNull(),
  status: text("status").notNull().default("pending"),
  primaryReviewerUid: text("primary_reviewer_uid").notNull(),
  secondaryReviewerUid: text("secondary_reviewer_uid").notNull().default(""),
  reviewedAt: text("reviewed_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("compliance_decision_requests_case_status_idx").on(table.caseId, table.caseRevision, table.status, table.createdAt, table.id),
  index("compliance_decision_requests_org_status_idx").on(table.organisationId, table.status, table.createdAt, table.id),
  check("compliance_decision_requests_revision_check", sql`${table.caseRevision} > 0`),
  check("compliance_decision_requests_type_check", sql`${table.decisionType} IN ('eligibility', 'ready_to_submit')`),
  check("compliance_decision_requests_outcome_check", sql`${table.outcome} = 'approved'`),
  check("compliance_decision_requests_basis_check", sql`json_valid(${table.basisSnapshot})`),
  check("compliance_decision_requests_state_check", sql`${table.status} IN ('pending', 'approved', 'rejected', 'withdrawn')`),
  check("compliance_decision_requests_reviewer_check", sql`(${table.status} = 'pending' AND ${table.secondaryReviewerUid} = '' AND ${table.reviewedAt} = '') OR (${table.status} <> 'pending' AND ${table.secondaryReviewerUid} <> '' AND ${table.secondaryReviewerUid} <> ${table.primaryReviewerUid} AND ${table.reviewedAt} <> '')`),
]);

export const complianceEquipmentRecords = sqliteTable("compliance_equipment_records", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  caseId: text("case_id").notNull(),
  recordType: text("record_type").notNull(),
  manufacturer: text("manufacturer").notNull().default(""),
  model: text("model").notNull().default(""),
  serialNumber: text("serial_number").notNull().default(""),
  productRegistry: text("product_registry").notNull().default(""),
  productReference: text("product_reference").notNull().default(""),
  quantity: integer("quantity").notNull().default(1),
  status: text("status").notNull(),
  evidenceSnapshot: text("evidence_snapshot").notNull().default("{}"),
  recordedByUid: text("recorded_by_uid").notNull(),
  recordedAt: text("recorded_at").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("compliance_equipment_case_type_idx").on(table.caseId, table.recordType, table.status, table.recordedAt),
  index("compliance_equipment_serial_idx").on(table.organisationId, table.serialNumber, table.status),
  check("compliance_equipment_type_check", sql`${table.recordType} IN ('installed', 'decommissioned', 'stock')`),
  check("compliance_equipment_status_check", sql`${table.status} IN ('expected', 'received', 'installed', 'decommissioned', 'removed', 'returned', 'scrapped')`),
  check("compliance_equipment_quantity_check", sql`${table.quantity} > 0`),
  check("compliance_equipment_evidence_check", sql`json_valid(${table.evidenceSnapshot})`),
]);

export const complianceCalculatorVersions = sqliteTable("compliance_calculator_versions", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  activityVersionId: text("activity_version_id").notNull(),
  calculatorKey: text("calculator_key").notNull(),
  version: integer("version").notNull(),
  title: text("title").notNull(),
  outputType: text("output_type").notNull(),
  specification: text("specification").notNull(),
  roundingPolicy: text("rounding_policy").notNull(),
  officialSourceUrl: text("official_source_url").notNull(),
  officialSourceVersion: text("official_source_version").notNull(),
  officialSourceSha256: text("official_source_sha256").notNull(),
  approvalState: text("approval_state").notNull().default("draft"),
  primaryApproverUid: text("primary_approver_uid").notNull().default(""),
  secondaryApproverUid: text("secondary_approver_uid").notNull().default(""),
  approvedAt: text("approved_at").notNull().default(""),
  withdrawnAt: text("withdrawn_at").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_calculator_versions_key_idx").on(table.activityVersionId, table.calculatorKey, table.version),
  index("compliance_calculator_versions_state_idx").on(table.organisationId, table.approvalState, table.updatedAt),
  check("compliance_calculator_versions_version_check", sql`${table.version} > 0`),
  check("compliance_calculator_versions_state_check", sql`${table.approvalState} IN ('draft', 'testing', 'approved', 'blocked', 'withdrawn')`),
  check("compliance_calculator_versions_specification_check", sql`json_valid(${table.specification})`),
  check("compliance_calculator_versions_source_hash_check", sql`length(${table.officialSourceSha256}) = 64 AND lower(${table.officialSourceSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_calculator_versions_dual_control_check", sql`${table.approvalState} <> 'approved' OR (${table.primaryApproverUid} <> '' AND ${table.secondaryApproverUid} <> '' AND ${table.primaryApproverUid} <> ${table.secondaryApproverUid} AND ${table.approvedAt} <> '')`),
]);

export const complianceCalculatorTestVectors = sqliteTable("compliance_calculator_test_vectors", {
  id: text("id").primaryKey(),
  calculatorVersionId: text("calculator_version_id").notNull(),
  vectorKey: text("vector_key").notNull(),
  inputSnapshot: text("input_snapshot").notNull(),
  expectedOutput: text("expected_output").notNull(),
  toleranceSnapshot: text("tolerance_snapshot").notNull().default("{}"),
  sourceCitation: text("source_citation").notNull(),
  lastResult: text("last_result").notNull().default("not_run"),
  lastRunAt: text("last_run_at").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_calculator_vectors_key_idx").on(table.calculatorVersionId, table.vectorKey),
  check("compliance_calculator_vectors_json_check", sql`json_valid(${table.inputSnapshot}) AND json_valid(${table.expectedOutput}) AND json_valid(${table.toleranceSnapshot})`),
  check("compliance_calculator_vectors_result_check", sql`${table.lastResult} IN ('not_run', 'passed', 'failed')`),
]);

export const complianceCalculatorAuthoringReceipts = sqliteTable("compliance_calculator_authoring_receipts", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  clientRequestId: text("client_request_id").notNull(),
  requestSha256: text("request_sha256").notNull(),
  calculatorVersionId: text("calculator_version_id").notNull(),
  activityVersionId: text("activity_version_id").notNull(),
  sourceArtifactId: text("source_artifact_id").notNull(),
  activitySourceBindingId: text("activity_source_binding_id").notNull(),
  calculatorSourceBindingId: text("calculator_source_binding_id").notNull(),
  sourceArtifactSha256: text("source_artifact_sha256").notNull(),
  specificationSha256: text("specification_sha256").notNull(),
  engineContractHash: text("engine_contract_hash").notNull(),
  authoringContractSha256: text("authoring_contract_sha256").notNull(),
  authoringState: text("authoring_state").notNull().default("pending_review"),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_calculator_authoring_org_request_idx")
    .on(table.organisationId, table.clientRequestId),
  uniqueIndex("compliance_calculator_authoring_version_idx")
    .on(table.calculatorVersionId),
  index("compliance_calculator_authoring_org_state_idx")
    .on(table.organisationId, table.authoringState, table.createdAt),
  check("compliance_calculator_authoring_state_check", sql`${table.authoringState} = 'pending_review'`),
  check("compliance_calculator_authoring_hashes_check", sql`length(${table.requestSha256}) = 64 AND lower(${table.requestSha256}) NOT GLOB '*[^0-9a-f]*' AND length(${table.sourceArtifactSha256}) = 64 AND lower(${table.sourceArtifactSha256}) NOT GLOB '*[^0-9a-f]*' AND length(${table.specificationSha256}) = 64 AND lower(${table.specificationSha256}) NOT GLOB '*[^0-9a-f]*' AND ${table.engineContractHash} LIKE 'sha256:%' AND length(${table.engineContractHash}) = 71 AND lower(substr(${table.engineContractHash}, 8)) NOT GLOB '*[^0-9a-f]*' AND length(${table.authoringContractSha256}) = 64 AND lower(${table.authoringContractSha256}) NOT GLOB '*[^0-9a-f]*'`),
]);

export const complianceCalculatorVectorAuthoringReceipts = sqliteTable("compliance_calculator_vector_authoring_receipts", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  clientRequestId: text("client_request_id").notNull(),
  requestSha256: text("request_sha256").notNull(),
  vectorId: text("vector_id").notNull(),
  calculatorVersionId: text("calculator_version_id").notNull(),
  sourceArtifactId: text("source_artifact_id").notNull(),
  activitySourceBindingId: text("activity_source_binding_id").notNull(),
  sourceArtifactSha256: text("source_artifact_sha256").notNull(),
  inputSha256: text("input_sha256").notNull(),
  expectedOutputSha256: text("expected_output_sha256").notNull(),
  sourceCitationSha256: text("source_citation_sha256").notNull(),
  vectorContractSha256: text("vector_contract_sha256").notNull(),
  authoringState: text("authoring_state").notNull().default("pending_review"),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_calculator_vector_authoring_org_request_idx")
    .on(table.organisationId, table.clientRequestId),
  uniqueIndex("compliance_calculator_vector_authoring_vector_idx")
    .on(table.vectorId),
  index("compliance_calculator_vector_authoring_parent_idx")
    .on(table.organisationId, table.calculatorVersionId, table.createdAt),
  check("compliance_calculator_vector_authoring_state_check", sql`${table.authoringState} = 'pending_review'`),
  check("compliance_calculator_vector_authoring_hashes_check", sql`length(${table.requestSha256}) = 64 AND lower(${table.requestSha256}) NOT GLOB '*[^0-9a-f]*' AND length(${table.sourceArtifactSha256}) = 64 AND lower(${table.sourceArtifactSha256}) NOT GLOB '*[^0-9a-f]*' AND length(${table.inputSha256}) = 64 AND lower(${table.inputSha256}) NOT GLOB '*[^0-9a-f]*' AND length(${table.expectedOutputSha256}) = 64 AND lower(${table.expectedOutputSha256}) NOT GLOB '*[^0-9a-f]*' AND length(${table.sourceCitationSha256}) = 64 AND lower(${table.sourceCitationSha256}) NOT GLOB '*[^0-9a-f]*' AND length(${table.vectorContractSha256}) = 64 AND lower(${table.vectorContractSha256}) NOT GLOB '*[^0-9a-f]*'`),
]);

export const complianceCalculationRuns = sqliteTable("compliance_calculation_runs", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  caseId: text("case_id").notNull(),
  caseRevision: integer("case_revision").notNull(),
  calculatorVersionId: text("calculator_version_id").notNull(),
  inputSnapshot: text("input_snapshot").notNull(),
  outputSnapshot: text("output_snapshot").notNull().default("{}"),
  status: text("status").notNull().default("blocked"),
  blockedReason: text("blocked_reason").notNull().default(""),
  runByUid: text("run_by_uid").notNull(),
  runAt: text("run_at").notNull(),
  verifiedByUid: text("verified_by_uid").notNull().default(""),
  verifiedAt: text("verified_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("compliance_calculation_runs_case_idx").on(table.caseId, table.caseRevision, table.runAt),
  check("compliance_calculation_runs_revision_check", sql`${table.caseRevision} > 0`),
  check("compliance_calculation_runs_status_check", sql`${table.status} IN ('blocked', 'calculated', 'verified', 'rejected')`),
  check("compliance_calculation_runs_json_check", sql`json_valid(${table.inputSnapshot}) AND json_valid(${table.outputSnapshot})`),
]);

export const complianceSubmissionBatches = sqliteTable("compliance_submission_batches", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  programId: text("program_id").notNull(),
  batchNumber: text("batch_number").notNull(),
  externalReference: text("external_reference").notNull().default(""),
  format: text("format").notNull(),
  status: text("status").notNull().default("draft"),
  payloadSha256: text("payload_sha256").notNull().default(""),
  caseCount: integer("case_count").notNull().default(0),
  certificateQuantity: integer("certificate_quantity").notNull().default(0),
  createdByUid: text("created_by_uid").notNull(),
  exportedAt: text("exported_at").notNull().default(""),
  submittedAt: text("submitted_at").notNull().default(""),
  reconciledAt: text("reconciled_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_submission_batches_number_idx").on(table.organisationId, table.batchNumber),
  index("compliance_submission_batches_queue_idx").on(table.organisationId, table.status, table.createdAt),
  check("compliance_submission_batches_format_check", sql`${table.format} IN ('json', 'csv', 'manual', 'api')`),
  check("compliance_submission_batches_status_check", sql`${table.status} IN ('draft', 'ready', 'exported', 'submitted', 'partially_accepted', 'accepted', 'rejected', 'reconciled', 'cancelled')`),
  check("compliance_submission_batches_payload_hash_check", sql`${table.payloadSha256} = '' OR (length(${table.payloadSha256}) = 64 AND lower(${table.payloadSha256}) NOT GLOB '*[^0-9a-f]*')`),
  check("compliance_submission_batches_counts_check", sql`${table.caseCount} >= 0 AND ${table.certificateQuantity} >= 0`),
]);

export const complianceSubmissionBatchItems = sqliteTable("compliance_submission_batch_items", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  batchId: text("batch_id").notNull(),
  caseId: text("case_id").notNull(),
  caseRevision: integer("case_revision").notNull(),
  status: text("status").notNull().default("staged"),
  externalReference: text("external_reference").notNull().default(""),
  resultSnapshot: text("result_snapshot").notNull().default("{}"),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_submission_batch_items_case_idx").on(table.batchId, table.caseId),
  index("compliance_submission_batch_items_status_idx").on(table.batchId, table.status, table.createdAt),
  check("compliance_submission_batch_items_revision_check", sql`${table.caseRevision} > 0`),
  check("compliance_submission_batch_items_status_check", sql`${table.status} IN ('staged', 'submitted', 'accepted', 'rejected', 'correction_required', 'removed')`),
  check("compliance_submission_batch_items_result_check", sql`json_valid(${table.resultSnapshot})`),
]);

export const complianceSubmissionArtifacts = sqliteTable("compliance_submission_artifacts", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  batchId: text("batch_id").notNull(),
  artifactType: text("artifact_type").notNull(),
  objectKey: text("object_key").notNull(),
  fileName: text("file_name").notNull(),
  contentType: text("content_type").notNull(),
  sizeBytes: integer("size_bytes").notNull(),
  sha256: text("sha256").notNull(),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("compliance_submission_artifacts_batch_idx").on(table.batchId, table.createdAt),
  check("compliance_submission_artifacts_type_check", sql`${table.artifactType} IN ('export_json', 'export_csv', 'submission_receipt', 'response_file', 'reconciliation_report')`),
  check("compliance_submission_artifacts_hash_check", sql`${table.sizeBytes} > 0 AND length(${table.sha256}) = 64 AND lower(${table.sha256}) NOT GLOB '*[^0-9a-f]*'`),
]);

export const complianceSubmissionResponses = sqliteTable("compliance_submission_responses", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  batchId: text("batch_id").notNull(),
  batchItemId: text("batch_item_id").notNull().default(""),
  responseType: text("response_type").notNull(),
  responseCode: text("response_code").notNull().default(""),
  message: text("message").notNull(),
  payloadSnapshot: text("payload_snapshot").notNull().default("{}"),
  occurredAt: text("occurred_at").notNull(),
  recordedByUid: text("recorded_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("compliance_submission_responses_batch_idx").on(table.batchId, table.occurredAt, table.id),
  check("compliance_submission_responses_type_check", sql`${table.responseType} IN ('accepted', 'rejected', 'warning', 'error', 'duplicate')`),
  check("compliance_submission_responses_payload_check", sql`json_valid(${table.payloadSnapshot})`),
]);

export const complianceCertificateLots = sqliteTable("compliance_certificate_lots", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  programId: text("program_id").notNull(),
  batchId: text("batch_id").notNull().default(""),
  certificateType: text("certificate_type").notNull(),
  registryLotReference: text("registry_lot_reference").notNull().default(""),
  quantity: integer("quantity").notNull(),
  status: text("status").notNull().default("pending"),
  vintageFrom: text("vintage_from").notNull().default(""),
  vintageTo: text("vintage_to").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("compliance_certificate_lots_inventory_idx").on(table.organisationId, table.certificateType, table.status, table.createdAt),
  check("compliance_certificate_lots_quantity_check", sql`${table.quantity} >= 0`),
  check("compliance_certificate_lots_status_check", sql`${table.status} IN ('pending', 'created', 'available', 'reserved', 'traded', 'retired', 'cancelled')`),
]);

export const complianceTrades = sqliteTable("compliance_trades", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  certificateLotId: text("certificate_lot_id").notNull(),
  counterpartyReference: text("counterparty_reference").notNull(),
  quantity: integer("quantity").notNull(),
  unitPriceCents: integer("unit_price_cents").notNull(),
  tradeDate: text("trade_date").notNull(),
  status: text("status").notNull().default("pending"),
  externalReference: text("external_reference").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("compliance_trades_status_idx").on(table.organisationId, table.status, table.tradeDate),
  check("compliance_trades_quantity_price_check", sql`${table.quantity} > 0 AND ${table.unitPriceCents} >= 0`),
  check("compliance_trades_status_check", sql`${table.status} IN ('pending', 'confirmed', 'settled', 'cancelled')`),
]);

export const complianceSettlements = sqliteTable("compliance_settlements", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  tradeId: text("trade_id").notNull(),
  grossCents: integer("gross_cents").notNull(),
  feeCents: integer("fee_cents").notNull().default(0),
  netCents: integer("net_cents").notNull(),
  dueDate: text("due_date").notNull(),
  settledAt: text("settled_at").notNull().default(""),
  status: text("status").notNull().default("pending"),
  externalReference: text("external_reference").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("compliance_settlements_status_idx").on(table.organisationId, table.status, table.dueDate),
  uniqueIndex("compliance_settlements_active_trade_idx")
    .on(table.tradeId)
    .where(sql`${table.status} <> 'cancelled'`),
  check("compliance_settlements_status_check", sql`${table.status} IN ('pending', 'processing', 'settled', 'failed', 'cancelled')`),
  check("compliance_settlements_money_check", sql`${table.grossCents} >= 0 AND ${table.feeCents} >= 0 AND ${table.netCents} >= 0`),
  check("compliance_settlements_totals_check", sql`${table.netCents} = ${table.grossCents} - ${table.feeCents}`),
]);

export const compliancePilotRuns = sqliteTable("compliance_pilot_runs", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  programCode: text("program_code").notNull(),
  name: text("name").notNull(),
  seedVersion: text("seed_version").notNull(),
  recordMode: text("record_mode").notNull().default("synthetic_test"),
  status: text("status").notNull().default("provisioning"),
  installerTarget: integer("installer_target").notNull().default(10),
  techniciansPerInstaller: integer("technicians_per_installer").notNull().default(3),
  jobsPerTechnician: integer("jobs_per_technician").notNull().default(10),
  activityCatalogueSha256: text("activity_catalogue_sha256").notNull(),
  sourceManifestSha256: text("source_manifest_sha256").notNull(),
  ruleImportStatus: text("rule_import_status").notNull().default("captured_pending_independent_review"),
  lookupStatus: text("lookup_status").notNull().default("contracts_ready_live_sources_blocked"),
  evidenceStatus: text("evidence_status").notNull().default("transport_contract_ready_physical_acceptance_blocked"),
  calculatorStatus: text("calculator_status").notNull().default("typed_contract_ready_formula_blocked"),
  connectorStatus: text("connector_status").notNull().default("dry_run_only"),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  activatedAt: text("activated_at").notNull().default(""),
  archivedAt: text("archived_at").notNull().default(""),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_pilot_runs_org_seed_idx").on(table.organisationId, table.programCode, table.seedVersion),
  index("compliance_pilot_runs_org_status_idx").on(table.organisationId, table.status, table.updatedAt),
  check("compliance_pilot_runs_record_mode_check", sql`${table.recordMode} = 'synthetic_test'`),
  check("compliance_pilot_runs_status_check", sql`${table.status} IN ('provisioning', 'active', 'archived')`),
  check("compliance_pilot_runs_shape_check", sql`${table.installerTarget} = 10 AND ${table.techniciansPerInstaller} = 3 AND ${table.jobsPerTechnician} = 10`),
  check("compliance_pilot_runs_hash_check", sql`length(${table.activityCatalogueSha256}) = 64 AND lower(${table.activityCatalogueSha256}) NOT GLOB '*[^0-9a-f]*' AND length(${table.sourceManifestSha256}) = 64 AND lower(${table.sourceManifestSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_pilot_runs_lifecycle_check", sql`(${table.status} = 'provisioning' AND ${table.activatedAt} = '' AND ${table.archivedAt} = '') OR (${table.status} = 'active' AND trim(${table.activatedAt}) <> '' AND ${table.archivedAt} = '') OR (${table.status} = 'archived' AND trim(${table.archivedAt}) <> '')`),
]);

export const compliancePilotSourceInstruments = sqliteTable("compliance_pilot_source_instruments", {
  id: text("id").primaryKey(),
  pilotRunId: text("pilot_run_id").notNull(),
  sourceKey: text("source_key").notNull(),
  sourceKind: text("source_kind").notNull(),
  title: text("title").notNull(),
  officialSourceUrl: text("official_source_url").notNull(),
  officialVersion: text("official_version").notNull().default(""),
  effectiveFrom: text("effective_from").notNull().default(""),
  effectiveTo: text("effective_to").notNull().default(""),
  officialSourceSha256: text("official_source_sha256").notNull().default(""),
  hashStatus: text("hash_status").notNull(),
  verificationStatus: text("verification_status").notNull().default("pending_independent_review"),
  sourcePriority: integer("source_priority").notNull(),
  capturedAt: text("captured_at").notNull(),
  verifiedByUid: text("verified_by_uid").notNull().default(""),
  verifiedAt: text("verified_at").notNull().default(""),
  verificationNote: text("verification_note").notNull().default(""),
}, (table) => [
  uniqueIndex("compliance_pilot_source_key_idx").on(table.pilotRunId, table.sourceKey),
  index("compliance_pilot_source_status_idx").on(table.pilotRunId, table.verificationStatus, table.sourcePriority),
]);

export const compliancePilotControlOptions = sqliteTable("compliance_pilot_control_options", {
  id: text("id").primaryKey(),
  pilotRunId: text("pilot_run_id").notNull(),
  controlType: text("control_type").notNull(),
  optionCode: text("option_code").notNull(),
  label: text("label").notNull(),
  optionOrder: integer("option_order").notNull(),
  effectiveFrom: text("effective_from").notNull(),
  effectiveTo: text("effective_to").notNull().default(""),
  sourceKey: text("source_key").notNull(),
  liveLookupEnabled: integer("live_lookup_enabled", { mode: "boolean" }).notNull().default(false),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_pilot_control_option_idx").on(table.pilotRunId, table.controlType, table.optionCode),
  index("compliance_pilot_control_type_idx").on(table.pilotRunId, table.controlType, table.optionOrder),
]);

export const compliancePilotEvidenceContracts = sqliteTable("compliance_pilot_evidence_contracts", {
  id: text("id").primaryKey(),
  pilotRunId: text("pilot_run_id").notNull(),
  requirementCode: text("requirement_code").notNull(),
  title: text("title").notNull(),
  evidenceKind: text("evidence_kind").notNull(),
  captureTiming: text("capture_timing").notNull(),
  originalRequired: integer("original_required", { mode: "boolean" }).notNull().default(true),
  metadataRequired: integer("metadata_required", { mode: "boolean" }).notNull().default(true),
  gpsRequired: integer("gps_required", { mode: "boolean" }).notNull().default(false),
  minimumCount: integer("minimum_count").notNull().default(1),
  maximumCount: integer("maximum_count").notNull().default(1),
  allowedContentTypes: text("allowed_content_types").notNull().default("[]"),
  contractScope: text("contract_scope").notNull().default("transport_validation_only"),
  governmentRequirementStatus: text("government_requirement_status").notNull().default("not_transcribed"),
  sourceKey: text("source_key").notNull(),
  optionOrder: integer("option_order").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_pilot_evidence_contract_idx").on(table.pilotRunId, table.requirementCode),
  index("compliance_pilot_evidence_order_idx").on(table.pilotRunId, table.optionOrder),
  check("compliance_pilot_evidence_content_check", sql`json_valid(${table.allowedContentTypes})`),
]);

export const compliancePilotCalculatorContracts = sqliteTable("compliance_pilot_calculator_contracts", {
  id: text("id").primaryKey(),
  pilotRunId: text("pilot_run_id").notNull(),
  activityTemplateId: text("activity_template_id").notNull(),
  registryActivityCode: text("registry_activity_code").notNull(),
  inputSchema: text("input_schema").notNull(),
  outputSchema: text("output_schema").notNull(),
  outputUnit: text("output_unit").notNull().default("VEEC"),
  formulaStatus: text("formula_status").notNull().default("blocked_pending_independent_verification"),
  testVectorStatus: text("test_vector_status").notNull().default("not_available"),
  sourceKey: text("source_key").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_pilot_calculator_activity_idx").on(table.pilotRunId, table.activityTemplateId),
  index("compliance_pilot_calculator_status_idx").on(table.pilotRunId, table.formulaStatus, table.testVectorStatus),
  check("compliance_pilot_calculator_json_check", sql`json_valid(${table.inputSchema}) AND json_valid(${table.outputSchema})`),
]);

export const compliancePilotInstallers = sqliteTable("compliance_pilot_installers", {
  id: text("id").primaryKey(),
  pilotRunId: text("pilot_run_id").notNull(),
  installerSlot: integer("installer_slot").notNull(),
  tradeAccountUid: text("trade_account_uid").notNull(),
  companyCode: text("company_code").notNull(),
  businessName: text("business_name").notNull(),
  status: text("status").notNull().default("test_active"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_pilot_installer_slot_idx").on(table.pilotRunId, table.installerSlot),
  uniqueIndex("compliance_pilot_installer_account_idx").on(table.pilotRunId, table.tradeAccountUid),
]);

export const compliancePilotTechnicians = sqliteTable("compliance_pilot_technicians", {
  id: text("id").primaryKey(),
  pilotRunId: text("pilot_run_id").notNull(),
  installerId: text("installer_id").notNull(),
  technicianSlot: integer("technician_slot").notNull(),
  teamMemberId: text("team_member_id").notNull(),
  technicianCode: text("technician_code").notNull(),
  displayName: text("display_name").notNull(),
  status: text("status").notNull().default("test_active"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_pilot_technician_slot_idx").on(table.pilotRunId, table.installerId, table.technicianSlot),
  uniqueIndex("compliance_pilot_technician_member_idx").on(table.pilotRunId, table.teamMemberId),
]);

export const compliancePilotJobs = sqliteTable("compliance_pilot_jobs", {
  id: text("id").primaryKey(),
  pilotRunId: text("pilot_run_id").notNull(),
  installerId: text("installer_id").notNull(),
  technicianId: text("technician_id").notNull(),
  workOrderId: text("work_order_id").notNull(),
  caseNumber: text("case_number").notNull(),
  jobNumber: text("job_number").notNull(),
  activityTemplateId: text("activity_template_id").notNull(),
  activityKey: text("activity_key").notNull(),
  registryActivityCode: text("registry_activity_code").notNull(),
  specificationPart: text("specification_part").notNull().default(""),
  title: text("title").notNull(),
  serviceCategory: text("service_category").notNull(),
  productCategory: text("product_category").notNull().default(""),
  scenarioCode: text("scenario_code").notNull().default(""),
  scenario: text("scenario").notNull().default(""),
  catalogueState: text("catalogue_state").notNull(),
  activityDate: text("activity_date").notNull(),
  recordMode: text("record_mode").notNull().default("synthetic_test"),
  ruleStatus: text("rule_status").notNull().default("blocked_pending_independent_review"),
  lookupStatus: text("lookup_status").notNull().default("not_checked"),
  evidenceStatus: text("evidence_status").notNull().default("not_started"),
  calculatorStatus: text("calculator_status").notNull().default("blocked_unverified_formula"),
  connectorStatus: text("connector_status").notNull().default("not_staged"),
  reviewStatus: text("review_status").notNull().default("test_ready"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_pilot_jobs_case_idx").on(table.pilotRunId, table.caseNumber),
  uniqueIndex("compliance_pilot_jobs_number_idx").on(table.pilotRunId, table.jobNumber),
  uniqueIndex("compliance_pilot_jobs_work_order_idx").on(table.workOrderId),
  index("compliance_pilot_jobs_run_activity_idx").on(table.pilotRunId, table.activityTemplateId, table.reviewStatus),
  index("compliance_pilot_jobs_run_installer_idx").on(table.pilotRunId, table.installerId, table.technicianId, table.jobNumber),
  index("compliance_pilot_jobs_register_review_idx").on(table.pilotRunId, table.reviewStatus, table.updatedAt, table.id),
  check("compliance_pilot_jobs_mode_check", sql`${table.recordMode} = 'synthetic_test'`),
]);

export const compliancePilotConnectorRuns = sqliteTable("compliance_pilot_connector_runs", {
  id: text("id").primaryKey(),
  pilotRunId: text("pilot_run_id").notNull(),
  connectorCode: text("connector_code").notNull(),
  mappingVersion: text("mapping_version").notNull(),
  mode: text("mode").notNull().default("dry_run"),
  status: text("status").notNull(),
  itemCount: integer("item_count").notNull().default(0),
  acceptedCount: integer("accepted_count").notNull().default(0),
  rejectedCount: integer("rejected_count").notNull().default(0),
  unmatchedCount: integer("unmatched_count").notNull().default(0),
  duplicateCount: integer("duplicate_count").notNull().default(0),
  artifactSha256: text("artifact_sha256").notNull(),
  artifactManifest: text("artifact_manifest").notNull(),
  externalSubmissionEnabled: integer("external_submission_enabled", { mode: "boolean" }).notNull().default(false),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_pilot_connector_run_idx").on(table.pilotRunId, table.connectorCode, table.mappingVersion),
  index("compliance_pilot_connector_status_idx").on(table.pilotRunId, table.status, table.updatedAt),
  check("compliance_pilot_connector_manifest_check", sql`json_valid(${table.artifactManifest})`),
  check("compliance_pilot_connector_execution_check", sql`${table.mode} = 'dry_run' AND ${table.externalSubmissionEnabled} = 0`),
]);

export const compliancePilotEvents = sqliteTable("compliance_pilot_events", {
  id: text("id").primaryKey(),
  pilotRunId: text("pilot_run_id").notNull(),
  organisationId: text("organisation_id").notNull(),
  eventType: text("event_type").notNull(),
  actorUid: text("actor_uid").notNull(),
  summary: text("summary").notNull(),
  metadata: text("metadata").notNull().default("{}"),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("compliance_pilot_events_run_time_idx").on(table.pilotRunId, table.createdAt, table.id),
  index("compliance_pilot_events_org_time_idx").on(table.organisationId, table.createdAt, table.id),
  check("compliance_pilot_events_metadata_check", sql`json_valid(${table.metadata})`),
]);

export const complianceManualEvidenceFormVersions = sqliteTable("compliance_manual_evidence_form_versions", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  programCode: text("program_code").notNull(),
  activityTemplateId: text("activity_template_id").notNull(),
  activitySnapshot: text("activity_snapshot").notNull(),
  version: integer("version").notNull(),
  title: text("title").notNull(),
  status: text("status").notNull().default("draft"),
  formSchema: text("form_schema").notNull(),
  formSchemaSha256: text("form_schema_sha256").notNull(),
  recordMode: text("record_mode").notNull().default("synthetic_test"),
  revision: integer("revision").notNull().default(1),
  createdByUid: text("created_by_uid").notNull(),
  updatedByUid: text("updated_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
  lockedAt: text("locked_at").notNull().default(""),
  archivedAt: text("archived_at").notNull().default(""),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_manual_evidence_form_version_idx")
    .on(table.organisationId, table.activityTemplateId, table.version),
  index("compliance_manual_evidence_form_status_idx")
    .on(table.organisationId, table.status, table.updatedAt),
  check("compliance_manual_evidence_form_mode_check", sql`${table.recordMode} = 'synthetic_test'`),
  check("compliance_manual_evidence_form_status_check", sql`${table.status} IN ('draft', 'test_ready', 'archived')`),
  check("compliance_manual_evidence_form_json_check", sql`json_valid(${table.activitySnapshot}) AND json_valid(${table.formSchema})`),
  check("compliance_manual_evidence_form_hash_check", sql`length(${table.formSchemaSha256}) = 64 AND lower(${table.formSchemaSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_manual_evidence_form_version_check", sql`${table.version} >= 1 AND ${table.revision} >= 1`),
  check("compliance_manual_evidence_form_lifecycle_check", sql`(${table.status} = 'draft' AND ${table.lockedAt} = '' AND ${table.archivedAt} = '') OR (${table.status} = 'test_ready' AND trim(${table.lockedAt}) <> '' AND ${table.archivedAt} = '') OR (${table.status} = 'archived' AND trim(${table.archivedAt}) <> '')`),
]);

export const complianceManualEvidenceTestJobs = sqliteTable("compliance_manual_evidence_test_jobs", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  formVersionId: text("form_version_id").notNull(),
  programCode: text("program_code").notNull(),
  activityTemplateId: text("activity_template_id").notNull(),
  activitySnapshot: text("activity_snapshot").notNull(),
  formSchema: text("form_schema").notNull(),
  formSchemaSha256: text("form_schema_sha256").notNull(),
  jobNumber: text("job_number").notNull(),
  installerId: text("installer_id").notNull().default(""),
  installerLabel: text("installer_label").notNull().default("Unassigned test installer"),
  technicianId: text("technician_id").notNull().default(""),
  technicianLabel: text("technician_label").notNull().default("Unassigned test technician"),
  fieldTesterUid: text("field_tester_uid").notNull().default(""),
  customerLabel: text("customer_label").notNull(),
  siteState: text("site_state").notNull(),
  sitePostcode: text("site_postcode").notNull(),
  status: text("status").notNull().default("draft"),
  responseSnapshot: text("response_snapshot").notNull().default("[]"),
  responseSha256: text("response_sha256").notNull(),
  requiredCount: integer("required_count").notNull().default(0),
  completedRequiredCount: integer("completed_required_count").notNull().default(0),
  issueCount: integer("issue_count").notNull().default(0),
  reviewNote: text("review_note").notNull().default(""),
  recordMode: text("record_mode").notNull().default("synthetic_test"),
  revision: integer("revision").notNull().default(1),
  createdByUid: text("created_by_uid").notNull(),
  updatedByUid: text("updated_by_uid").notNull(),
  passedByUid: text("passed_by_uid").notNull().default(""),
  createdAt: text("created_at").notNull(),
  passedAt: text("passed_at").notNull().default(""),
  archivedAt: text("archived_at").notNull().default(""),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_manual_evidence_test_job_number_idx")
    .on(table.organisationId, table.jobNumber),
  index("compliance_manual_evidence_test_job_status_idx")
    .on(table.organisationId, table.status, table.updatedAt),
  index("compliance_manual_evidence_test_job_activity_idx")
    .on(table.organisationId, table.activityTemplateId, table.updatedAt),
  index("compliance_manual_evidence_test_job_field_tester_idx")
    .on(table.organisationId, table.fieldTesterUid, table.status, table.updatedAt),
  index("compliance_manual_evidence_test_job_register_program_idx")
    .on(table.organisationId, table.programCode, table.status, table.updatedAt, table.id),
  index("compliance_manual_evidence_test_job_register_activity_idx")
    .on(table.organisationId, table.activityTemplateId, table.status, table.updatedAt, table.id),
  index("compliance_manual_evidence_test_job_register_personnel_idx")
    .on(table.organisationId, table.installerId, table.technicianId, table.updatedAt, table.id),
  index("compliance_manual_evidence_test_job_register_postcode_idx")
    .on(table.organisationId, table.sitePostcode, table.updatedAt, table.id),
  index("compliance_manual_evidence_test_job_register_created_idx")
    .on(table.organisationId, table.createdAt, table.id),
  check("compliance_manual_evidence_test_job_mode_check", sql`${table.recordMode} = 'synthetic_test'`),
  check("compliance_manual_evidence_test_job_status_check", sql`${table.status} IN ('draft', 'field_testing', 'ready_for_audit', 'changes_required', 'passed', 'archived')`),
  check("compliance_manual_evidence_test_job_json_check", sql`json_valid(${table.activitySnapshot}) AND json_valid(${table.formSchema}) AND json_valid(${table.responseSnapshot})`),
  check("compliance_manual_evidence_test_job_hash_check", sql`length(${table.formSchemaSha256}) = 64 AND lower(${table.formSchemaSha256}) NOT GLOB '*[^0-9a-f]*' AND length(${table.responseSha256}) = 64 AND lower(${table.responseSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_manual_evidence_test_job_count_check", sql`${table.requiredCount} >= 0 AND ${table.completedRequiredCount} >= 0 AND ${table.completedRequiredCount} <= ${table.requiredCount} AND ${table.issueCount} >= 0 AND ${table.revision} >= 1`),
  check("compliance_manual_evidence_test_job_lifecycle_check", sql`(${table.status} NOT IN ('passed', 'archived') AND ${table.passedAt} = '' AND ${table.archivedAt} = '') OR (${table.status} = 'passed' AND trim(${table.passedByUid}) <> '' AND trim(${table.passedAt}) <> '' AND ${table.archivedAt} = '') OR (${table.status} = 'archived' AND trim(${table.archivedAt}) <> '')`),
]);

export const complianceManualEvidenceTestEvents = sqliteTable("compliance_manual_evidence_test_events", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  jobId: text("job_id").notNull(),
  eventType: text("event_type").notNull(),
  actorUid: text("actor_uid").notNull(),
  summary: text("summary").notNull(),
  metadata: text("metadata").notNull().default("{}"),
  createdAt: text("created_at").notNull(),
}, (table) => [
  index("compliance_manual_evidence_test_event_job_idx")
    .on(table.organisationId, table.jobId, table.createdAt, table.id),
  check("compliance_manual_evidence_test_event_json_check", sql`json_valid(${table.metadata})`),
]);

export const complianceManualFieldDevices = sqliteTable("compliance_manual_field_devices", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  firebaseUid: text("firebase_uid").notNull(),
  deviceId: text("device_id").notNull(),
  platform: text("platform").notNull(),
  deviceName: text("device_name").notNull(),
  appVersion: text("app_version").notNull(),
  isPhysicalDevice: integer("is_physical_device").notNull().default(0),
  status: text("status").notNull().default("active"),
  registeredAt: text("registered_at").notNull(),
  lastSeenAt: text("last_seen_at").notNull(),
  revokedAt: text("revoked_at").notNull().default(""),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_manual_field_device_identity_idx")
    .on(table.organisationId, table.firebaseUid, table.deviceId),
  index("compliance_manual_field_device_status_idx")
    .on(table.organisationId, table.firebaseUid, table.status, table.lastSeenAt),
  check("compliance_manual_field_device_platform_check", sql`${table.platform} IN ('ios', 'android')`),
  check("compliance_manual_field_device_status_check", sql`${table.status} IN ('active', 'revoked')`),
  check("compliance_manual_field_device_physical_check", sql`${table.isPhysicalDevice} IN (0, 1)`),
]);

export const complianceManualFieldUploadSessions = sqliteTable("compliance_manual_field_upload_sessions", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  jobId: text("job_id").notNull(),
  fieldCode: text("field_code").notNull(),
  fieldTesterUid: text("field_tester_uid").notNull(),
  deviceId: text("device_id").notNull(),
  clientUploadId: text("client_upload_id").notNull(),
  objectKey: text("object_key").notNull(),
  uploadId: text("upload_id").notNull(),
  fileName: text("file_name").notNull(),
  contentType: text("content_type").notNull(),
  sizeBytes: integer("size_bytes").notNull(),
  partSizeBytes: integer("part_size_bytes").notNull(),
  evidenceEnvelope: text("evidence_envelope").notNull(),
  declaredSha256: text("declared_sha256").notNull(),
  status: text("status").notNull().default("initiated"),
  captureId: text("capture_id").notNull().default(""),
  lastError: text("last_error").notNull().default(""),
  recordMode: text("record_mode").notNull().default("synthetic_test"),
  expiresAt: text("expires_at").notNull(),
  createdAt: text("created_at").notNull(),
  completedAt: text("completed_at").notNull().default(""),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_manual_field_upload_client_idx")
    .on(table.organisationId, table.fieldTesterUid, table.clientUploadId),
  uniqueIndex("compliance_manual_field_upload_object_idx").on(table.objectKey),
  index("compliance_manual_field_upload_job_idx")
    .on(table.organisationId, table.jobId, table.status, table.updatedAt),
  index("compliance_manual_field_upload_device_idx")
    .on(table.organisationId, table.fieldTesterUid, table.deviceId, table.status),
  check("compliance_manual_field_upload_mode_check", sql`${table.recordMode} = 'synthetic_test'`),
  check("compliance_manual_field_upload_status_check", sql`${table.status} IN ('initiated', 'uploading', 'completing', 'completed', 'rejected', 'expired', 'aborted')`),
  check("compliance_manual_field_upload_json_check", sql`json_valid(${table.evidenceEnvelope})`),
  check("compliance_manual_field_upload_size_check", sql`${table.sizeBytes} >= 1 AND ${table.sizeBytes} <= 52428800 AND ${table.partSizeBytes} = 5242880`),
  check("compliance_manual_field_upload_hash_check", sql`length(${table.declaredSha256}) = 64 AND lower(${table.declaredSha256}) NOT GLOB '*[^0-9a-f]*'`),
]);

export const complianceManualFieldUploadParts = sqliteTable("compliance_manual_field_upload_parts", {
  id: text("id").primaryKey(),
  sessionId: text("session_id").notNull(),
  partNumber: integer("part_number").notNull(),
  etag: text("etag").notNull(),
  sizeBytes: integer("size_bytes").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_manual_field_upload_part_idx").on(table.sessionId, table.partNumber),
  check("compliance_manual_field_upload_part_check", sql`${table.partNumber} >= 1 AND ${table.sizeBytes} >= 1 AND ${table.sizeBytes} <= 5242880`),
]);

export const complianceManualEvidenceTestCaptures = sqliteTable("compliance_manual_evidence_test_captures", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  jobId: text("job_id").notNull(),
  fieldCode: text("field_code").notNull(),
  fieldTesterUid: text("field_tester_uid").notNull(),
  deviceId: text("device_id").notNull(),
  uploadSessionId: text("upload_session_id").notNull(),
  objectKey: text("object_key").notNull(),
  fileName: text("file_name").notNull(),
  contentType: text("content_type").notNull(),
  sizeBytes: integer("size_bytes").notNull(),
  originalSha256: text("original_sha256").notNull(),
  evidenceEnvelope: text("evidence_envelope").notNull(),
  serverVerification: text("server_verification").notNull(),
  metadataState: text("metadata_state").notNull(),
  gpsState: text("gps_state").notNull(),
  captureTimeState: text("capture_time_state").notNull(),
  physicalDeviceState: text("physical_device_state").notNull(),
  status: text("status").notNull().default("captured"),
  recordMode: text("record_mode").notNull().default("synthetic_test"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_manual_evidence_test_capture_session_idx").on(table.uploadSessionId),
  index("compliance_manual_evidence_test_capture_job_idx")
    .on(table.organisationId, table.jobId, table.fieldCode, table.status, table.createdAt),
  check("compliance_manual_evidence_test_capture_mode_check", sql`${table.recordMode} = 'synthetic_test'`),
  check("compliance_manual_evidence_test_capture_status_check", sql`${table.status} IN ('captured', 'superseded', 'rejected')`),
  check("compliance_manual_evidence_test_capture_json_check", sql`json_valid(${table.evidenceEnvelope}) AND json_valid(${table.serverVerification})`),
  check("compliance_manual_evidence_test_capture_hash_check", sql`length(${table.originalSha256}) = 64 AND lower(${table.originalSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_manual_evidence_test_capture_state_check", sql`${table.metadataState} IN ('verified', 'not_required', 'missing', 'invalid') AND ${table.gpsState} IN ('verified', 'not_required', 'missing', 'invalid') AND ${table.captureTimeState} IN ('verified', 'not_required', 'missing', 'invalid') AND ${table.physicalDeviceState} IN ('reported_physical', 'reported_emulator')`),
]);

export const complianceManualFieldIntegrityReceipts = sqliteTable("compliance_manual_field_integrity_receipts", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  captureId: text("capture_id").notNull(),
  requestId: text("request_id").notNull(),
  objectKey: text("object_key").notNull(),
  expectedSha256: text("expected_sha256").notNull(),
  observedSha256: text("observed_sha256").notNull(),
  expectedSizeBytes: integer("expected_size_bytes").notNull(),
  observedSizeBytes: integer("observed_size_bytes").notNull(),
  result: text("result").notNull(),
  verificationScope: text("verification_scope").notNull().default("r2_object_bytes_and_embedded_metadata"),
  verifiedByUid: text("verified_by_uid").notNull(),
  verifiedAt: text("verified_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_manual_field_integrity_request_idx").on(table.organisationId, table.requestId),
  index("compliance_manual_field_integrity_capture_idx")
    .on(table.organisationId, table.captureId, table.verifiedAt),
  check("compliance_manual_field_integrity_result_check", sql`${table.result} IN ('matched', 'mismatch', 'object_missing', 'storage_unavailable')`),
  check("compliance_manual_field_integrity_hash_check", sql`length(${table.expectedSha256}) = 64 AND lower(${table.expectedSha256}) NOT GLOB '*[^0-9a-f]*' AND (${table.observedSha256} = '' OR (length(${table.observedSha256}) = 64 AND lower(${table.observedSha256}) NOT GLOB '*[^0-9a-f]*'))`),
]);

export const complianceManualFieldAcceptanceRuns = sqliteTable("compliance_manual_field_acceptance_runs", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  jobId: text("job_id").notNull(),
  testerUid: text("tester_uid").notNull(),
  reviewerUid: text("reviewer_uid").notNull().default(""),
  deviceId: text("device_id").notNull(),
  platform: text("platform").notNull(),
  appVersion: text("app_version").notNull(),
  scenarioResults: text("scenario_results").notNull().default("[]"),
  status: text("status").notNull().default("not_run"),
  testerNote: text("tester_note").notNull().default(""),
  reviewerNote: text("reviewer_note").notNull().default(""),
  recordMode: text("record_mode").notNull().default("synthetic_test"),
  startedAt: text("started_at").notNull().default(""),
  submittedAt: text("submitted_at").notNull().default(""),
  reviewedAt: text("reviewed_at").notNull().default(""),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  index("compliance_manual_field_acceptance_job_idx")
    .on(table.organisationId, table.jobId, table.status, table.updatedAt),
  check("compliance_manual_field_acceptance_mode_check", sql`${table.recordMode} = 'synthetic_test'`),
  check("compliance_manual_field_acceptance_status_check", sql`${table.status} IN ('not_run', 'in_progress', 'submitted', 'passed', 'failed')`),
  check("compliance_manual_field_acceptance_json_check", sql`json_valid(${table.scenarioResults})`),
  check("compliance_manual_field_acceptance_platform_check", sql`${table.platform} IN ('ios', 'android')`),
  check("compliance_manual_field_acceptance_dual_control_check", sql`${table.reviewerUid} = '' OR ${table.reviewerUid} <> ${table.testerUid}`),
]);

export const complianceManualFieldActionReceipts = sqliteTable("compliance_manual_field_action_receipts", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  fieldTesterUid: text("field_tester_uid").notNull(),
  clientActionId: text("client_action_id").notNull(),
  actionType: text("action_type").notNull(),
  jobId: text("job_id").notNull(),
  formId: text("form_id").notNull(),
  baseRevision: integer("base_revision").notNull(),
  payloadSha256: text("payload_sha256").notNull(),
  responseSha256: text("response_sha256").notNull(),
  resultRevision: integer("result_revision").notNull(),
  status: text("status").notNull().default("applied"),
  recordMode: text("record_mode").notNull().default("synthetic_test"),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_manual_field_action_receipt_client_idx")
    .on(table.organisationId, table.fieldTesterUid, table.clientActionId),
  index("compliance_manual_field_action_receipt_job_idx")
    .on(table.organisationId, table.jobId, table.createdAt, table.id),
  check("compliance_manual_field_action_receipt_identity_check", sql`trim(${table.organisationId}) <> '' AND trim(${table.fieldTesterUid}) <> '' AND trim(${table.clientActionId}) <> '' AND trim(${table.jobId}) <> '' AND ${table.formId} = ${table.jobId} || ':technical'`),
  check("compliance_manual_field_action_receipt_action_check", sql`${table.actionType} = 'save_job_form'`),
  check("compliance_manual_field_action_receipt_revision_check", sql`${table.baseRevision} >= 1 AND ${table.resultRevision} = ${table.baseRevision} + 1`),
  check("compliance_manual_field_action_receipt_hash_check", sql`length(${table.payloadSha256}) = 64 AND lower(${table.payloadSha256}) NOT GLOB '*[^0-9a-f]*' AND length(${table.responseSha256}) = 64 AND lower(${table.responseSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_manual_field_action_receipt_status_check", sql`${table.status} = 'applied'`),
  check("compliance_manual_field_action_receipt_mode_check", sql`${table.recordMode} = 'synthetic_test'`),
]);

export const complianceManualPolicyBindings = sqliteTable("compliance_manual_policy_bindings", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  activityTemplateId: text("activity_template_id").notNull(),
  version: integer("version").notNull(),
  programId: text("program_id").notNull(),
  activityVersionId: text("activity_version_id").notNull(),
  evidencePolicyVersionId: text("evidence_policy_version_id").notNull(),
  programSourceBindingId: text("program_source_binding_id").notNull(),
  activitySourceBindingId: text("activity_source_binding_id").notNull(),
  evidencePolicySourceBindingId: text("evidence_policy_source_binding_id").notNull(),
  bindingSnapshot: text("binding_snapshot").notNull(),
  bindingSnapshotSha256: text("binding_snapshot_sha256").notNull(),
  lifecycleState: text("lifecycle_state").notNull().default("draft"),
  requestedByUid: text("requested_by_uid").notNull(),
  requestedAt: text("requested_at").notNull(),
  approvedByUid: text("approved_by_uid").notNull().default(""),
  approvedAt: text("approved_at").notNull().default(""),
  approvalNote: text("approval_note").notNull().default(""),
  withdrawnByUid: text("withdrawn_by_uid").notNull().default(""),
  withdrawnAt: text("withdrawn_at").notNull().default(""),
  withdrawalNote: text("withdrawal_note").notNull().default(""),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_manual_policy_template_version_idx")
    .on(table.organisationId, table.activityTemplateId, table.version),
  uniqueIndex("compliance_manual_policy_current_template_idx")
    .on(table.organisationId, table.activityTemplateId)
    .where(sql`${table.lifecycleState} IN ('draft', 'approved')`),
  index("compliance_manual_policy_activity_idx")
    .on(table.organisationId, table.activityVersionId, table.lifecycleState),
  index("compliance_manual_policy_evidence_policy_idx")
    .on(table.organisationId, table.evidencePolicyVersionId, table.lifecycleState),
  check("compliance_manual_policy_version_check", sql`${table.version} > 0`),
  check("compliance_manual_policy_identity_check", sql`trim(${table.id}) <> '' AND trim(${table.organisationId}) <> '' AND trim(${table.activityTemplateId}) <> '' AND trim(${table.programId}) <> '' AND trim(${table.activityVersionId}) <> '' AND trim(${table.evidencePolicyVersionId}) <> '' AND trim(${table.programSourceBindingId}) <> '' AND trim(${table.activitySourceBindingId}) <> '' AND trim(${table.evidencePolicySourceBindingId}) <> '' AND trim(${table.requestedByUid}) <> ''`),
  check("compliance_manual_policy_snapshot_check", sql`json_valid(${table.bindingSnapshot}) AND json_extract(${table.bindingSnapshot}, '$.contract') = 'creditex-manual-policy-binding-v1' AND json_extract(${table.bindingSnapshot}, '$.organisationId') = ${table.organisationId} AND json_extract(${table.bindingSnapshot}, '$.activityTemplate.templateId') = ${table.activityTemplateId} AND json_extract(${table.bindingSnapshot}, '$.program.id') = ${table.programId} AND json_extract(${table.bindingSnapshot}, '$.activity.id') = ${table.activityVersionId} AND json_extract(${table.bindingSnapshot}, '$.evidencePolicy.id') = ${table.evidencePolicyVersionId} AND json_extract(${table.bindingSnapshot}, '$.sourceApprovals.programBindingId') = ${table.programSourceBindingId} AND json_extract(${table.bindingSnapshot}, '$.sourceApprovals.activityBindingId') = ${table.activitySourceBindingId} AND json_extract(${table.bindingSnapshot}, '$.sourceApprovals.evidencePolicyBindingId') = ${table.evidencePolicySourceBindingId} AND json_type(${table.bindingSnapshot}, '$.requirements') = 'array' AND json_array_length(${table.bindingSnapshot}, '$.requirements') > 0 AND length(${table.bindingSnapshotSha256}) = 64 AND lower(${table.bindingSnapshotSha256}) NOT GLOB '*[^0-9a-f]*' AND ${table.bindingSnapshotSha256} = lower(${table.bindingSnapshotSha256})`),
  check("compliance_manual_policy_state_check", sql`${table.lifecycleState} IN ('draft', 'approved', 'withdrawn')`),
  check("compliance_manual_policy_timestamps_check", sql`datetime(${table.requestedAt}) IS NOT NULL AND datetime(${table.createdAt}) IS NOT NULL AND datetime(${table.updatedAt}) IS NOT NULL`),
  check("compliance_manual_policy_lifecycle_check", sql`(${table.lifecycleState} = 'draft' AND ${table.approvedByUid} = '' AND ${table.approvedAt} = '' AND ${table.approvalNote} = '' AND ${table.withdrawnByUid} = '' AND ${table.withdrawnAt} = '' AND ${table.withdrawalNote} = '') OR (${table.lifecycleState} = 'approved' AND trim(${table.approvedByUid}) <> '' AND ${table.approvedByUid} <> ${table.requestedByUid} AND datetime(${table.approvedAt}) IS NOT NULL AND length(trim(${table.approvalNote})) BETWEEN 10 AND 1000 AND ${table.withdrawnByUid} = '' AND ${table.withdrawnAt} = '' AND ${table.withdrawalNote} = '') OR (${table.lifecycleState} = 'withdrawn' AND trim(${table.approvedByUid}) <> '' AND ${table.approvedByUid} <> ${table.requestedByUid} AND datetime(${table.approvedAt}) IS NOT NULL AND length(trim(${table.approvalNote})) BETWEEN 10 AND 1000 AND trim(${table.withdrawnByUid}) <> '' AND datetime(${table.withdrawnAt}) IS NOT NULL AND length(trim(${table.withdrawalNote})) BETWEEN 10 AND 1000)`),
]);

export const complianceManualPolicyCompositionLocks = sqliteTable("compliance_manual_policy_composition_locks", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  bindingId: text("binding_id").notNull(),
  bindingVersion: integer("binding_version").notNull(),
  bindingSnapshotSha256: text("binding_snapshot_sha256").notNull(),
  activityTemplateId: text("activity_template_id").notNull(),
  activityVersionId: text("activity_version_id").notNull(),
  referenceType: text("reference_type").notNull(),
  referenceId: text("reference_id").notNull(),
  referenceActivityDate: text("reference_activity_date").notNull(),
  referenceUpdatedAt: text("reference_updated_at").notNull(),
  referenceSnapshotSha256: text("reference_snapshot_sha256").notNull(),
  revision: integer("revision").notNull(),
  compositionSnapshot: text("composition_snapshot").notNull(),
  compositionSha256: text("composition_sha256").notNull(),
  diffSnapshot: text("diff_snapshot").notNull(),
  diffSha256: text("diff_sha256").notNull(),
  lockedByUid: text("locked_by_uid").notNull(),
  lockedAt: text("locked_at").notNull(),
  supersededById: text("superseded_by_id").notNull().default(""),
  supersededAt: text("superseded_at").notNull().default(""),
}, (table) => [
  uniqueIndex("compliance_manual_policy_composition_revision_idx")
    .on(table.organisationId, table.referenceType, table.referenceId, table.revision),
  uniqueIndex("compliance_manual_policy_composition_current_idx")
    .on(table.organisationId, table.referenceType, table.referenceId)
    .where(sql`${table.supersededById} = ''`),
  index("compliance_manual_policy_composition_binding_idx")
    .on(table.organisationId, table.bindingId, table.revision),
  check("compliance_manual_policy_composition_identity_check", sql`trim(${table.id}) <> '' AND trim(${table.organisationId}) <> '' AND trim(${table.bindingId}) <> '' AND ${table.bindingVersion} > 0 AND trim(${table.activityTemplateId}) <> '' AND trim(${table.activityVersionId}) <> '' AND ${table.referenceType} IN ('compliance_case', 'synthetic_pilot_job') AND trim(${table.referenceId}) <> '' AND ${table.revision} > 0 AND trim(${table.lockedByUid}) <> ''`),
  check("compliance_manual_policy_composition_date_check", sql`length(${table.referenceActivityDate}) = 10 AND ${table.referenceActivityDate} GLOB '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]' AND date(${table.referenceActivityDate}) = ${table.referenceActivityDate} AND datetime(${table.referenceUpdatedAt}) IS NOT NULL`),
  check("compliance_manual_policy_composition_snapshot_check", sql`json_valid(${table.compositionSnapshot}) AND json_extract(${table.compositionSnapshot}, '$.contract') = 'creditex-manual-evidence-form-v2' AND json_extract(${table.compositionSnapshot}, '$.bindingId') = ${table.bindingId} AND json_extract(${table.compositionSnapshot}, '$.bindingVersion') = ${table.bindingVersion} AND json_extract(${table.compositionSnapshot}, '$.bindingSnapshotSha256') = ${table.bindingSnapshotSha256} AND json_extract(${table.compositionSnapshot}, '$.bindingSnapshot.activityTemplate.templateId') = ${table.activityTemplateId} AND json_extract(${table.compositionSnapshot}, '$.bindingSnapshot.activity.id') = ${table.activityVersionId} AND json_extract(${table.compositionSnapshot}, '$.activityReference.referenceType') = ${table.referenceType} AND json_extract(${table.compositionSnapshot}, '$.activityReference.referenceId') = ${table.referenceId} AND json_extract(${table.compositionSnapshot}, '$.activityReference.activityDate') = ${table.referenceActivityDate} AND json_extract(${table.compositionSnapshot}, '$.activityReference.referenceUpdatedAt') = ${table.referenceUpdatedAt} AND json_extract(${table.compositionSnapshot}, '$.activityReference.referenceSnapshotSha256') = ${table.referenceSnapshotSha256} AND json_valid(${table.diffSnapshot}) AND json_type(${table.diffSnapshot}) = 'array' AND length(${table.bindingSnapshotSha256}) = 64 AND lower(${table.bindingSnapshotSha256}) NOT GLOB '*[^0-9a-f]*' AND ${table.bindingSnapshotSha256} = lower(${table.bindingSnapshotSha256}) AND length(${table.referenceSnapshotSha256}) = 64 AND lower(${table.referenceSnapshotSha256}) NOT GLOB '*[^0-9a-f]*' AND ${table.referenceSnapshotSha256} = lower(${table.referenceSnapshotSha256}) AND length(${table.compositionSha256}) = 64 AND lower(${table.compositionSha256}) NOT GLOB '*[^0-9a-f]*' AND ${table.compositionSha256} = lower(${table.compositionSha256}) AND length(${table.diffSha256}) = 64 AND lower(${table.diffSha256}) NOT GLOB '*[^0-9a-f]*' AND ${table.diffSha256} = lower(${table.diffSha256})`),
  check("compliance_manual_policy_composition_lifecycle_check", sql`datetime(${table.lockedAt}) IS NOT NULL AND ((${table.supersededById} = '' AND ${table.supersededAt} = '') OR (trim(${table.supersededById}) <> '' AND ${table.supersededById} <> ${table.id} AND datetime(${table.supersededAt}) IS NOT NULL))`),
]);

export const complianceLegacyImportBatches = sqliteTable("compliance_legacy_import_batches", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  sourceSystem: text("source_system").notNull().default("dataforce"),
  contractVersion: text("contract_version").notNull().default("dataforce-jobs-v1"),
  fileName: text("file_name").notNull(),
  contentSha256: text("content_sha256").notNull(),
  fileSizeBytes: integer("file_size_bytes").notNull(),
  rowCount: integer("row_count").notNull(),
  status: text("status").notNull().default("staged_unmapped"),
  regulatedJobCreationEnabled: integer("regulated_job_creation_enabled", { mode: "boolean" }).notNull().default(false),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_legacy_import_batches_org_hash_idx").on(table.organisationId, table.contentSha256),
  index("compliance_legacy_import_batches_org_time_idx").on(table.organisationId, table.createdAt, table.id),
  check("compliance_legacy_import_batches_source_check", sql`${table.sourceSystem} = 'dataforce' AND ${table.contractVersion} = 'dataforce-jobs-v1'`),
  check("compliance_legacy_import_batches_hash_check", sql`length(${table.contentSha256}) = 64 AND lower(${table.contentSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_legacy_import_batches_size_check", sql`${table.fileSizeBytes} > 0 AND ${table.fileSizeBytes} <= 5242880 AND ${table.rowCount} > 0 AND ${table.rowCount} <= 2500`),
  check("compliance_legacy_import_batches_boundary_check", sql`${table.status} = 'staged_unmapped' AND ${table.regulatedJobCreationEnabled} = 0`),
]);

export const complianceLegacyImportRows = sqliteTable("compliance_legacy_import_rows", {
  id: text("id").primaryKey(),
  batchId: text("batch_id").notNull(),
  organisationId: text("organisation_id").notNull(),
  rowNumber: integer("row_number").notNull(),
  appId: text("app_id").notNull().default(""),
  jobId: text("job_id").notNull(),
  rowSha256: text("row_sha256").notNull(),
  dataJson: text("data_json").notNull(),
  mappingStatus: text("mapping_status").notNull().default("staged_unmapped"),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_legacy_import_rows_batch_row_idx").on(table.batchId, table.rowNumber),
  index("compliance_legacy_import_rows_org_job_idx").on(table.organisationId, table.jobId, table.createdAt),
  check("compliance_legacy_import_rows_number_check", sql`${table.rowNumber} > 1`),
  check("compliance_legacy_import_rows_job_check", sql`trim(${table.jobId}) <> ''`),
  check("compliance_legacy_import_rows_hash_check", sql`length(${table.rowSha256}) = 64 AND lower(${table.rowSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_legacy_import_rows_json_check", sql`json_valid(${table.dataJson})`),
  check("compliance_legacy_import_rows_boundary_check", sql`${table.mappingStatus} = 'staged_unmapped'`),
]);

export const complianceOfficialSourceArtifacts = sqliteTable("compliance_official_source_artifacts", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  clientRequestId: text("client_request_id").notNull(),
  sourceUrl: text("source_url").notNull(),
  sourceHost: text("source_host").notNull(),
  sourceTitle: text("source_title").notNull(),
  sourceVersion: text("source_version").notNull().default(""),
  originalFileName: text("original_file_name").notNull(),
  contentType: text("content_type").notNull(),
  sizeBytes: integer("size_bytes").notNull(),
  sha256: text("sha256").notNull(),
  objectKey: text("object_key").notNull(),
  retrievalMethod: text("retrieval_method").notNull().default("manual_upload"),
  assertedRetrievedAt: text("asserted_retrieved_at").notNull(),
  sourceEtag: text("source_etag").notNull().default(""),
  sourceLastModified: text("source_last_modified").notNull().default(""),
  custodyState: text("custody_state").notNull().default("pending_review"),
  ruleActivationEnabled: integer("rule_activation_enabled", { mode: "boolean" }).notNull().default(false),
  capturedByUid: text("captured_by_uid").notNull(),
  capturedAt: text("captured_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_official_source_artifacts_org_request_idx").on(table.organisationId, table.clientRequestId),
  uniqueIndex("compliance_official_source_artifacts_org_object_idx").on(table.organisationId, table.objectKey),
  index("compliance_official_source_artifacts_org_state_idx").on(table.organisationId, table.custodyState, table.capturedAt, table.id),
  index("compliance_official_source_artifacts_org_hash_idx").on(table.organisationId, table.sha256, table.capturedAt),
  check("compliance_official_source_artifacts_url_check", sql`${table.sourceUrl} LIKE 'https://%'`),
  check("compliance_official_source_artifacts_host_check", sql`${table.sourceHost} = lower(${table.sourceHost}) AND (${table.sourceHost} = 'gov.au' OR ${table.sourceHost} LIKE '%.gov.au')`),
  check("compliance_official_source_artifacts_title_check", sql`trim(${table.sourceTitle}) <> '' AND trim(${table.originalFileName}) <> ''`),
  check("compliance_official_source_artifacts_content_type_check", sql`${table.contentType} IN ('application/pdf', 'application/json', 'application/xml', 'text/xml', 'text/html', 'text/plain', 'text/csv', 'application/msword', 'application/vnd.ms-excel', 'application/vnd.openxmlformats-officedocument.wordprocessingml.document', 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet')`),
  check("compliance_official_source_artifacts_size_check", sql`${table.sizeBytes} > 0 AND ${table.sizeBytes} <= 15728640`),
  check("compliance_official_source_artifacts_hash_check", sql`length(${table.sha256}) = 64 AND lower(${table.sha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_official_source_artifacts_object_check", sql`trim(${table.objectKey}) <> ''`),
  check("compliance_official_source_artifacts_retrieval_check", sql`${table.retrievalMethod} = 'manual_upload' AND datetime(${table.assertedRetrievedAt}) IS NOT NULL`),
  check("compliance_official_source_artifacts_state_check", sql`${table.custodyState} IN ('draft', 'pending_review') AND ${table.ruleActivationEnabled} = 0`),
]);

export const complianceOfficialSourceBindings = sqliteTable("compliance_official_source_bindings", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  artifactId: text("artifact_id").notNull(),
  targetType: text("target_type").notNull(),
  targetId: text("target_id").notNull(),
  citationLocation: text("citation_location").notNull(),
  bindingState: text("binding_state").notNull().default("pending_review"),
  ruleActivationEnabled: integer("rule_activation_enabled", { mode: "boolean" }).notNull().default(false),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_official_source_bindings_identity_idx").on(
    table.organisationId,
    table.artifactId,
    table.targetType,
    table.targetId,
    table.citationLocation,
  ),
  index("compliance_official_source_bindings_target_idx").on(table.organisationId, table.targetType, table.targetId, table.createdAt),
  index("compliance_official_source_bindings_artifact_idx").on(table.artifactId, table.createdAt),
  check("compliance_official_source_bindings_target_check", sql`${table.targetType} IN ('program', 'activity', 'evidence_policy', 'calculator') AND trim(${table.targetId}) <> ''`),
  check("compliance_official_source_bindings_citation_check", sql`trim(${table.citationLocation}) <> ''`),
  check("compliance_official_source_bindings_state_check", sql`${table.bindingState} IN ('draft', 'pending_review') AND ${table.ruleActivationEnabled} = 0`),
]);

export const complianceOfficialSourceReviewDecisions = sqliteTable("compliance_official_source_review_decisions", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  subjectType: text("subject_type").notNull(),
  subjectId: text("subject_id").notNull(),
  artifactId: text("artifact_id").notNull(),
  artifactSha256: text("artifact_sha256").notNull(),
  artifactObjectKey: text("artifact_object_key").notNull(),
  bindingTargetType: text("binding_target_type").notNull().default(""),
  bindingTargetId: text("binding_target_id").notNull().default(""),
  citationLocation: text("citation_location").notNull().default(""),
  decision: text("decision").notNull(),
  supersedesDecisionId: text("supersedes_decision_id").notNull().default(""),
  reviewNote: text("review_note").notNull(),
  reviewedByUid: text("reviewed_by_uid").notNull(),
  reviewedAt: text("reviewed_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_source_review_initial_idx")
    .on(table.organisationId, table.subjectType, table.subjectId)
    .where(sql`${table.supersedesDecisionId} = ''`),
  uniqueIndex("compliance_source_review_supersedes_idx")
    .on(table.supersedesDecisionId)
    .where(sql`${table.supersedesDecisionId} <> ''`),
  index("compliance_source_review_subject_idx").on(
    table.organisationId,
    table.subjectType,
    table.subjectId,
    table.reviewedAt,
    table.id,
  ),
  index("compliance_source_review_artifact_idx").on(
    table.organisationId,
    table.artifactId,
    table.artifactSha256,
    table.reviewedAt,
    table.id,
  ),
  check("compliance_source_review_subject_type_check", sql`${table.subjectType} IN ('artifact', 'binding')`),
  check("compliance_source_review_subject_check", sql`trim(${table.subjectId}) <> '' AND trim(${table.artifactId}) <> ''`),
  check("compliance_source_review_hash_check", sql`length(${table.artifactSha256}) = 64 AND lower(${table.artifactSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_source_review_object_check", sql`trim(${table.artifactObjectKey}) <> ''`),
  check("compliance_source_review_decision_check", sql`${table.decision} IN ('approved', 'rejected', 'withdrawn')`),
  check("compliance_source_review_note_check", sql`trim(${table.reviewNote}) <> '' AND length(${table.reviewNote}) <= 1000`),
  check("compliance_source_review_reviewer_check", sql`trim(${table.reviewedByUid}) <> '' AND datetime(${table.reviewedAt}) IS NOT NULL`),
  check("compliance_source_review_subject_snapshot_check", sql`(${table.subjectType} = 'artifact' AND ${table.subjectId} = ${table.artifactId} AND ${table.bindingTargetType} = '' AND ${table.bindingTargetId} = '' AND ${table.citationLocation} = '') OR (${table.subjectType} = 'binding' AND ${table.bindingTargetType} IN ('program', 'activity', 'evidence_policy', 'calculator') AND trim(${table.bindingTargetId}) <> '' AND trim(${table.citationLocation}) <> '')`),
  check("compliance_source_review_transition_shape_check", sql`(${table.decision} IN ('approved', 'rejected') AND ${table.supersedesDecisionId} = '') OR (${table.decision} = 'withdrawn' AND trim(${table.supersedesDecisionId}) <> '')`),
]);

export const complianceEvidenceIntegrityReceipts = sqliteTable("compliance_evidence_integrity_receipts", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  evidenceId: text("evidence_id").notNull(),
  requestId: text("request_id").notNull(),
  objectKey: text("object_key").notNull(),
  expectedSha256: text("expected_sha256").notNull(),
  observedSha256: text("observed_sha256").notNull().default(""),
  expectedSizeBytes: integer("expected_size_bytes").notNull(),
  observedSizeBytes: integer("observed_size_bytes").notNull().default(0),
  expectedContentType: text("expected_content_type").notNull(),
  observedContentType: text("observed_content_type").notNull().default(""),
  result: text("result").notNull(),
  verificationScope: text("verification_scope").notNull().default("r2_object_bytes_only"),
  physicalDeviceValidationState: text("physical_device_validation_state").notNull().default("not_assessed"),
  verifiedByUid: text("verified_by_uid").notNull(),
  verifiedAt: text("verified_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_evidence_integrity_receipts_org_request_idx").on(table.organisationId, table.requestId),
  index("compliance_evidence_integrity_receipts_evidence_idx").on(table.organisationId, table.evidenceId, table.verifiedAt, table.id),
  index("compliance_evidence_integrity_receipts_result_idx").on(table.organisationId, table.result, table.verifiedAt, table.id),
  check("compliance_evidence_integrity_receipts_expected_hash_check", sql`length(${table.expectedSha256}) = 64 AND lower(${table.expectedSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_evidence_integrity_receipts_observed_hash_check", sql`${table.observedSha256} = '' OR (length(${table.observedSha256}) = 64 AND lower(${table.observedSha256}) NOT GLOB '*[^0-9a-f]*')`),
  check("compliance_evidence_integrity_receipts_size_check", sql`${table.expectedSizeBytes} > 0 AND ${table.observedSizeBytes} >= 0`),
  check("compliance_evidence_integrity_receipts_result_check", sql`${table.result} IN ('matched', 'mismatch', 'object_missing', 'object_oversize', 'storage_unavailable')`),
  check("compliance_evidence_integrity_receipts_scope_check", sql`${table.verificationScope} = 'r2_object_bytes_only' AND ${table.physicalDeviceValidationState} = 'not_assessed'`),
  check("compliance_evidence_integrity_receipts_verified_at_check", sql`datetime(${table.verifiedAt}) IS NOT NULL`),
  check("compliance_evidence_integrity_receipts_result_consistency_check", sql`(${table.result} = 'matched' AND ${table.observedSha256} = ${table.expectedSha256} AND ${table.observedSizeBytes} = ${table.expectedSizeBytes} AND (${table.observedContentType} = '' OR lower(${table.observedContentType}) = lower(${table.expectedContentType}))) OR (${table.result} = 'mismatch' AND ${table.observedSha256} <> '' AND (${table.observedSha256} <> ${table.expectedSha256} OR ${table.observedSizeBytes} <> ${table.expectedSizeBytes} OR (${table.observedContentType} <> '' AND lower(${table.observedContentType}) <> lower(${table.expectedContentType})))) OR (${table.result} IN ('object_missing', 'storage_unavailable') AND ${table.observedSha256} = '' AND ${table.observedSizeBytes} = 0 AND ${table.observedContentType} = '') OR (${table.result} = 'object_oversize' AND ${table.observedSha256} = '' AND ${table.observedSizeBytes} > 52428800)`),
]);

export const complianceFieldCustodyTestArtifacts = sqliteTable("compliance_field_custody_test_artifacts", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  clientRequestId: text("client_request_id").notNull(),
  requestSha256: text("request_sha256").notNull(),
  artifactSha256: text("artifact_sha256").notNull(),
  platform: text("platform").notNull(),
  nativeBuildIdentifier: text("native_build_identifier").notNull(),
  nativeBuildSha256: text("native_build_sha256").notNull(),
  deviceClass: text("device_class").notNull().default("physical"),
  deviceModel: text("device_model").notNull(),
  deviceOsVersion: text("device_os_version").notNull(),
  deviceIdentifierSha256: text("device_identifier_sha256").notNull(),
  requirementId: text("requirement_id").notNull(),
  evidenceId: text("evidence_id").notNull(),
  integrityReceiptId: text("integrity_receipt_id").notNull(),
  offlineScenario: text("offline_scenario").notNull().default("offline_capture_restore"),
  restoreSha256: text("restore_sha256").notNull(),
  testResult: text("test_result").notNull(),
  testerUid: text("tester_uid").notNull(),
  testedAt: text("tested_at").notNull(),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_field_custody_test_artifact_request_idx").on(table.organisationId, table.clientRequestId),
  index("compliance_field_custody_test_artifact_evidence_idx").on(table.organisationId, table.evidenceId, table.testedAt, table.id),
  index("compliance_field_custody_test_artifact_tester_idx").on(table.organisationId, table.testerUid, table.testedAt, table.id),
  check("compliance_field_custody_test_artifact_request_check", sql`length(${table.clientRequestId}) BETWEEN 8 AND 120`),
  check("compliance_field_custody_test_artifact_request_hash_check", sql`length(${table.requestSha256}) = 64 AND lower(${table.requestSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_field_custody_test_artifact_hash_check", sql`length(${table.artifactSha256}) = 64 AND lower(${table.artifactSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_field_custody_test_artifact_platform_check", sql`${table.platform} IN ('ios', 'android')`),
  check("compliance_field_custody_test_artifact_build_check", sql`trim(${table.nativeBuildIdentifier}) <> '' AND length(${table.nativeBuildIdentifier}) <= 180`),
  check("compliance_field_custody_test_artifact_build_hash_check", sql`length(${table.nativeBuildSha256}) = 64 AND lower(${table.nativeBuildSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_field_custody_test_artifact_device_class_check", sql`${table.deviceClass} = 'physical'`),
  check("compliance_field_custody_test_artifact_device_check", sql`trim(${table.deviceModel}) <> '' AND length(${table.deviceModel}) <= 180 AND trim(${table.deviceOsVersion}) <> '' AND length(${table.deviceOsVersion}) <= 120`),
  check("compliance_field_custody_test_artifact_device_hash_check", sql`length(${table.deviceIdentifierSha256}) = 64 AND lower(${table.deviceIdentifierSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_field_custody_test_artifact_links_check", sql`trim(${table.requirementId}) <> '' AND trim(${table.evidenceId}) <> '' AND trim(${table.integrityReceiptId}) <> ''`),
  check("compliance_field_custody_test_artifact_scenario_check", sql`${table.offlineScenario} = 'offline_capture_restore'`),
  check("compliance_field_custody_test_artifact_restore_hash_check", sql`length(${table.restoreSha256}) = 64 AND lower(${table.restoreSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_field_custody_test_artifact_result_check", sql`${table.testResult} IN ('failed', 'passed')`),
  check("compliance_field_custody_test_artifact_actor_check", sql`trim(${table.testerUid}) <> '' AND ${table.createdByUid} = ${table.testerUid}`),
  check("compliance_field_custody_test_artifact_time_check", sql`datetime(${table.testedAt}) IS NOT NULL AND datetime(${table.createdAt}) IS NOT NULL AND datetime(${table.testedAt}) <= datetime(${table.createdAt})`),
]);

export const complianceFieldCustodyAcceptanceRecords = sqliteTable("compliance_field_custody_acceptance_records", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  clientRequestId: text("client_request_id").notNull(),
  requestSha256: text("request_sha256").notNull(),
  platform: text("platform").notNull(),
  nativeBuildIdentifier: text("native_build_identifier").notNull(),
  nativeBuildSha256: text("native_build_sha256").notNull(),
  deviceClass: text("device_class").notNull().default("physical"),
  deviceModel: text("device_model").notNull(),
  deviceOsVersion: text("device_os_version").notNull(),
  deviceIdentifierSha256: text("device_identifier_sha256").notNull(),
  requirementId: text("requirement_id").notNull(),
  evidenceId: text("evidence_id").notNull(),
  integrityReceiptId: text("integrity_receipt_id").notNull(),
  offlineScenario: text("offline_scenario").notNull().default("offline_capture_restore"),
  restoreSha256: text("restore_sha256").notNull().default(""),
  status: text("status").notNull().default("not_run"),
  testArtifactId: text("test_artifact_id").notNull().default(""),
  testArtifactSha256: text("test_artifact_sha256").notNull().default(""),
  testerUid: text("tester_uid").notNull(),
  independentApproverUid: text("independent_approver_uid").notNull(),
  testedAt: text("tested_at").notNull().default(""),
  approvedAt: text("approved_at").notNull().default(""),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_field_custody_acceptance_request_idx").on(table.organisationId, table.clientRequestId),
  index("compliance_field_custody_acceptance_evidence_idx").on(table.organisationId, table.evidenceId, table.createdAt, table.id),
  index("compliance_field_custody_acceptance_status_idx").on(table.organisationId, table.status, table.platform, table.createdAt, table.id),
  check("compliance_field_custody_acceptance_request_check", sql`length(${table.clientRequestId}) BETWEEN 8 AND 120`),
  check("compliance_field_custody_acceptance_request_hash_check", sql`length(${table.requestSha256}) = 64 AND lower(${table.requestSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_field_custody_acceptance_platform_check", sql`${table.platform} IN ('ios', 'android')`),
  check("compliance_field_custody_acceptance_build_check", sql`trim(${table.nativeBuildIdentifier}) <> '' AND length(${table.nativeBuildIdentifier}) <= 180`),
  check("compliance_field_custody_acceptance_build_hash_check", sql`length(${table.nativeBuildSha256}) = 64 AND lower(${table.nativeBuildSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_field_custody_acceptance_device_class_check", sql`${table.deviceClass} = 'physical'`),
  check("compliance_field_custody_acceptance_device_check", sql`trim(${table.deviceModel}) <> '' AND length(${table.deviceModel}) <= 180 AND trim(${table.deviceOsVersion}) <> '' AND length(${table.deviceOsVersion}) <= 120`),
  check("compliance_field_custody_acceptance_device_hash_check", sql`length(${table.deviceIdentifierSha256}) = 64 AND lower(${table.deviceIdentifierSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_field_custody_acceptance_links_check", sql`trim(${table.requirementId}) <> '' AND trim(${table.evidenceId}) <> '' AND trim(${table.integrityReceiptId}) <> ''`),
  check("compliance_field_custody_acceptance_scenario_check", sql`${table.offlineScenario} = 'offline_capture_restore'`),
  check("compliance_field_custody_acceptance_restore_hash_check", sql`${table.restoreSha256} = '' OR (length(${table.restoreSha256}) = 64 AND lower(${table.restoreSha256}) NOT GLOB '*[^0-9a-f]*')`),
  check("compliance_field_custody_acceptance_status_check", sql`${table.status} IN ('not_run', 'blocked', 'failed', 'passed', 'rejected')`),
  check("compliance_field_custody_acceptance_test_artifact_check", sql`length(${table.testArtifactId}) <= 180 AND (${table.testArtifactSha256} = '' OR (length(${table.testArtifactSha256}) = 64 AND lower(${table.testArtifactSha256}) NOT GLOB '*[^0-9a-f]*'))`),
  check("compliance_field_custody_acceptance_actor_check", sql`trim(${table.testerUid}) <> '' AND trim(${table.independentApproverUid}) <> '' AND ${table.testerUid} <> ${table.independentApproverUid} AND trim(${table.createdByUid}) <> ''`),
  check("compliance_field_custody_acceptance_time_check", sql`(${table.testedAt} = '' OR datetime(${table.testedAt}) IS NOT NULL) AND (${table.approvedAt} = '' OR datetime(${table.approvedAt}) IS NOT NULL) AND datetime(${table.createdAt}) IS NOT NULL`),
  check("compliance_field_custody_acceptance_state_check", sql`(${table.status} = 'not_run' AND ${table.restoreSha256} = '' AND ${table.testedAt} = '' AND ${table.approvedAt} = '' AND ${table.testArtifactId} = '' AND ${table.testArtifactSha256} = '') OR (${table.status} = 'blocked' AND length(${table.restoreSha256}) = 64 AND trim(${table.testArtifactId}) <> '' AND length(${table.testArtifactSha256}) = 64 AND datetime(${table.testedAt}) IS NOT NULL AND datetime(${table.testedAt}) <= datetime(${table.createdAt}) AND ${table.approvedAt} = '') OR (${table.status} = 'failed' AND length(${table.restoreSha256}) = 64 AND trim(${table.testArtifactId}) <> '' AND length(${table.testArtifactSha256}) = 64 AND datetime(${table.testedAt}) IS NOT NULL AND datetime(${table.testedAt}) <= datetime(${table.createdAt}) AND ${table.approvedAt} = '') OR (${table.status} IN ('passed', 'rejected') AND length(${table.restoreSha256}) = 64 AND trim(${table.testArtifactId}) <> '' AND length(${table.testArtifactSha256}) = 64 AND datetime(${table.testedAt}) IS NOT NULL AND datetime(${table.approvedAt}) IS NOT NULL AND datetime(${table.approvedAt}) >= datetime(${table.testedAt}) AND datetime(${table.approvedAt}) <= datetime(${table.createdAt}) AND ${table.createdByUid} = ${table.independentApproverUid})`),
]);

export const complianceOperationalLookupImports = sqliteTable("compliance_operational_lookup_imports", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  clientRequestId: text("client_request_id").notNull(),
  lookupKind: text("lookup_kind").notNull(),
  sourceArtifactId: text("source_artifact_id").notNull(),
  sourceArtifactSha256: text("source_artifact_sha256").notNull(),
  sourceArtifactCustodyState: text("source_artifact_custody_state").notNull(),
  sourceTimestamp: text("source_timestamp").notNull(),
  requestSha256: text("request_sha256").notNull(),
  recordsSha256: text("records_sha256").notNull(),
  recordCount: integer("record_count").notNull(),
  status: text("status").notNull().default("staged_pending"),
  liveVerificationEnabled: integer("live_verification_enabled", { mode: "boolean" }).notNull().default(false),
  eligibilityActivationEnabled: integer("eligibility_activation_enabled", { mode: "boolean" }).notNull().default(false),
  localAssertionEnabled: integer("local_assertion_enabled", { mode: "boolean" }).notNull().default(false),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_operational_lookup_imports_org_request_idx").on(table.organisationId, table.clientRequestId),
  index("compliance_operational_lookup_imports_org_kind_idx").on(table.organisationId, table.lookupKind, table.sourceTimestamp, table.createdAt, table.id),
  index("compliance_operational_lookup_imports_source_idx").on(table.organisationId, table.sourceArtifactId, table.createdAt, table.id),
  check("compliance_operational_lookup_imports_request_check", sql`length(${table.clientRequestId}) BETWEEN 8 AND 120`),
  check("compliance_operational_lookup_imports_kind_check", sql`${table.lookupKind} IN ('participant', 'product', 'licence', 'recall', 'suspension')`),
  check("compliance_operational_lookup_imports_source_hash_check", sql`length(${table.sourceArtifactSha256}) = 64 AND lower(${table.sourceArtifactSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_operational_lookup_imports_source_state_check", sql`${table.sourceArtifactCustodyState} IN ('draft', 'pending_review')`),
  check("compliance_operational_lookup_imports_source_time_check", sql`datetime(${table.sourceTimestamp}) IS NOT NULL`),
  check("compliance_operational_lookup_imports_request_hash_check", sql`length(${table.requestSha256}) = 64 AND lower(${table.requestSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_operational_lookup_imports_records_hash_check", sql`length(${table.recordsSha256}) = 64 AND lower(${table.recordsSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_operational_lookup_imports_count_check", sql`${table.recordCount} > 0 AND ${table.recordCount} <= 1000`),
  check("compliance_operational_lookup_imports_boundary_check", sql`${table.status} = 'staged_pending' AND ${table.liveVerificationEnabled} = 0 AND ${table.eligibilityActivationEnabled} = 0 AND ${table.localAssertionEnabled} = 0`),
  check("compliance_operational_lookup_imports_actor_check", sql`trim(${table.createdByUid}) <> '' AND datetime(${table.createdAt}) IS NOT NULL`),
]);

export const complianceOperationalLookupRecords = sqliteTable("compliance_operational_lookup_records", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  importId: text("import_id").notNull(),
  rowNumber: integer("row_number").notNull(),
  sourceRecordKey: text("source_record_key").notNull(),
  sourceEffectiveFrom: text("source_effective_from").notNull(),
  sourceEffectiveTo: text("source_effective_to").notNull().default(""),
  sourceStatus: text("source_status").notNull(),
  recordJson: text("record_json").notNull(),
  recordSha256: text("record_sha256").notNull(),
  status: text("status").notNull().default("staged_pending"),
  liveVerificationEnabled: integer("live_verification_enabled", { mode: "boolean" }).notNull().default(false),
  eligibilityActivationEnabled: integer("eligibility_activation_enabled", { mode: "boolean" }).notNull().default(false),
  localAssertionEnabled: integer("local_assertion_enabled", { mode: "boolean" }).notNull().default(false),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_operational_lookup_records_import_row_idx").on(table.importId, table.rowNumber),
  uniqueIndex("compliance_operational_lookup_records_source_identity_idx").on(table.importId, table.sourceRecordKey, table.sourceEffectiveFrom, table.sourceEffectiveTo),
  index("compliance_operational_lookup_records_org_import_idx").on(table.organisationId, table.importId, table.rowNumber),
  check("compliance_operational_lookup_records_row_check", sql`${table.rowNumber} > 0 AND ${table.rowNumber} <= 1000`),
  check("compliance_operational_lookup_records_key_check", sql`trim(${table.sourceRecordKey}) <> '' AND length(${table.sourceRecordKey}) <= 240`),
  check("compliance_operational_lookup_records_effective_check", sql`date(${table.sourceEffectiveFrom}) = ${table.sourceEffectiveFrom} AND (${table.sourceEffectiveTo} = '' OR (date(${table.sourceEffectiveTo}) = ${table.sourceEffectiveTo} AND ${table.sourceEffectiveTo} >= ${table.sourceEffectiveFrom}))`),
  check("compliance_operational_lookup_records_status_check", sql`trim(${table.sourceStatus}) <> '' AND length(${table.sourceStatus}) <= 240`),
  check("compliance_operational_lookup_records_json_check", sql`json_valid(${table.recordJson}) AND json_type(${table.recordJson}) = 'object' AND length(${table.recordJson}) <= 65536`),
  check("compliance_operational_lookup_records_hash_check", sql`length(${table.recordSha256}) = 64 AND lower(${table.recordSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_operational_lookup_records_boundary_check", sql`${table.status} = 'staged_pending' AND ${table.liveVerificationEnabled} = 0 AND ${table.eligibilityActivationEnabled} = 0 AND ${table.localAssertionEnabled} = 0`),
  check("compliance_operational_lookup_records_created_check", sql`datetime(${table.createdAt}) IS NOT NULL`),
]);

export const complianceOperationalLookupReviewDecisions = sqliteTable("compliance_operational_lookup_review_decisions", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  importId: text("import_id").notNull(),
  sourceArtifactId: text("source_artifact_id").notNull(),
  sourceArtifactSha256: text("source_artifact_sha256").notNull(),
  recordsSha256: text("records_sha256").notNull(),
  recordCount: integer("record_count").notNull(),
  decision: text("decision").notNull(),
  supersedesDecisionId: text("supersedes_decision_id").notNull().default(""),
  reviewNote: text("review_note").notNull(),
  reviewedByUid: text("reviewed_by_uid").notNull(),
  reviewedAt: text("reviewed_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_lookup_review_initial_idx")
    .on(table.organisationId, table.importId)
    .where(sql`${table.supersedesDecisionId} = ''`),
  uniqueIndex("compliance_lookup_review_supersedes_idx")
    .on(table.supersedesDecisionId)
    .where(sql`${table.supersedesDecisionId} <> ''`),
  index("compliance_lookup_review_import_idx").on(
    table.organisationId,
    table.importId,
    table.reviewedAt,
    table.id,
  ),
  check("compliance_lookup_review_import_check", sql`trim(${table.importId}) <> '' AND trim(${table.sourceArtifactId}) <> ''`),
  check("compliance_lookup_review_source_hash_check", sql`length(${table.sourceArtifactSha256}) = 64 AND lower(${table.sourceArtifactSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_lookup_review_records_hash_check", sql`length(${table.recordsSha256}) = 64 AND lower(${table.recordsSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_lookup_review_count_check", sql`${table.recordCount} > 0 AND ${table.recordCount} <= 1000`),
  check("compliance_lookup_review_decision_check", sql`${table.decision} IN ('approved', 'rejected', 'withdrawn')`),
  check("compliance_lookup_review_transition_shape_check", sql`(${table.decision} IN ('approved', 'rejected') AND ${table.supersedesDecisionId} = '') OR (${table.decision} = 'withdrawn' AND trim(${table.supersedesDecisionId}) <> '')`),
  check("compliance_lookup_review_note_check", sql`trim(${table.reviewNote}) <> '' AND length(${table.reviewNote}) <= 1000`),
  check("compliance_lookup_review_reviewer_check", sql`trim(${table.reviewedByUid}) <> '' AND datetime(${table.reviewedAt}) IS NOT NULL`),
]);

export const complianceLegacyMappingArtifacts = sqliteTable("compliance_legacy_mapping_artifacts", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  legacySystemKey: text("legacy_system_key").notNull(),
  mappingVersion: text("mapping_version").notNull(),
  artifactFormat: text("artifact_format").notNull(),
  objectKey: text("object_key").notNull(),
  artifactSha256: text("artifact_sha256").notNull(),
  authorizationState: text("authorization_state").notNull().default("draft"),
  authorizationBasis: text("authorization_basis").notNull().default(""),
  requestedByUid: text("requested_by_uid").notNull(),
  primaryAuthorizerUid: text("primary_authorizer_uid").notNull().default(""),
  secondaryAuthorizerUid: text("secondary_authorizer_uid").notNull().default(""),
  authorizedAt: text("authorized_at").notNull().default(""),
  withdrawnByUid: text("withdrawn_by_uid").notNull().default(""),
  withdrawnAt: text("withdrawn_at").notNull().default(""),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_legacy_mapping_artifacts_version_idx").on(table.organisationId, table.legacySystemKey, table.mappingVersion),
  index("compliance_legacy_mapping_artifacts_state_idx").on(table.organisationId, table.authorizationState, table.createdAt, table.id),
  check("compliance_legacy_mapping_artifacts_key_check", sql`trim(${table.legacySystemKey}) <> '' AND length(${table.legacySystemKey}) <= 120 AND trim(${table.mappingVersion}) <> '' AND length(${table.mappingVersion}) <= 120`),
  check("compliance_legacy_mapping_artifacts_format_check", sql`${table.artifactFormat} IN ('json', 'csv') AND trim(${table.objectKey}) <> ''`),
  check("compliance_legacy_mapping_artifacts_hash_check", sql`length(${table.artifactSha256}) = 64 AND lower(${table.artifactSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_legacy_mapping_artifacts_state_check", sql`${table.authorizationState} IN ('draft', 'approved', 'withdrawn')`),
  check("compliance_legacy_mapping_artifacts_approval_check", sql`(${table.authorizationState} = 'draft' AND ${table.authorizationBasis} = '' AND ${table.primaryAuthorizerUid} = '' AND ${table.secondaryAuthorizerUid} = '' AND ${table.authorizedAt} = '' AND ${table.withdrawnByUid} = '' AND ${table.withdrawnAt} = '') OR (${table.authorizationState} = 'approved' AND trim(${table.authorizationBasis}) <> '' AND trim(${table.primaryAuthorizerUid}) <> '' AND trim(${table.secondaryAuthorizerUid}) <> '' AND ${table.requestedByUid} <> ${table.primaryAuthorizerUid} AND ${table.requestedByUid} <> ${table.secondaryAuthorizerUid} AND ${table.primaryAuthorizerUid} <> ${table.secondaryAuthorizerUid} AND datetime(${table.authorizedAt}) IS NOT NULL AND ${table.withdrawnByUid} = '' AND ${table.withdrawnAt} = '') OR (${table.authorizationState} = 'withdrawn' AND trim(${table.authorizationBasis}) <> '' AND trim(${table.primaryAuthorizerUid}) <> '' AND trim(${table.secondaryAuthorizerUid}) <> '' AND ${table.requestedByUid} <> ${table.primaryAuthorizerUid} AND ${table.requestedByUid} <> ${table.secondaryAuthorizerUid} AND ${table.primaryAuthorizerUid} <> ${table.secondaryAuthorizerUid} AND datetime(${table.authorizedAt}) IS NOT NULL AND trim(${table.withdrawnByUid}) <> '' AND datetime(${table.withdrawnAt}) IS NOT NULL)`),
  check("compliance_legacy_mapping_artifacts_created_check", sql`trim(${table.requestedByUid}) <> '' AND datetime(${table.createdAt}) IS NOT NULL`),
]);

export const complianceLegacyMappingArtifactPayloads = sqliteTable("compliance_legacy_mapping_artifact_payloads", {
  artifactId: text("artifact_id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  legacySystemKey: text("legacy_system_key").notNull(),
  mappingVersion: text("mapping_version").notNull(),
  contractFormat: text("contract_format").notNull().default("creditex-legacy-field-mapping-v1"),
  canonicalMappingJson: text("canonical_mapping_json").notNull(),
  artifactSha256: text("artifact_sha256").notNull(),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_legacy_mapping_payloads_version_idx")
    .on(table.organisationId, table.legacySystemKey, table.mappingVersion),
  index("compliance_legacy_mapping_payloads_org_time_idx")
    .on(table.organisationId, table.createdAt, table.artifactId),
  check("compliance_legacy_mapping_payloads_key_check", sql`trim(${table.legacySystemKey}) <> '' AND length(${table.legacySystemKey}) <= 120 AND trim(${table.mappingVersion}) <> '' AND length(${table.mappingVersion}) <= 120`),
  check("compliance_legacy_mapping_payloads_contract_check", sql`${table.contractFormat} = 'creditex-legacy-field-mapping-v1'`),
  check("compliance_legacy_mapping_payloads_json_check", sql`json_valid(${table.canonicalMappingJson}) AND json_type(${table.canonicalMappingJson}) = 'object' AND length(${table.canonicalMappingJson}) BETWEEN 2 AND 131072`),
  check("compliance_legacy_mapping_payloads_hash_check", sql`length(${table.artifactSha256}) = 64 AND lower(${table.artifactSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_legacy_mapping_payloads_created_check", sql`trim(${table.createdByUid}) <> '' AND datetime(${table.createdAt}) IS NOT NULL`),
]);

export const complianceLegacyMappingReviewDecisions = sqliteTable("compliance_legacy_mapping_review_decisions", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  artifactId: text("artifact_id").notNull(),
  legacySystemKey: text("legacy_system_key").notNull(),
  mappingVersion: text("mapping_version").notNull(),
  artifactSha256: text("artifact_sha256").notNull(),
  decision: text("decision").notNull(),
  supersedesDecisionId: text("supersedes_decision_id").notNull().default(""),
  reviewNote: text("review_note").notNull(),
  reviewedByUid: text("reviewed_by_uid").notNull(),
  reviewedAt: text("reviewed_at").notNull(),
}, (table) => [
  index("compliance_legacy_mapping_reviews_subject_idx")
    .on(table.organisationId, table.artifactId, table.reviewedAt, table.id),
  index("compliance_legacy_mapping_reviews_state_idx")
    .on(table.organisationId, table.decision, table.reviewedAt, table.id),
  check("compliance_legacy_mapping_reviews_key_check", sql`trim(${table.legacySystemKey}) <> '' AND length(${table.legacySystemKey}) <= 120 AND trim(${table.mappingVersion}) <> '' AND length(${table.mappingVersion}) <= 120`),
  check("compliance_legacy_mapping_reviews_hash_check", sql`length(${table.artifactSha256}) = 64 AND lower(${table.artifactSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_legacy_mapping_reviews_decision_check", sql`${table.decision} IN ('approved', 'rejected', 'withdrawn')`),
  check("compliance_legacy_mapping_reviews_transition_check", sql`(${table.decision} IN ('approved', 'rejected') AND ${table.supersedesDecisionId} = '') OR (${table.decision} = 'withdrawn' AND trim(${table.supersedesDecisionId}) <> '')`),
  check("compliance_legacy_mapping_reviews_note_check", sql`trim(${table.reviewNote}) <> '' AND length(${table.reviewNote}) <= 1000`),
  check("compliance_legacy_mapping_reviews_reviewer_check", sql`trim(${table.reviewedByUid}) <> '' AND datetime(${table.reviewedAt}) IS NOT NULL`),
]);

export const complianceCalculatorEngineReceipts = sqliteTable("compliance_calculator_engine_receipts", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  calculatorVersionId: text("calculator_version_id").notNull(),
  calculatorVersionNumber: integer("calculator_version_number").notNull(),
  engineContractId: text("engine_contract_id").notNull(),
  engineContractHash: text("engine_contract_hash").notNull(),
  goldenVectorSuiteSha256: text("golden_vector_suite_sha256").notNull(),
  engineSuiteHash: text("engine_suite_hash").notNull(),
  suiteReceiptSchema: text("suite_receipt_schema").notNull(),
  suiteReceiptHash: text("suite_receipt_hash").notNull(),
  vectorCount: integer("vector_count").notNull(),
  result: text("result").notNull(),
  executedByUid: text("executed_by_uid").notNull(),
  executedAt: text("executed_at").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_calculator_engine_receipts_exact_idx").on(
    table.organisationId,
    table.calculatorVersionId,
    table.engineContractHash,
    table.goldenVectorSuiteSha256,
    table.engineSuiteHash,
    table.suiteReceiptHash,
  ),
  index("compliance_calculator_engine_receipts_calculator_idx").on(
    table.organisationId,
    table.calculatorVersionId,
    table.executedAt,
    table.id,
  ),
  check("compliance_calculator_engine_receipts_calculator_check", sql`${table.calculatorVersionNumber} > 0`),
  check("compliance_calculator_engine_receipts_contract_check", sql`${table.engineContractId} = 'creditex-fixed-decimal-engine-contract/base10-strings-v2' AND ${table.suiteReceiptSchema} = 'creditex-calculator-suite-receipt/v2'`),
  check("compliance_calculator_engine_receipts_engine_hash_check", sql`length(${table.engineContractHash}) = 71 AND ${table.engineContractHash} GLOB 'sha256:*' AND lower(substr(${table.engineContractHash}, 8)) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_calculator_engine_receipts_suite_check", sql`length(${table.goldenVectorSuiteSha256}) = 64 AND lower(${table.goldenVectorSuiteSha256}) NOT GLOB '*[^0-9a-f]*' AND length(${table.engineSuiteHash}) = 71 AND ${table.engineSuiteHash} GLOB 'sha256:*' AND lower(substr(${table.engineSuiteHash}, 8)) NOT GLOB '*[^0-9a-f]*' AND length(${table.suiteReceiptHash}) = 71 AND ${table.suiteReceiptHash} GLOB 'sha256:*' AND lower(substr(${table.suiteReceiptHash}, 8)) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_calculator_engine_receipts_result_check", sql`${table.vectorCount} > 0 AND ${table.vectorCount} <= 500 AND ${table.result} = 'passed'`),
  check("compliance_calculator_engine_receipts_actor_check", sql`trim(${table.executedByUid}) <> '' AND datetime(${table.executedAt}) IS NOT NULL AND datetime(${table.createdAt}) IS NOT NULL`),
]);

export const complianceParallelReconciliationRuns = sqliteTable("compliance_parallel_reconciliation_runs", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  clientRequestId: text("client_request_id").notNull(),
  requestSha256: text("request_sha256").notNull(),
  activityVersionId: text("activity_version_id").notNull(),
  activityVersionNumber: integer("activity_version_number").notNull(),
  activityPublicationSnapshotSha256: text("activity_publication_snapshot_sha256").notNull(),
  calculatorVersionId: text("calculator_version_id").notNull(),
  calculatorVersionNumber: integer("calculator_version_number").notNull(),
  calculatorOfficialSourceSha256: text("calculator_official_source_sha256").notNull(),
  goldenVectorStatus: text("golden_vector_status").notNull(),
  goldenVectorCount: integer("golden_vector_count").notNull(),
  goldenVectorSuiteSha256: text("golden_vector_suite_sha256").notNull(),
  calculatorEngineReceiptId: text("calculator_engine_receipt_id").notNull().default(""),
  calculatorEngineContractHash: text("calculator_engine_contract_hash").notNull().default(""),
  calculatorSuiteReceiptHash: text("calculator_suite_receipt_hash").notNull().default(""),
  mappingArtifactId: text("mapping_artifact_id").notNull(),
  mappingVersion: text("mapping_version").notNull(),
  mappingArtifactSha256: text("mapping_artifact_sha256").notNull(),
  comparisonScope: text("comparison_scope").notNull().default("verified_output_hash_vs_manual_reference_non_evidentiary"),
  referenceOrigin: text("reference_origin").notNull().default("caller_supplied"),
  referenceScope: text("reference_scope").notNull().default("manual_reference_non_evidentiary"),
  status: text("status").notNull().default("dry_run_completed"),
  rowCount: integer("row_count").notNull(),
  matchedCount: integer("matched_count").notNull(),
  mismatchedCount: integer("mismatched_count").notNull(),
  externalSubmissionEnabled: integer("external_submission_enabled", { mode: "boolean" }).notNull().default(false),
  certificateCreationEnabled: integer("certificate_creation_enabled", { mode: "boolean" }).notNull().default(false),
  runByUid: text("run_by_uid").notNull(),
  runAt: text("run_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_parallel_reconciliation_runs_org_request_idx").on(table.organisationId, table.clientRequestId),
  index("compliance_parallel_reconciliation_runs_org_time_idx").on(table.organisationId, table.runAt, table.id),
  index("compliance_parallel_reconciliation_runs_governed_inputs_idx").on(table.organisationId, table.activityVersionId, table.calculatorVersionId, table.mappingArtifactId, table.runAt),
  check("compliance_parallel_reconciliation_runs_request_check", sql`length(${table.clientRequestId}) BETWEEN 8 AND 120 AND length(${table.requestSha256}) = 64 AND lower(${table.requestSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_parallel_reconciliation_runs_activity_check", sql`${table.activityVersionNumber} > 0 AND length(${table.activityPublicationSnapshotSha256}) = 64 AND lower(${table.activityPublicationSnapshotSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_parallel_reconciliation_runs_calculator_check", sql`${table.calculatorVersionNumber} > 0 AND length(${table.calculatorOfficialSourceSha256}) = 64 AND lower(${table.calculatorOfficialSourceSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_parallel_reconciliation_runs_vectors_check", sql`${table.goldenVectorStatus} = 'passed' AND ${table.goldenVectorCount} > 0 AND length(${table.goldenVectorSuiteSha256}) = 64 AND lower(${table.goldenVectorSuiteSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_parallel_reconciliation_runs_mapping_check", sql`trim(${table.mappingVersion}) <> '' AND length(${table.mappingArtifactSha256}) = 64 AND lower(${table.mappingArtifactSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_parallel_reconciliation_runs_engine_check", sql`(${table.calculatorEngineContractHash} = '' OR (length(${table.calculatorEngineContractHash}) = 71 AND ${table.calculatorEngineContractHash} GLOB 'sha256:*' AND lower(substr(${table.calculatorEngineContractHash}, 8)) NOT GLOB '*[^0-9a-f]*')) AND (${table.calculatorSuiteReceiptHash} = '' OR (length(${table.calculatorSuiteReceiptHash}) = 71 AND ${table.calculatorSuiteReceiptHash} GLOB 'sha256:*' AND lower(substr(${table.calculatorSuiteReceiptHash}, 8)) NOT GLOB '*[^0-9a-f]*'))`),
  check("compliance_parallel_reconciliation_runs_boundary_check", sql`${table.comparisonScope} IN ('verified_output_hash_vs_manual_reference_non_evidentiary', 'verified_output_hash_vs_dataforce_staged_row_non_evidentiary') AND ${table.status} = 'dry_run_completed' AND ${table.externalSubmissionEnabled} = 0 AND ${table.certificateCreationEnabled} = 0`),
  check("compliance_parallel_reconciliation_runs_reference_check", sql`(${table.comparisonScope} = 'verified_output_hash_vs_manual_reference_non_evidentiary' AND ${table.referenceOrigin} = 'caller_supplied' AND ${table.referenceScope} = 'manual_reference_non_evidentiary') OR (${table.comparisonScope} = 'verified_output_hash_vs_dataforce_staged_row_non_evidentiary' AND ${table.referenceOrigin} = 'dataforce_staged_row' AND ${table.referenceScope} = 'dataforce_certificate_quantity_non_evidentiary' AND trim(${table.calculatorEngineReceiptId}) <> '' AND trim(${table.calculatorEngineContractHash}) <> '' AND trim(${table.calculatorSuiteReceiptHash}) <> '')`),
  check("compliance_parallel_reconciliation_runs_count_check", sql`${table.rowCount} > 0 AND ${table.rowCount} <= 250 AND ${table.matchedCount} >= 0 AND ${table.mismatchedCount} >= 0 AND ${table.rowCount} = ${table.matchedCount} + ${table.mismatchedCount}`),
  check("compliance_parallel_reconciliation_runs_actor_check", sql`trim(${table.runByUid}) <> '' AND datetime(${table.runAt}) IS NOT NULL`),
]);

export const complianceParallelReconciliationRows = sqliteTable("compliance_parallel_reconciliation_rows", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  runId: text("run_id").notNull(),
  rowNumber: integer("row_number").notNull(),
  caseId: text("case_id").notNull(),
  caseRevision: integer("case_revision").notNull(),
  caseSnapshotSha256: text("case_snapshot_sha256").notNull(),
  calculationRunId: text("calculation_run_id").notNull(),
  inputSha256: text("input_sha256").notNull(),
  outputSha256: text("output_sha256").notNull(),
  referenceSha256: text("reference_sha256").notNull(),
  result: text("result").notNull(),
  externalSubmissionEnabled: integer("external_submission_enabled", { mode: "boolean" }).notNull().default(false),
  certificateCreationEnabled: integer("certificate_creation_enabled", { mode: "boolean" }).notNull().default(false),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_parallel_reconciliation_rows_run_row_idx").on(table.runId, table.rowNumber),
  uniqueIndex("compliance_parallel_reconciliation_rows_run_case_idx").on(table.runId, table.caseId),
  index("compliance_parallel_reconciliation_rows_org_result_idx").on(table.organisationId, table.runId, table.result, table.rowNumber),
  check("compliance_parallel_reconciliation_rows_row_check", sql`${table.rowNumber} > 0 AND ${table.rowNumber} <= 250 AND ${table.caseRevision} > 0`),
  check("compliance_parallel_reconciliation_rows_case_hash_check", sql`length(${table.caseSnapshotSha256}) = 64 AND lower(${table.caseSnapshotSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_parallel_reconciliation_rows_input_hash_check", sql`length(${table.inputSha256}) = 64 AND lower(${table.inputSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_parallel_reconciliation_rows_output_hash_check", sql`length(${table.outputSha256}) = 64 AND lower(${table.outputSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_parallel_reconciliation_rows_reference_hash_check", sql`length(${table.referenceSha256}) = 64 AND lower(${table.referenceSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_parallel_reconciliation_rows_result_check", sql`(${table.result} = 'matched' AND ${table.outputSha256} = ${table.referenceSha256}) OR (${table.result} = 'mismatched' AND ${table.outputSha256} <> ${table.referenceSha256})`),
  check("compliance_parallel_reconciliation_rows_boundary_check", sql`${table.externalSubmissionEnabled} = 0 AND ${table.certificateCreationEnabled} = 0 AND datetime(${table.createdAt}) IS NOT NULL`),
]);

export const complianceParallelReferenceBindings = sqliteTable("compliance_parallel_reference_bindings", {
  id: text("id").primaryKey(),
  organisationId: text("organisation_id").notNull(),
  runId: text("run_id").notNull(),
  parallelRowId: text("parallel_row_id").notNull(),
  mappingArtifactId: text("mapping_artifact_id").notNull(),
  mappingVersion: text("mapping_version").notNull(),
  mappingArtifactSha256: text("mapping_artifact_sha256").notNull(),
  transformationContract: text("transformation_contract").notNull().default("dataforce-jobs-v1:certificate-quantity-v1"),
  legacyBatchId: text("legacy_batch_id").notNull(),
  legacyBatchContentSha256: text("legacy_batch_content_sha256").notNull(),
  legacyImportRowId: text("legacy_import_row_id").notNull(),
  legacyRowNumber: integer("legacy_row_number").notNull(),
  legacyRowSha256: text("legacy_row_sha256").notNull(),
  dataforceAppId: text("dataforce_app_id").notNull().default(""),
  dataforceJobId: text("dataforce_job_id").notNull(),
  tlinkCaseId: text("tlink_case_id").notNull(),
  tlinkWorkOrderId: text("tlink_work_order_id").notNull(),
  tlinkWorkNumber: text("tlink_work_number").notNull(),
  identityMatchBasis: text("identity_match_basis").notNull(),
  referenceSnapshot: text("reference_snapshot").notNull(),
  referenceSha256: text("reference_sha256").notNull(),
  evidenceUse: text("evidence_use").notNull().default("non_evidentiary"),
  externalSubmissionEnabled: integer("external_submission_enabled", { mode: "boolean" }).notNull().default(false),
  certificateCreationEnabled: integer("certificate_creation_enabled", { mode: "boolean" }).notNull().default(false),
  createdByUid: text("created_by_uid").notNull(),
  createdAt: text("created_at").notNull(),
}, (table) => [
  uniqueIndex("compliance_parallel_reference_bindings_run_row_idx").on(table.runId, table.parallelRowId),
  uniqueIndex("compliance_parallel_reference_bindings_run_legacy_idx").on(table.runId, table.legacyImportRowId),
  index("compliance_parallel_reference_bindings_org_source_idx").on(table.organisationId, table.legacyBatchId, table.legacyImportRowId),
  check("compliance_parallel_reference_bindings_mapping_check", sql`trim(${table.mappingVersion}) <> '' AND length(${table.mappingArtifactSha256}) = 64 AND lower(${table.mappingArtifactSha256}) NOT GLOB '*[^0-9a-f]*' AND ${table.transformationContract} = 'dataforce-jobs-v1:certificate-quantity-v1'`),
  check("compliance_parallel_reference_bindings_source_hash_check", sql`length(${table.legacyBatchContentSha256}) = 64 AND lower(${table.legacyBatchContentSha256}) NOT GLOB '*[^0-9a-f]*' AND ${table.legacyRowNumber} > 1 AND length(${table.legacyRowSha256}) = 64 AND lower(${table.legacyRowSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_parallel_reference_bindings_identity_check", sql`trim(${table.dataforceJobId}) <> '' AND trim(${table.tlinkCaseId}) <> '' AND trim(${table.tlinkWorkOrderId}) <> '' AND trim(${table.tlinkWorkNumber}) <> '' AND ${table.identityMatchBasis} IN ('job_id', 'app_id_and_job_id')`),
  check("compliance_parallel_reference_bindings_reference_check", sql`json_valid(${table.referenceSnapshot}) AND json_type(${table.referenceSnapshot}, '$.certificateQuantity') IN ('integer', 'real') AND length(${table.referenceSha256}) = 64 AND lower(${table.referenceSha256}) NOT GLOB '*[^0-9a-f]*'`),
  check("compliance_parallel_reference_bindings_boundary_check", sql`${table.evidenceUse} = 'non_evidentiary' AND ${table.externalSubmissionEnabled} = 0 AND ${table.certificateCreationEnabled} = 0 AND trim(${table.createdByUid}) <> '' AND datetime(${table.createdAt}) IS NOT NULL`),
]);
