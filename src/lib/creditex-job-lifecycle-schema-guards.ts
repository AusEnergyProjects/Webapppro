// Sites migrations split on semicolons. Keep trigger bodies intact in D1 prepared statements.
import { JOB_DELETION_SCHEMA_GUARDS } from "./trade-job-deletion-schema-guards.ts";

export const CREDITEX_JOB_LIFECYCLE_SCHEMA_GUARD_DEFINITIONS: readonly Readonly<{ name: string; sql: string }>[] = Object.freeze([
  Object.freeze({"name":"creditex_registry_batch_item_ready_insert","sql":"CREATE TRIGGER IF NOT EXISTS creditex_registry_batch_item_ready_insert\nBEFORE INSERT ON creditex_registry_batch_items\nBEGIN\n  SELECT CASE WHEN NOT EXISTS (\n    SELECT 1 FROM compliance_output_action_packets packet\n    JOIN compliance_output_action_reviews review ON review.organisation_id=packet.organisation_id\n      AND review.packet_id=packet.id AND review.decision='approved' AND review.packet_sha256=packet.packet_sha256\n    JOIN creditex_registry_claim_accounts binding ON binding.organisation_id=packet.organisation_id\n      AND binding.packet_id=packet.id AND binding.account_id=NEW.account_id AND binding.packet_sha256=packet.packet_sha256\n    JOIN creditex_registry_accounts account ON account.organisation_id=binding.organisation_id AND account.id=binding.account_id\n    JOIN creditex_registry_batches batch ON batch.organisation_id=NEW.organisation_id AND batch.id=NEW.batch_id\n    JOIN compliance_cases compliance_case ON compliance_case.organisation_id=packet.organisation_id AND compliance_case.id=packet.compliance_case_id\n    JOIN trade_work_orders work ON work.id=compliance_case.work_order_id AND work.firebase_uid=compliance_case.installer_uid\n    WHERE packet.organisation_id=NEW.organisation_id AND packet.id=NEW.packet_id AND packet.packet_sha256=NEW.packet_sha256\n      AND account.scheme=NEW.scheme AND account.version=NEW.account_version AND account.enabled=1\n      AND work.record_status='active' AND work.stage<>'cancelled'\n      AND (account.authority_expires_on='' OR account.authority_expires_on>=substr(batch.created_at,1,10))\n      AND EXISTS(SELECT 1 FROM json_each(account.activity_scope) WHERE value=packet.activity_template_id)\n      AND NOT EXISTS(SELECT 1 FROM compliance_output_action_events event WHERE event.organisation_id=packet.organisation_id\n        AND event.packet_id=packet.id AND event.to_status<>'prepared')\n      AND NOT EXISTS(SELECT 1 FROM compliance_output_dispatch_intents intent WHERE intent.organisation_id=packet.organisation_id AND intent.packet_id=packet.id)\n  ) THEN RAISE(ABORT,'REGISTRY_BATCH_CLAIM_CHANGED') END;\n  SELECT CASE WHEN NEW.scheme IN ('nsw_esc','nsw_prc','stc') AND NOT EXISTS (\n    SELECT 1 FROM creditex_registry_exports output\n    JOIN creditex_registry_export_reviews review ON review.organisation_id=output.organisation_id AND review.export_id=output.id AND review.decision='approved'\n    WHERE output.organisation_id=NEW.organisation_id AND output.id=NEW.export_id AND output.account_id=NEW.account_id\n      AND output.account_version=NEW.account_version\n      AND EXISTS(SELECT 1 FROM json_each(output.packet_ids) WHERE value=NEW.packet_id)\n  ) THEN RAISE(ABORT,'REGISTRY_BATCH_APPROVED_FILE_REQUIRED') END;\nEND;"}),
  Object.freeze({"name":"creditex_job_lifecycle_scope_guard","sql":"CREATE TRIGGER IF NOT EXISTS creditex_job_lifecycle_scope_guard BEFORE INSERT ON creditex_job_lifecycle_events\nWHEN NOT EXISTS (SELECT 1 FROM trade_work_orders w JOIN trade_work_order_compliance_intents i\n  ON i.work_order_id=w.id AND i.installer_uid=w.firebase_uid\n  WHERE w.id=NEW.work_order_id AND w.firebase_uid=NEW.owner_uid\n    AND i.compliance_organisation_id=NEW.organisation_id\n    AND (NEW.intent_id='' OR i.id=NEW.intent_id))\nBEGIN SELECT RAISE(ABORT,'JOB_LIFECYCLE_SCOPE_INVALID'); END;"}),
  Object.freeze({"name":"creditex_job_lifecycle_no_update","sql":"CREATE TRIGGER IF NOT EXISTS creditex_job_lifecycle_no_update BEFORE UPDATE ON creditex_job_lifecycle_events\nBEGIN SELECT RAISE(ABORT,'JOB_LIFECYCLE_HISTORY_IMMUTABLE'); END;"}),
  Object.freeze({"name":"creditex_job_lifecycle_no_delete","sql":"CREATE TRIGGER IF NOT EXISTS creditex_job_lifecycle_no_delete BEFORE DELETE ON creditex_job_lifecycle_events\nBEGIN SELECT RAISE(ABORT,'JOB_LIFECYCLE_HISTORY_RETAINED'); END;"}),
  Object.freeze({"name":"creditex_job_correction_delivery_identity_guard","sql":"CREATE TRIGGER IF NOT EXISTS creditex_job_correction_delivery_identity_guard BEFORE UPDATE ON creditex_job_correction_deliveries\nWHEN NEW.event_id<>OLD.event_id OR NEW.work_order_id<>OLD.work_order_id OR NEW.owner_uid<>OLD.owner_uid OR NEW.member_id<>OLD.member_id\n  OR NEW.recipient_email<>OLD.recipient_email OR NEW.subject<>OLD.subject OR NEW.body<>OLD.body OR NEW.created_at<>OLD.created_at\nBEGIN SELECT RAISE(ABORT,'JOB_CORRECTION_DELIVERY_IDENTITY_IMMUTABLE'); END;"}),
  Object.freeze({"name":"creditex_job_correction_delivery_no_delete","sql":"CREATE TRIGGER IF NOT EXISTS creditex_job_correction_delivery_no_delete BEFORE DELETE ON creditex_job_correction_deliveries\nBEGIN SELECT RAISE(ABORT,'JOB_CORRECTION_DELIVERY_RETAINED'); END;"}),
  Object.freeze({"name":"creditex_job_correction_delivery_scope_guard","sql":"CREATE TRIGGER IF NOT EXISTS creditex_job_correction_delivery_scope_guard BEFORE INSERT ON creditex_job_correction_deliveries\nWHEN NOT EXISTS (SELECT 1 FROM creditex_job_lifecycle_events e WHERE e.id=NEW.event_id AND e.action='correction_required'\n  AND e.work_order_id=NEW.work_order_id AND e.owner_uid=NEW.owner_uid)\nBEGIN SELECT RAISE(ABORT,'JOB_CORRECTION_DELIVERY_SCOPE_INVALID'); END;"}),
  Object.freeze({"name":"creditex_completed_workpack_correction_guard","sql":"CREATE TRIGGER IF NOT EXISTS creditex_completed_workpack_correction_guard BEFORE INSERT ON compliance_activity_work_pack_instances\nWHEN EXISTS (SELECT 1 FROM compliance_activity_work_pack_instances prior WHERE prior.id=NEW.supersedes_instance_id AND prior.status='completed')\nAND (NEW.status<>'in_progress' OR NOT EXISTS (\n  SELECT 1 FROM creditex_job_lifecycle_events e,json_each(e.source_snapshot,'$.records') source\n  WHERE e.action='correction_required' AND e.organisation_id=NEW.organisation_id AND e.work_order_id=NEW.work_order_id\n    AND e.intent_id=NEW.compliance_intent_id AND e.actor_uid=NEW.created_by_uid AND e.created_at=NEW.created_at\n    AND json_extract(source.value,'$.kind')='pack' AND json_extract(source.value,'$.id')=NEW.supersedes_instance_id\n    AND json_extract(source.value,'$.revision')=NEW.revision-1))\nBEGIN SELECT RAISE(ABORT,'COMPLETED_WORKPACK_CORRECTION_REQUIRED'); END;"}),
  Object.freeze({"name":"creditex_registry_batch_business_review_guard","sql":"CREATE TRIGGER IF NOT EXISTS creditex_registry_batch_business_review_guard BEFORE INSERT ON creditex_registry_batch_items\nWHEN NOT EXISTS (SELECT 1 FROM compliance_output_action_packets packet\n JOIN compliance_cases c ON c.id=packet.compliance_case_id AND c.organisation_id=packet.organisation_id\n JOIN trade_work_order_compliance_intents intent ON intent.id=c.compliance_intent_id\n   AND intent.compliance_organisation_id=c.organisation_id AND intent.work_order_id=c.work_order_id AND intent.installer_uid=c.installer_uid\n JOIN creditex_job_lifecycle_events review ON review.intent_id=intent.id AND review.organisation_id=intent.compliance_organisation_id\n   AND review.work_order_id=intent.work_order_id AND review.owner_uid=intent.installer_uid\n WHERE packet.id=NEW.packet_id AND packet.organisation_id=NEW.organisation_id\n   AND c.status<>'changes_requested' AND c.evidence_status<>'changes_required'\n   AND review.action='reviewed' AND review.actor_kind='trade' AND review.source_snapshot=json_object('intent', intent.id, 'intentRevision', intent.revision,\n    'intentSha256', intent.intent_snapshot_sha256,\n    'records', json((SELECT COALESCE(json_group_array(json(record)), '[]') FROM (\n      SELECT json_object('kind','field','id',f.id,'revision',f.revision,'status',f.status,\n        'sha256',f.pdf_sha256,'objectKey',f.pdf_object_key) record, 'field:' || f.id sort_key\n      FROM trade_activity_field_records f WHERE f.intent_id=intent.id\n        AND f.work_order_id=intent.work_order_id AND f.owner_uid=intent.installer_uid\n        AND f.organisation_id=intent.compliance_organisation_id\n        AND NOT EXISTS (SELECT 1 FROM trade_activity_field_records successor WHERE successor.supersedes_record_id=f.id)\n      UNION ALL\n      SELECT json_object('kind','pack','id',p.id,'revision',p.revision,'status',p.status,\n        'finals',json((SELECT COALESCE(json_group_array(json(final)), '[]') FROM (\n          SELECT json_object('id',r.id,'sha256',r.pdf_sha256,'objectKey',r.object_key) final\n          FROM compliance_activity_work_pack_final_records r\n          WHERE r.case_instance_id=p.id AND r.organisation_id=p.organisation_id ORDER BY r.id)))) record,\n        'pack:' || p.id sort_key\n      FROM compliance_activity_work_pack_instances p\n      WHERE p.compliance_intent_id=intent.id AND p.work_order_id=intent.work_order_id\n        AND p.organisation_id=intent.compliance_organisation_id\n        AND NOT EXISTS (SELECT 1 FROM compliance_activity_work_pack_instances newer\n          WHERE newer.organisation_id=p.organisation_id AND newer.instance_key=p.instance_key AND newer.revision>p.revision)\n      ORDER BY sort_key))) )\n   AND NOT EXISTS (SELECT 1 FROM creditex_job_lifecycle_events newer WHERE newer.organisation_id=review.organisation_id\n     AND newer.intent_id=review.intent_id AND newer.work_order_id=review.work_order_id AND newer.owner_uid=review.owner_uid\n     AND newer.action IN ('reviewed','correction_required')\n     AND (newer.created_at>review.created_at OR (newer.created_at=review.created_at AND newer.id>review.id))))\nBEGIN SELECT RAISE(ABORT,'REGISTRY_BATCH_BUSINESS_REVIEW_REQUIRED'); END;"}),
  Object.freeze({"name":"trade_job_cancel_completion_history_guard","sql":"CREATE TRIGGER IF NOT EXISTS trade_job_cancel_completion_history_guard BEFORE UPDATE OF stage ON trade_work_orders\nWHEN NEW.stage='cancelled' AND OLD.stage<>'cancelled' AND (old.stage='completed'\n    OR EXISTS (SELECT 1 FROM trade_crm_job_details d WHERE d.work_order_id=old.id\n      AND d.firebase_uid=old.firebase_uid AND d.pipeline_stage IN ('complete','invoiced','paid'))\n    OR EXISTS (SELECT 1 FROM trade_activity_field_records f WHERE f.work_order_id=old.id\n      AND f.owner_uid=old.firebase_uid AND (f.status='submitted_for_creditex_review' OR f.submitted_at<>''))\n    OR EXISTS (SELECT 1 FROM trade_activity_field_record_versions v JOIN trade_activity_field_records f ON f.id=v.record_id\n      WHERE f.work_order_id=old.id AND f.owner_uid=old.firebase_uid\n        AND json_extract(v.payload,'$.status')='submitted_for_creditex_review')\n    OR EXISTS (SELECT 1 FROM compliance_activity_work_pack_instances p\n      JOIN trade_work_order_compliance_intents i ON i.id=p.compliance_intent_id\n        AND i.compliance_organisation_id=p.organisation_id\n      WHERE p.work_order_id=old.id AND i.installer_uid=old.firebase_uid\n        AND (p.status='completed' OR EXISTS (SELECT 1 FROM compliance_activity_work_pack_final_records r\n          WHERE r.case_instance_id=p.id AND r.organisation_id=p.organisation_id)))\n    OR EXISTS (SELECT 1 FROM trade_job_forms f WHERE f.work_order_id=old.id AND f.firebase_uid=old.firebase_uid AND f.status='complete')\n    OR EXISTS (SELECT 1 FROM trade_work_order_events e WHERE e.work_order_id=old.id AND e.firebase_uid=old.firebase_uid\n      AND (e.event_type IN ('completed','job_completed') OR (e.event_type='stage_changed' AND lower(e.summary) LIKE '%completed%'))))\nBEGIN SELECT RAISE(ABORT,'JOB_CANCEL_COMPLETED'); END;"}),
  Object.freeze({"name":"trade_activity_field_record_intent_guard","sql":"CREATE TRIGGER IF NOT EXISTS trade_activity_field_record_intent_guard BEFORE INSERT ON trade_activity_field_records\nWHEN NOT EXISTS(SELECT 1 FROM trade_work_order_compliance_intents i WHERE i.id=NEW.intent_id AND i.work_order_id=NEW.work_order_id\n  AND i.installer_uid=NEW.owner_uid AND i.compliance_organisation_id=NEW.organisation_id AND i.activity_template_id=NEW.activity_template_id\n  AND i.status IN ('planned','case_linked'))\nBEGIN SELECT RAISE(ABORT,'Activity field record must match its active assigned intent.'); END;"}),
  Object.freeze({"name":"trade_activity_field_correction_guard","sql":"CREATE TRIGGER IF NOT EXISTS trade_activity_field_correction_guard BEFORE INSERT ON trade_activity_field_records\nWHEN NEW.supersedes_record_id IS NOT NULL\nBEGIN\n  SELECT CASE WHEN NEW.status<>'draft' OR NEW.revision<>1 OR NEW.pdf_object_key<>'' OR NEW.pdf_sha256<>'' OR NEW.submitted_at<>''\n    OR COALESCE(json_type(NEW.payload,'$.signatures')='array' AND json_array_length(NEW.payload,'$.signatures')=0,0)=0\n    THEN RAISE(ABORT,'ACTIVITY_CORRECTION_MUST_START_UNSIGNED') END;\n  SELECT CASE WHEN NOT EXISTS(SELECT 1 FROM trade_activity_field_records source\n    JOIN creditex_job_lifecycle_events event ON event.id=NEW.correction_event_id AND event.action='correction_required'\n      AND event.organisation_id=source.organisation_id AND event.owner_uid=source.owner_uid\n      AND event.work_order_id=source.work_order_id AND event.intent_id=source.intent_id\n    JOIN trade_work_orders work ON work.id=source.work_order_id AND work.firebase_uid=source.owner_uid\n    WHERE source.id=NEW.supersedes_record_id AND source.status='submitted_for_creditex_review'\n      AND source.intent_id=NEW.intent_id AND source.owner_uid=NEW.owner_uid AND source.work_order_id=NEW.work_order_id\n      AND source.organisation_id=NEW.organisation_id AND source.activity_template_id=NEW.activity_template_id\n      AND work.record_status='active' AND work.stage<>'cancelled'\n      AND json_extract(NEW.payload,'$.correction.sourceRecordId')=source.id\n      AND json_extract(NEW.payload,'$.correction.sourceRevision')=source.revision\n      AND json_extract(NEW.payload,'$.correction.eventId')=event.id\n      AND NOT EXISTS(SELECT 1 FROM creditex_job_lifecycle_events later\n        WHERE later.organisation_id=event.organisation_id AND later.owner_uid=event.owner_uid\n          AND later.work_order_id=event.work_order_id AND later.intent_id=event.intent_id\n          AND later.action IN ('reviewed','correction_required')\n          AND (later.created_at>event.created_at OR (later.created_at=event.created_at AND later.id>event.id)))\n      AND EXISTS(SELECT 1 FROM json_each(event.source_snapshot,'$.records') retained\n        WHERE json_extract(retained.value,'$.kind')='field' AND json_extract(retained.value,'$.id')=source.id\n          AND json_extract(retained.value,'$.revision')=source.revision AND json_extract(retained.value,'$.sha256')=source.pdf_sha256\n          AND json_extract(retained.value,'$.objectKey')=source.pdf_object_key)\n  ) THEN RAISE(ABORT,'ACTIVITY_CORRECTION_SOURCE_CHANGED') END;\nEND;"}),
  Object.freeze({"name":"trade_activity_field_record_immutable","sql":"CREATE TRIGGER IF NOT EXISTS trade_activity_field_record_immutable BEFORE UPDATE ON trade_activity_field_records\nWHEN OLD.status='submitted_for_creditex_review' OR NEW.intent_id<>OLD.intent_id OR NEW.owner_uid<>OLD.owner_uid\n  OR NEW.organisation_id<>OLD.organisation_id OR NEW.work_order_id<>OLD.work_order_id\n  OR NEW.activity_template_id<>OLD.activity_template_id OR NEW.revision<>OLD.revision+1\n  OR NEW.supersedes_record_id IS NOT OLD.supersedes_record_id OR NEW.correction_event_id IS NOT OLD.correction_event_id\n  OR json_extract(NEW.payload,'$.correction') IS NOT json_extract(OLD.payload,'$.correction')\nBEGIN SELECT RAISE(ABORT,'Submitted field records and their scope are immutable.'); END;"}),
  Object.freeze({"name":"trade_activity_field_record_no_delete","sql":"CREATE TRIGGER IF NOT EXISTS trade_activity_field_record_no_delete BEFORE DELETE ON trade_activity_field_records\nBEGIN SELECT RAISE(ABORT, 'Field record history must be retained.'); END;"}),
  Object.freeze({"name":"trade_activity_field_record_created","sql":"CREATE TRIGGER IF NOT EXISTS trade_activity_field_record_created AFTER INSERT ON trade_activity_field_records\nBEGIN INSERT INTO trade_activity_field_record_versions VALUES(NEW.id,NEW.revision,NEW.payload,NEW.actor_uid,NEW.updated_at); END;"}),
  Object.freeze({"name":"trade_activity_field_record_changed","sql":"CREATE TRIGGER IF NOT EXISTS trade_activity_field_record_changed AFTER UPDATE ON trade_activity_field_records\nBEGIN INSERT INTO trade_activity_field_record_versions VALUES(NEW.id,NEW.revision,NEW.payload,NEW.actor_uid,NEW.updated_at); END;"}),
]);

// Only exact known field guards from migration 0170 may be upgraded. Migration 0194 normally removes them with the old table.
const PREVIOUS_FIELD_GUARD_SQL = new Map<string, string>([
  ["trade_activity_field_record_intent_guard", "CREATE TRIGGER trade_activity_field_record_intent_guard BEFORE INSERT ON trade_activity_field_records\nWHEN NOT EXISTS (SELECT 1 FROM trade_work_order_compliance_intents i\n  WHERE i.id = NEW.intent_id AND i.work_order_id = NEW.work_order_id AND i.installer_uid = NEW.owner_uid\n    AND i.compliance_organisation_id = NEW.organisation_id AND i.activity_template_id = NEW.activity_template_id\n    AND i.status IN ('planned', 'case_linked'))\nBEGIN SELECT RAISE(ABORT, 'Activity field record must match its active assigned intent.'); END;"],
  ["trade_activity_field_record_immutable", "CREATE TRIGGER trade_activity_field_record_immutable BEFORE UPDATE ON trade_activity_field_records\nWHEN OLD.status = 'submitted_for_creditex_review' OR NEW.intent_id <> OLD.intent_id OR NEW.owner_uid <> OLD.owner_uid\n  OR NEW.organisation_id <> OLD.organisation_id OR NEW.work_order_id <> OLD.work_order_id\n  OR NEW.activity_template_id <> OLD.activity_template_id OR NEW.revision <> OLD.revision + 1\nBEGIN SELECT RAISE(ABORT, 'Submitted field records and their scope are immutable.'); END;"],
  ["trade_activity_field_record_no_delete", "CREATE TRIGGER trade_activity_field_record_no_delete BEFORE DELETE ON trade_activity_field_records\nBEGIN SELECT RAISE(ABORT, 'Field record history must be retained.'); END;"],
  ["trade_activity_field_record_created", "CREATE TRIGGER trade_activity_field_record_created AFTER INSERT ON trade_activity_field_records\nBEGIN INSERT INTO trade_activity_field_record_versions VALUES (NEW.id, NEW.revision, NEW.payload, NEW.actor_uid, NEW.updated_at); END;"],
  ["trade_activity_field_record_changed", "CREATE TRIGGER trade_activity_field_record_changed AFTER UPDATE ON trade_activity_field_records\nBEGIN INSERT INTO trade_activity_field_record_versions VALUES (NEW.id, NEW.revision, NEW.payload, NEW.actor_uid, NEW.updated_at); END;"],
]);

const REQUIRED_TABLES = [
  "creditex_registry_batches", "creditex_registry_batch_items", "creditex_job_lifecycle_events",
  "creditex_job_correction_deliveries", "trade_activity_field_records",
] as const;
const readinessByDatabase = new WeakMap<object, Promise<void>>();

export function canonicalCreditexJobLifecycleSchemaGuardSql(sql: string) {
  return sql.trim()
    .replace(/^CREATE\s+TRIGGER\s+(?:IF\s+NOT\s+EXISTS\s+)?/i, "CREATE TRIGGER ")
    .replace(/;\s*$/, "")
    .replace(/('(?:''|[^'])*'|"(?:""|[^"])*"|`(?:``|[^`])*`)|\s+/g, (match, quoted: string | undefined) => quoted ?? " ");
}

async function installedGuards(database: D1Database) {
  const rows = await database.prepare("SELECT name, sql FROM sqlite_schema WHERE type = 'trigger'")
    .all<{ name: string; sql: string | null }>();
  return new Map(rows.results.map(row => [row.name, row.sql || ""]));
}

function matchesGuard(name: string, current: string, expected: string) {
  if (canonicalCreditexJobLifecycleSchemaGuardSql(current) === canonicalCreditexJobLifecycleSchemaGuardSql(expected)) return true;
  // The existing draft-job deletion installer owns this exact stronger guard.
  // Recognise it without replacing it with the original unconditional guard.
  const deletion = JOB_DELETION_SCHEMA_GUARDS.find(guard => guard.name === name);
  return Boolean(deletion && canonicalCreditexJobLifecycleSchemaGuardSql(current) === canonicalCreditexJobLifecycleSchemaGuardSql(deletion.sql));
}

async function installCreditexJobLifecycleSchemaGuards(database: D1Database) {
  const tables = await database.prepare(`SELECT name FROM sqlite_schema WHERE type='table'
    AND name IN (${REQUIRED_TABLES.map(name => `'${name}'`).join(",")})`).all<{ name: string }>();
  const present = new Set(tables.results.map(row => row.name));
  const missingTables = REQUIRED_TABLES.filter(name => !present.has(name));
  if (missingTables.length) throw new Error(`CREDITEX_JOB_LIFECYCLE_MIGRATIONS_REQUIRED:${missingTables.join(",")}`);
  const columns = await database.prepare("PRAGMA table_info(trade_activity_field_records)").all<{ name: string }>();
  for (const name of ["supersedes_record_id", "correction_event_id"]) {
    if (!columns.results.some(column => column.name === name)) throw new Error(`CREDITEX_JOB_LIFECYCLE_MIGRATIONS_REQUIRED:trade_activity_field_records.${name}`);
  }

  const installed = await installedGuards(database);
  const statements: D1PreparedStatement[] = [];
  for (const definition of CREDITEX_JOB_LIFECYCLE_SCHEMA_GUARD_DEFINITIONS) {
    const current = installed.get(definition.name);
    if (current && matchesGuard(definition.name, current, definition.sql)) continue;
    const previous = PREVIOUS_FIELD_GUARD_SQL.get(definition.name);
    if (current && (!previous || canonicalCreditexJobLifecycleSchemaGuardSql(current) !== canonicalCreditexJobLifecycleSchemaGuardSql(previous))) {
      throw new Error(`CREDITEX_JOB_LIFECYCLE_GUARD_MISMATCH:${definition.name}`);
    }
    if (current) statements.push(database.prepare(`DROP TRIGGER \`${definition.name}\``));
    statements.push(database.prepare(definition.sql));
  }
  // Validation above completes before any mutation. The whole replacement is atomic.
  if (statements.length) await database.batch(statements);
  const verified = await installedGuards(database);
  for (const definition of CREDITEX_JOB_LIFECYCLE_SCHEMA_GUARD_DEFINITIONS) {
    if (!matchesGuard(definition.name, verified.get(definition.name) || "", definition.sql)) {
      throw new Error(`CREDITEX_JOB_LIFECYCLE_GUARD_UNAVAILABLE:${definition.name}`);
    }
  }
}

export async function ensureCreditexJobLifecycleSchemaGuards(database: D1Database) {
  let readiness = readinessByDatabase.get(database);
  if (!readiness) {
    readiness = installCreditexJobLifecycleSchemaGuards(database);
    readinessByDatabase.set(database, readiness);
  }
  try {
    await readiness;
  } catch (error) {
    readinessByDatabase.delete(database);
    throw error;
  }
}
