"use client";

import { useCallback, useEffect, useState } from "react";

type Setting = { channel: string; provider: string; enabled: boolean; configured: boolean; senderLabel: string; dailyLimit: number; revision: number; updatedAt: string };
type Count = { channel: string; status: string; total: number };
type Failure = { id: string; channel: string; provider: string; status: string; attempts: number; providerStatus: string; lastError: string; updatedAt: string };
type AppointmentCount = { audience: string; channel: string; status: string; total: number };
type AppointmentDelivery = { id: string; eventType: string; audience: string; channel: string; provider: string; status: string; eligibilityReason: string; attempts: number; providerStatus: string; lastError: string; updatedAt: string };
type PhotoRequestCount = { channel: string; status: string; total: number };
type PhotoRequestDelivery = { id: string; channel: string; provider: string; intent: string; status: string; eligibilityReason: string; attempts: number; providerStatus: string; lastError: string; updatedAt: string };
type Result = { settings?: Setting[]; counts?: Count[]; failures?: Failure[]; appointmentCounts?: AppointmentCount[]; appointmentDeliveries?: AppointmentDelivery[]; photoRequestCounts?: PhotoRequestCount[]; photoRequestDeliveries?: PhotoRequestDelivery[]; smsSenderApproved?: boolean };

const readable = (value: string) => value.replaceAll("_", " ").replace(/\b\w/g, (letter) => letter.toUpperCase());

export function AdminServiceReminderDelivery({ api, setStatus }: { api: (path: string, init?: RequestInit) => Promise<Record<string, unknown>>; setStatus: (value: string) => void }) {
  const [data, setData] = useState<Result>({}); const [busy, setBusy] = useState("");
  const load = useCallback(async () => { try { setData(await api("/api/admin/service-reminder-delivery") as Result); } catch (error) { setStatus(error instanceof Error ? error.message : "Reminder delivery health could not be loaded."); } }, [api, setStatus]);
  useEffect(() => { const frame = window.requestAnimationFrame(() => void load()); return () => window.cancelAnimationFrame(frame); }, [load]);
  async function save(setting: Setting, updates: Partial<Setting>) {
    setBusy(setting.channel); setStatus("Saving protected reminder channel settings...");
    try {
      const next = { ...setting, ...updates }; const result = await api("/api/admin/service-reminder-delivery", { method: "PATCH", body: JSON.stringify({ channel: setting.channel,
        enabled: next.enabled, senderLabel: next.senderLabel, dailyLimit: next.dailyLimit, expectedRevision: setting.revision }) }) as Result;
      setData(result); setStatus(`${setting.channel === "email" ? "Email" : "SMS"} reminder settings saved and audited.`);
    } catch (error) { setStatus(error instanceof Error ? error.message : "Reminder channel settings could not be saved."); }
    finally { setBusy(""); }
  }
  async function retryAppointment(item: AppointmentDelivery) {
    setBusy(item.id); setStatus("Rechecking appointment notification consent and delivery readiness...");
    try { setData(await api("/api/admin/service-reminder-delivery", { method: "POST", body: JSON.stringify({ action: "retry_appointment_delivery", deliveryId: item.id }) }) as Result); setStatus("Appointment notification retry recorded and audited."); }
    catch (error) { setStatus(error instanceof Error ? error.message : "Appointment notification could not be retried."); }
    finally { setBusy(""); }
  }
  async function retryPhotoRequest(item: PhotoRequestDelivery) {
    setBusy(item.id); setStatus("Rechecking photo request consent, link and delivery readiness...");
    try { setData(await api("/api/admin/service-reminder-delivery", { method: "POST", body: JSON.stringify({ action: "retry_photo_request_delivery", deliveryId: item.id }) }) as Result); setStatus("Photo request delivery retry recorded and audited."); }
    catch (error) { setStatus(error instanceof Error ? error.message : "Photo request delivery could not be retried."); }
    finally { setBusy(""); }
  }
  return <section className="admin-panel admin-reminder-delivery"><div className="admin-panel-heading"><span>Customer communications</span><h2>Service reminder delivery</h2><p>Provider credentials stay in protected Sites secrets. A channel can be enabled only after authenticated delivery callbacks are ready.</p></div><div className="admin-reminder-channels">{(data.settings || []).map((setting) => <article key={setting.channel}><header><div><strong>{setting.channel === "email" ? "Email" : "SMS"}</strong><small>{setting.provider} | {setting.configured ? "credentials and callbacks ready" : "provider setup required"}{setting.channel === "sms" && !data.smsSenderApproved ? " | TLink sender approval pending" : ""}</small></div><span className={setting.enabled ? "ready" : "attention"}>{setting.enabled ? "Enabled" : "Disabled"}</span></header><label>Daily safety limit<input type="number" min="1" max="1000" defaultValue={setting.dailyLimit} onBlur={(event) => { const dailyLimit = Number(event.target.value); if (dailyLimit !== setting.dailyLimit) void save(setting, { dailyLimit }); }} /></label><button type="button" disabled={Boolean(busy) || (!setting.configured && !setting.enabled)} onClick={() => void save(setting, { enabled: !setting.enabled })}>{setting.enabled ? "Disable channel" : "Enable channel"}</button><small>{(data.counts || []).filter((item) => item.channel === setting.channel).map((item) => `${item.total} ${item.status}`).join(" | ") || "No deliveries yet"}</small></article>)}</div>{Boolean(data.failures?.length) && <details><summary>Recent failed or suppressed deliveries</summary><div className="admin-reminder-failures">{data.failures?.map((item) => <article key={item.id}><strong>{item.channel.toUpperCase()} | {item.status}</strong><span>{item.providerStatus || item.lastError || "Provider did not supply a reason"}</span><small>{new Date(item.updatedAt).toLocaleString("en-AU")}</small></article>)}</div></details>}<section className="admin-appointment-delivery"><header><div><span>Operational appointments</span><h3>Appointment notification delivery</h3><p>Creation, staff review, schedule changes and preparation confirmation reuse current consent, opt-out and authenticated callback controls.</p></div></header><div className="admin-reminder-channels">{["customer", "installer"].map((audience) => <article key={audience}><header><div><strong>{readable(audience)}</strong><small>No contact details are displayed here</small></div></header><small>{(data.appointmentCounts || []).filter((item) => item.audience === audience).map((item) => `${item.total} ${item.channel} ${readable(item.status)}`).join(" | ") || "No appointment deliveries yet"}</small></article>)}</div><div className="admin-reminder-failures">{(data.appointmentDeliveries || []).map((item) => <article key={item.id}><strong>{readable(item.eventType)} | {readable(item.audience)} {item.channel.toUpperCase()}</strong><span>{readable(item.status)} | {item.providerStatus || item.eligibilityReason || item.lastError || item.provider}</span><small>{new Date(item.updatedAt).toLocaleString("en-AU")} | {item.attempts} of 3 attempts</small>{["failed", "waiting_for_channel", "waiting_for_sender", "waiting_for_limit"].includes(item.status) && item.attempts < 3 && <button type="button" disabled={Boolean(busy)} onClick={() => void retryAppointment(item)}>Retry notification</button>}</article>)}{!data.appointmentDeliveries?.length && <p>No appointment notification events have been recorded yet.</p>}</div></section><section className="admin-appointment-delivery"><header><div><span>Job photo requests</span><h3>Photo request link delivery</h3><p>Only aggregate outcomes and provider state appear here. Customer contacts, secure links and photos stay hidden.</p></div></header><div className="admin-reminder-channels">{["email", "sms"].map((channel) => <article key={channel}><header><div><strong>{channel.toUpperCase()}</strong><small>No customer contact or capability token is displayed</small></div></header><small>{(data.photoRequestCounts || []).filter((item) => item.channel === channel).map((item) => `${item.total} ${readable(item.status)}`).join(" | ") || "No photo request deliveries yet"}</small></article>)}</div><div className="admin-reminder-failures">{(data.photoRequestDeliveries || []).map((item) => <article key={item.id}><strong>{readable(item.intent)} | {item.channel.toUpperCase()}</strong><span>{readable(item.status)} | {item.providerStatus || item.eligibilityReason || item.lastError || item.provider}</span><small>{new Date(item.updatedAt).toLocaleString("en-AU")} | {item.attempts} of 3 attempts</small>{["failed", "waiting_for_channel", "waiting_for_sender", "waiting_for_limit"].includes(item.status) && item.attempts < 3 && <button type="button" disabled={Boolean(busy)} onClick={() => void retryPhotoRequest(item)}>Retry photo request</button>}</article>)}{!data.photoRequestDeliveries?.length && <p>No photo request deliveries have been recorded yet.</p>}</div></section></section>;
}
