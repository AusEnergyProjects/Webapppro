# TLink and AEA release truth

Last verified: 21 July 2026

This is the canonical implementation-status document. README, roadmap, runbooks and historical architecture notes must link here instead of defining competing release states.

Future AI-assisted changes follow [AI delivery guardrails](./AI_DELIVERY_GUARDRAILS.md). The executable next milestone is maintained in the single rolling [handover](./HANDOVER_NEXT_TASK.md).

## Product identity

1. Australian Energy Assessments household platform: electricity and gas comparison, NEM12 processing, home energy plans, scenarios, guides, rebates, assessments and always-free consumer accounts.
2. TLink marketplace: protected household opportunities, installer and wholesaler accounts, verification, catalogue, product selection, purchasing and referrals.
3. TLink trade operating platform: CRM, customers, system-numbered jobs, scheduling, forms, assets, warranties, handover, accounting, payment pathways, teams and device management.
4. TLink field application: assigned work, encrypted offline data, conflict-safe synchronisation, forms, time, evidence and remote revocation for iOS and Android.

## Active runtime and data services

- Web and API runtime: OpenAI Sites using the Vinext Cloudflare worker output.
- Database: Cloudflare D1 binding `DB`.
- Private evidence storage: R2 binding `EVIDENCE`.
- Source and release record: GitHub.
- Low-cost email and monitoring path: Google Apps Script and Google Workspace.
- Not active: Netlify and Vercel deployment targets.

## Confirmed implementation

- Native electricity and gas comparison routes plus compatibility rollback for electricity.
- Consumer, installer, wholesaler and restricted admin authentication journeys.
- Certificate price education with seven certificate definitions, a scheduled six-month reported-trade history and a privacy-safe public read endpoint.
- Privacy-safe opportunity matching with household identity and direct contact withheld from trade accounts.
- Installer CRM, catalogue, purchasing, integrations, payments, teams, assets, handover and native field-sync foundations.
- Keyset pagination, server-side search, D1 FTS5 indexes, query telemetry and high-volume list tables.
- Synthetic scale fixtures separated from production migrations.
- Uppercase canonical Australian state codes for new writes, filters and normalisation.
- Server-configured membership checkout, billing portal and webhook payment-link mapping.
- Phase 6 free trade access: verified installers and wholesalers receive role-appropriate CRM, leads, marketplace, scheduling, team, field, forms, purchasing, catalogue and handover tools without a subscription, seat or per-lead entitlement.
- Marketplace opportunities expose direct quote, CRM job and dated site-visit actions while retaining the protected opportunity match as the owner-scoped source reference.
- Historical Stripe subscriptions remain manageable through the billing portal, but the public and dashboard journeys no longer offer a new paid plan or paid referral reward for core trade access.
- Additive migration `0047_customer_service_site_foundation.sql` creates owner-scoped customer contacts, service sites and site-contact assignments, backfills one primary contact and site for each existing direct CRM customer and links only direct jobs to the migrated site.
- Additive migration `0048_unified_enquiry_inbox.sql` creates the owner-scoped direct and protected-reference enquiry inbox, conversation and audit history, explicit duplicate conversion and generic CSV or Excel imports.
- Additive migration `0049_customer_asset_timeline.sql` extends the existing installed-asset register with customer, service-site, provenance, review and lifecycle links. Existing handover assets require installer confirmation before joining the direct-customer timeline.
- Additive migration `0050_versioned_trade_quotes.sql` adds direct-job quote headers, immutable versions, integer-cent line calculations and verified-customer decision evidence without changing the protected marketplace quote flow.
- Additive migration `0051_team_scheduling_capacity.sql` adds team working hours, unavailable periods, appointment member assignments and appointment revisions while preserving existing job, appointment, team and service-site sources of truth.
- Additive migration `0052_service_follow_up_preparation.sql` adds one owner-scoped service follow-up per service plan and due date plus immutable preparation audit events. Existing assets, service plans, customer preferences, consent receipts, customer records, service sites and team members remain authoritative.
- Additive migration `0053_service_reminder_delivery.sql` adds provider channel settings, idempotent delivery records, authenticated provider events, verified mobile contacts and customer channel opt-outs. Email and SMS channels remain disabled until their protected Sites credentials and authenticated callbacks are configured.
- Additive migration `0054_service_follow_up_reporting.sql` adds the date-first indexes required for bounded aggregate follow-up workload and delivery reporting without duplicating service, delivery or staff sources of truth.
- Additive migration `0055_appointment_rescheduling.sql` adds verified-customer appointment change requests, owner-scoped review tasks, immutable decision events and reconstructable appointment revisions. Customer submission never changes the schedule; authorised dispatch acceptance reruns existing conflict checks before the atomic update.
- Additive migration `0056_customer_contact_release.sql` adds optional private phone and service-address defaults, one customer-authorised contact snapshot per exact opportunity match and immutable grant, withdrawal and project-closure events. Marketplace scopes remain anonymised until a verified customer separately releases the named fields to one shortlisted verified installer.
- Additive migration `0057_customer_property_arrivals.sql` adds structured project property context, R2-backed evidence metadata, immutable evidence access events, revisioned installer arrival-window proposals and immutable proposal and customer-selection events. Customer-approved uploads are available to every active verified installer allocated to the exact enquiry. Job conversion and arrival proposals remain gated to the accepted connected installer.
- Additive migration `0058_trade_contact_arrival_handoff.sql` adds mandatory trade ABN storage, customer-selected direct installer contact snapshots, CRM job and appointment linkage and preparation acknowledgement state. Direct contact exposes only installer business name, contact number, email and ABN and creates an admin-visible audit notification.
- Additive migration `0059_appointment_notifications.sql` adds revision-bound appointment events, audience and channel delivery records and authenticated provider event history. Appointment creation, first staff assignment, authorised schedule changes and customer preparation confirmation reuse existing customer account updates, trade operational email, channel opt-out, daily limit and provider callback controls. The `TLink` SMS sender approval flag remains false by default, so appointment SMS cannot send before approval.
- Additive migration `0060_customer_photo_requests.sql` adds one revisioned photo request per direct-customer job, hashed expiring capability links, immutable request events and request context on existing private job media. Authorised installer office users can edit job-specific requirements and issue or revoke links. Customers complete a privacy self-review before signature-checked, metadata-stripped photos enter the exact job proof.
- Additive migration `0061_photo_request_templates.sql` adds owner-scoped photo-guidance templates, immutable published versions and source-version metadata on each independent job request. Archived templates cannot seed new requests. Usage and controlled trade-feedback counts use request metadata only and never inspect customer images.
- Additive migration `0062_photo_request_delivery.sql` adds encrypted current-link recovery, revision and token-issue idempotent email or SMS delivery, two bounded resends, a final-seven-day expiry reminder, authenticated receipt updates and privacy-safe administrator health. SMS remains suppressed until the Australian sender is approved.
- Additive migration `0063_photo_request_review.sql` adds immutable customer completion evidence, append-only per-requirement installer review, fixed retake reasons and review-bound targeted follow-up identity. Reviewed originals remain available to authorised job users, while field and template reporting receive only readiness and aggregate outcomes.
- Additive migration `0064_trade_price_book.sql` adds owner-scoped reusable trade items, integer-cent cost and sell values, deterministic basis-point markup and margin, immutable price-change history and quote-line snapshots. Owners, managers and coordinators can maintain the library; active items can populate direct-job quote drafts without changing issued quote revisions or duplicating the existing business-capability and approved-supplier-catalogue sources.
- Additive migration `0065_trade_job_packets.sql` adds owner-scoped reusable packet composition from current price-book items plus references to existing job-template tasks and published forms. Deterministic estimates and suggested-crew readiness reuse authoritative capability and active-team sources. Direct-job quote lines snapshot the packet, revision and item while issued quote revisions remain immutable.
- Additive migration `0066_optioned_trade_quotes.sql` adds immutable quote choices, customer-facing section headings and exact accepted-selection evidence. A packet can become a standard quote or three customer packages in one action. Server-validated package, choose-one and optional-extra totals use integer cents, while internal cost and margin remain available only to authorised installer office roles and never enter the customer payload.
- Additive migration `0068_accepted_quote_handoff.sql` materialises one immutable commercial handoff from each accepted direct-customer quote. The accepted scope, terms, GST and integer-cent total drive one configurable deposit request through Stripe or Square and one accounting invoice through Xero, MYOB or QuickBooks without office re-entry. Provider callbacks remain authoritative for paid state, and no accounting invoice is approved, sent or emailed automatically.
- Additive migration `0070_frictionless_team_roster.sql` separates scheduling from login access. Every installer owner is materialised as an active Me resource, roster-only people are assignable immediately, and an optional secure login invitation can be created or refreshed independently.
- Additive migration `0071_job_execution_progress.sql` snapshots packet task titles, form keys and versions, duration, capability and crew metadata when a quote is issued. Accepted jobs use that immutable snapshot, while legacy manual quotes keep the lightweight line-based fallback. Owner-scoped actual labour and material records drive phase progress, forecast cost, margin variance and a completion gate for scope, forms, materials and requested photo proof. Completion prepares invoice and handover work without mutating the accepted scope or total.
- Additive migration `0074_global_tlink_job_numbers.sql` assigns every existing installer job one stable global `TLJ-########` number, advances the shared sequence and enforces uniqueness for future jobs. Installers and administrators see and search the same number while the internal UUID and job history remain unchanged.
- Additive migration `0075_guided_quick_invoices.sql` stores one owner-scoped fixed-fee invoice per guided direct-customer job, snapshots authoritative price-book items and custom lines in integer cents, records explicit email consent and keeps provider delivery status recoverable from the job Invoice tab.
- Feature-owned admin account, opportunity, catalogue, inbox and product-enquiry workspaces, with shared saved-list request helpers.
- Admin performance SLO dashboard with 7-day p95 latency, average database time, error-rate and sample-size assessment plus read-only keyset query-plan checks.
- Stripe production preflight on 17 July 2026: no active account tasks; four active Direct Trade membership payment links at the configured monthly and annual prices; one active membership webhook destination subscribing to five events with a 0% error rate.

## Validation contract

`npm run validate` is the release gate. It must pass on the exact commit being published and includes:

- TypeScript checking for the active web application and worker.
- ESLint.
- Runtime integration tests.
- Full automated tests.
- Fresh Cloudflare D1 migration application including FTS5.
- Production build.

`npm run test:coverage` provides the coverage report. Source-pattern tests remain structural guardrails, not proof of runtime behaviour. New access-control, data-flow and calculation work should add executable tests at the domain or route boundary.

## External or unverified state

- Stripe production account status, historical membership payment links and webhook destination were inspected on 17 July 2026. Phase 6 supersedes new subscription-led core access. Existing subscribers retain a billing-portal path while product and commercial owners determine the retirement, cancellation or credit process. Webhook signing secrets remain deployment secrets.
- The public Sites baseline before P6-2A was version 122, sourced from `d3b69fb` on `codex/sites-custom-domain-migration`. The P6-2A release branch continues from that exact baseline; future releases must continue from the newest live Sites version and rerun the release audit.
- P6-2A was published as Sites version 123 from commit `e5ddc9471b059342840d2e7c86cb0a91758814ca` on `codex/sites-custom-domain-migration`.
- P6-2B was published as Sites version 124 from commit `5863f0419c420811bfa145ed9c4523f212888dde` on `codex/sites-custom-domain-migration`.
- P6-2C was published as Sites version 126 from commit `d711fda4590dfa67c1e09dbb413d18b5fde07f92` on `codex/sites-custom-domain-migration`.
- P6-2D was published as Sites version 127 from commit `5fb6511845e059d843e29cf179b1134141b52842` on `codex/sites-custom-domain-migration`. The canonical domain and `/api/health` both returned HTTP 200 after publication.
- P6-2E was published as Sites version 129 from commit `83007b3001b3ac61e0af5b1fdd1fdd346208708a` on `codex/sites-custom-domain-migration`. It adds team scheduling, capacity and server-side conflict checks and makes the admin account's saved verification status authoritative in both admin and signed-in trade entitlement responses. The Sites origin redirected to the canonical domain, and the canonical domain and `/api/health` both returned HTTP 200 after publication.
- P6-2F was published as Sites version 133 from commit `86418e57f1d0f97f2cfcf9baf07de30720d3d821` on `codex/sites-custom-domain-migration`. It adds consent-aware service follow-up preparation, deterministic reminder drafts, staff assignment, suppression, completion and audit controls. The final release aligns unstored customer lifecycle preferences to explicit opt-in rather than a visually checked default. The Sites origin redirected to the canonical domain, and the canonical domain and `/api/health` both returned HTTP 200 after publication.
- P6-2G was published as Sites version 135 from commit `5bff61906e490d8bcd6f60402fb16cb0011b70c1` on `codex/sites-custom-domain-migration`. It adds consent-safe Resend email and Twilio SMS delivery boundaries, explicit customer channel selection, verified mobile contacts, idempotent delivery history, authenticated provider callbacks, channel opt-outs and administrator delivery health controls. Provider channels remain disabled until protected account credentials, verified senders and callback secrets are configured. Sites environment revision 11 added only the non-secret reply-to address and exact Twilio callback URL. The Sites origin redirected to the canonical domain, and the canonical domain and `/api/health` both returned HTTP 200 after publication.
- P6-2H was published as Sites version 137 from commit `61e0a1b837d0762bce250f9bc41db8c16986a5e4` on `codex/sites-custom-domain-migration`. It adds consent-safe aggregate follow-up workload and delivery reporting plus date-first reporting indexes. The Resend sending domain is verified, and its credentials and authenticated webhook are protected in Sites. The upgraded Twilio account, Messaging Service, delivery callback, Advanced Opt-Out and SMS Verify Service with Fraud Guard are configured in deployed Sites environment revision 14. The `TLink` Australian sender registration remains a draft pending genuine brand-ownership and identity evidence, so SMS remains disabled. The Sites origin redirected to the canonical domain, and the canonical domain and `/api/health` both returned HTTP 200 after publication.
- P6-2I was published as Sites version 140 from commit `17c21503924e3b1fb567cbec04d46a5b6fa5484b` on `codex/sites-custom-domain-migration`. It adds verified-customer appointment change requests, duplicate-active-request prevention, owner-scoped CRM review tasks, immutable decision history and dispatch accept, reject and alternative controls. Customer submission does not change the appointment. Acceptance is request and appointment revision protected, reruns team schedule conflict checks and preserves the prior and accepted schedule revisions. The Sites origin redirected to the canonical domain, and the canonical appointment route and `/api/health` both returned HTTP 200 after publication.
- P6-2J was published as Sites version 142 from commit `c257543427b6a214b57851bfd1132750e7387b50` on `codex/sites-custom-domain-migration`. It keeps marketplace opportunities anonymous through matching and quote review, requires a private customer phone and complete project-matching service address before a trade request, and releases a snapshotted contact record only to the exact verified installer whose quote the customer shortlisted and explicitly consented to contact. Other matches remain redacted, and withdrawal removes future portal visibility while preserving immutable audit history. The Sites origin redirected to the canonical domain, and the canonical account profile, trade dashboard and `/api/health` routes returned HTTP 200 after publication.
- P6-2K was published as Sites version 144 from commit `14e03278926d979d2f84862be1edb0b4c1e473a3` on `codex/sites-custom-domain-migration`. It adds required structured property context, consented phone or tablet photo capture, signature-checked R2 evidence storage and revisioned installer-owned arrival proposals. Quoting photos are shared with every active verified installer allocated to the exact enquiry, while supporting documents remain restricted to the accepted connected installer. Only that installer can propose arrival windows or convert the platform lead to a CRM job, and customer selection does not create an appointment. The Sites origin redirected to the canonical domain, and the canonical new-project, trade-dashboard, marketplace-standards and `/api/health` routes returned HTTP 200 after publication.
- P6-2K photo privacy hardening was published as Sites version 146 from commit `25eba2c226e48870c1252914a0c27946f5121151`. It converts quote-guidance images to bounded JPEGs in supported browsers and independently strips JPEG, PNG or WebP metadata on the server before private R2 storage. The Sites origin redirected to the canonical domain, and the canonical new-project, trade-dashboard, marketplace-standards and `/api/health` routes returned HTTP 200 after publication.
- P6-2L was published as Sites version 148 from commit `d4036832d0e47ae455d29103f963a78fb3571d5c` on `codex/sites-custom-domain-migration`. It shares every customer-approved project upload, including PDFs, with each active verified installer allocated to the exact enquiry; adds the customer direct-contact choice with only installer business name, contact number, email and ABN plus an admin-visible audit notification; requires those four business fields for new or updated trade profiles; and materialises a selected arrival window as an unassigned CRM appointment only when the accepted installer converts the lead. Customer preparation acknowledgement is reset after dispatch assignment or an accepted reschedule. The Sites origin returned HTTP 308 to the canonical domain, and the canonical homepage, new-project, trade-dashboard, trade-signup, marketplace-standards, operations control-centre and `/api/health` routes returned HTTP 200 after publication.
- P6-2Q was published as Sites version 159 from commit `c90417d4f23943cbca4881bc63ccd71bc57933c9` on `codex/sites-custom-domain-migration`. The Jobs index cancellation correction in `aa771460b190c7e744caca216bb4c8dde3087c77` was published with the same implementation baseline as Sites version 160. Superseded Jobs and Customers index requests are aborted before they can replace the current filtered results. The canonical signed-in Jobs list showed its expected job and detail workspace after publication, and the canonical domain and `/api/health` returned HTTP 200.
- P6-3A was published as Sites version 161 from release commit `9ed209550357d66d3c2ef996a21fa6b311465958`, with implementation commit `ab7331b0ae1d6b0ce4a2981278a74a09c95ac708`, on `codex/sites-custom-domain-migration`. It adds the authoritative trade price book, guided quick starts and direct-job quote reuse while keeping issued quote versions immutable. Production environment revision 14 was retained.
- P6-3B was first published as Sites version 163 from release commit `6cbfd210bf8f805cbbb15bdf487a975deec03a31`, with implementation commit `b4b1ffd02b1d82a5293ba0a849255f507707a4f0`, on `codex/sites-custom-domain-migration`. It adds the installer-first reusable job-packet library, current price-book estimates, existing checklist, form and team references, and idempotent direct-quote application with exact packet revision snapshots. Live responsive QA produced the bounded mobile job-card alignment correction in `a7ba30f7540b298e966c08601e777b18c87221bb`. Production environment revision 14 was retained.
- P6-3C was published as Sites version 167 from release commit `ae21802df41136debbe7ea53dfdd599a98e13333`, with implementation commit `35da31d1d5300c123d03a0848275aad069e57849`, on `codex/sites-custom-domain-migration`. It adds one-action Good, Better, Best packet conversion, customer-facing sections, optional extras, choose-one groups, server-calculated selection totals, office-only cost and margin visibility, and immutable verified-customer selection evidence. Live desktop and 390px responsive QA confirmed the job appears in the Jobs tab and produced the bounded phone quote-editor stacking correction in the release commit. The canonical domain and `/api/health` returned HTTP 200, and production environment revision 14 was retained.
- P6-3D was published as Sites version 169 from implementation commit `ac1c87e3e84e5de7151afc00546042ed61d0070b` on `codex/sites-custom-domain-migration`. It adds expiring and revocable no-account quote links, deliberate email delivery, customer questions, option review, typed acceptance evidence, stable provider-neutral commercial references and a QuickBooks OAuth connection boundary alongside the existing Xero, MYOB, Stripe and Square boundaries. QuickBooks draft-invoice export remains explicitly deferred to P6-3E. Live signed-in desktop and phone-width QA confirmed the production job appears in Jobs, its Money workspace renders without horizontal overflow, and the quote editor stacks into single-column touch controls. No production quote was issued or accepted for QA. The canonical `/api/health` route returned HTTP 200, and production environment revision 14 was retained.
- P6-3E was published as Sites version 170 from implementation commit `41db312cc2545811eb3426d6a50e5b6e777b290b` on `codex/sites-custom-domain-migration`. It materialises the accepted quote as the authoritative commercial handoff, locks the deposit amount before provider checkout, prevents duplicate cross-provider deposit requests, reconciles verified Stripe or Square payment events, and creates one exact-scope accounting invoice through Xero, MYOB or QuickBooks for direct customers. Manual financial tracking remains collapsed as a fallback, and disconnected provider actions route directly to Integrations. Live signed-in desktop and phone-width QA confirmed the production job appears in Jobs, the Money workspace presents the quote-to-acceptance handoff with progressive disclosure, and both layouts render without horizontal overflow or browser console errors. No production quote, deposit request or accounting document was created for QA. The canonical dashboard and `/api/health` routes returned HTTP 200, and production environment revision 14 was retained.
- P6-3F readiness and catalogue usability were published as Sites version 171 from implementation commit `38f62fcd28bdbbb374eae151dbcf870a23a4b649` on `codex/sites-custom-domain-migration`. Invited technicians can be preassigned while job access remains inactive until invitation acceptance. Accepted direct-customer scope can become one idempotent owner-scoped job plan with scope, forms, technician, materials and deposit readiness. Installer catalogue headers now provide searchable include and exclude controls for wholesaler, brand and model code, obvious horizontal navigation and a pinned first column. Clicking a wholesaler opens its verified trade contacts, service coverage, self-maintained dispatch locations and approved catalogue. Live signed-in desktop and phone-width QA confirmed the pending technician option, working wholesaler include filter, profile drawer, internal table scrolling, stacked mobile product cards and no document-level horizontal overflow. No assignment, quote conversion or supplier profile was mutated during QA. The only console entries were Chrome extension message-channel closures with no application stack. The canonical `/api/health` route returned HTTP 200, and production environment revision 14 was retained.
- The frictionless self-and-staff scheduling batch was published as Sites version 172 from implementation commit `8ea5dd6f4471e82bc16621e3e940a76dbff1b0f7` on `codex/sites-custom-domain-migration`. The signed-in owner is an immediately assignable Me resource, added people can be scheduled without confirming or creating a login, and optional secure login links remain separately controlled. Schedule now defaults an unassigned sole-trader job to Me, collapses working-hours administration, replaces tall empty day columns with a compact week strip and keeps dispatch to one labelled row. Live signed-in QA confirmed Me and the existing technician in Schedule, the active Add to schedule action, the owner record and optional-login roster copy in People, and no document-level horizontal overflow. No staff, assignment or appointment record was mutated during QA. The only console entries were Chrome extension message-channel closures with no application stack. Production environment revision 14 was retained.
- P6-3G immutable execution snapshots and actual job progress were published as Sites version 173 from implementation commit `7c5d2d1a6eaeeedea7f531984eb877841e99a514` on `codex/sites-custom-domain-migration`. Normal work uses one-action Done as planned or Used as planned controls, while changed time, quantity, cost and notes stay behind progressive disclosure. Budget variance uses TLink integer-cent actuals and never treats Xero, MYOB, QuickBooks, Stripe or Square as operational truth. Live signed-in desktop and 390 px QA confirmed the real Jobs list, job detail and Money entry flow with no document-level horizontal overflow, and Sites worker logs contained no errors. The production account has no accepted quote, so the execution panel's data-filled state was covered by route, migration and responsive contract tests without creating, accepting or completing a production quote or job. Production environment revision 14 was retained.
- P6-3H simpler invoicing and dependable dispatch were published as Sites version 174 from implementation commit `499050a8138371739994a0335f17db6d0f4b7751` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5b9217d4848191bb95459db3a1ee76`. Installer Orders is removed from primary navigation and search while existing records, APIs and supplier fulfilment remain preserved for later validation with active trades and wholesalers. Installer-facing Job packets are now Common jobs without changing stored packet identifiers or immutable quote snapshots. The separate Invoice tab previews an accepted scope and exact subtotal, GST and total beside one progressive Xero, MYOB or QuickBooks draft action. Schedule rejects past starts on the client and server, keeps working hours advisory, preserves hard overlap and recorded-time-off checks, and supports seven-day drag and drop with immediate response-driven regrouping. Live signed-in desktop and 390 px QA confirmed the absent Orders navigation, Common jobs wording, separate Quote and Invoice tabs, past-day disabled state, visible drop targets and no document-level horizontal overflow. The production account has no accepted quote, so the populated invoice preview and schedule mutations were covered by automated contract tests without creating an accepted quote or changing its existing appointment. The only console entries were Chrome extension message-channel closures with no application stack. Production environment revision 14 was retained.
- P6-3I visual dispatch and invoice home were published as Sites version 175 from implementation commit `97c6b0b8fdd066c233b0c12e68bce59fa11859ee` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5ba026ebd4819180df41b2cd4aaa28`. Invoices now has its own installer navigation section backed by owner-scoped jobs and accepted commercial records. Invoice rows and schedule blocks open the focused editable job on double-click, with explicit Open job controls for touch and keyboard use. Schedule is a colour-coded time grid with internally scrollable days, drag-to-day blocks, one start control and a 15-minute-step duration slider from 15 minutes to 8 hours. Google Calendar and Outlook use owner-scoped OAuth mappings as one-way mirrors while TLink remains authoritative; protected appointments omit customer identity and exact address. Live signed-in desktop and 390 px QA confirmed the invoice list, double-click job navigation, focused job workspace, one-hour appointment block, duration bounds, provider setup states and no document-level horizontal overflow. No appointment, invoice, quote or calendar record was mutated during QA. Google and Microsoft calendar client credentials are not configured in production, so both connection controls correctly show Administrator setup needed; production environment revision 14 was retained. The only console entries were Chrome extension message-channel closures with no application stack, and the canonical `/api/health` route returned HTTP 200.
- P6-3J simpler job intake and phone-first field work were published as Sites version 176 from implementation commit `338a9cdd3e20e7254eb36a3132fd121fdab8c4c5` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5bb6b401888191b9cfbf2a34513c5d`. One customer search or inline-create boundary now reuses authoritative direct-customer and service-site records, while new customer, service site and job creation commit atomically. The default form keeps dates, estimated value and notes out of initial entry, stores building type, and moves instructions, next action, tags and internal history into Notes. Web and native field work now expose one audited, idempotent active-state action at a time with stable offline receipts, direct-record-only contact and directions, and explicit finish blockers for tasks, forms, plan requirements, proof, issues and pending sync. Address suggestions remain provider-neutral and production shows an honest manual-entry fallback because no approved endpoint and credential are configured. Live signed-in desktop and phone-width QA confirmed keyboard-dismissable New and More menus, existing-customer reuse, inline customer creation, custom job titles, manual address entry, the separate Notes workspace, the Start travel field action, readiness checklist and visible proof blocker, with no document-level horizontal overflow. No production customer, service site, job, appointment or field state was created or changed during QA. The only browser console entries were Chrome extension message-channel closures with no application stack. The worker log contained one boundary 404 when the collapsed readiness panel queried an internal job that is intentionally outside the trade-owned readiness route; no 5xx was observed. The canonical dashboard and `/api/health` routes returned HTTP 200, and production environment revision 14 was retained.
- Guided job scheduling and global support IDs were published as Sites version 177 from implementation commit `7175a8406d962a4a79156760ba34853e6d9a0dd6` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5bc441ebfc8191a42a32782f717a3b`. Existing and future installer jobs now use one collision-free global `TLJ-########` number shown identically in the installer workspace and the administrator job directory. Job setup is one five-step flow for work, customer matching or creation, team member and appointment type, schedule, and secure evidence request; titles are generated from the authoritative customer name and work type. Duplicate phone or email matches offer the existing customer instead of failing, and the final action atomically creates the job, appointment and evidence request before deliberate email delivery. Live signed-in QA confirmed `TLJ-00000801` in both the installer schedule and exact admin search, the corrected New menu, the guided desktop layout and 390 px layout with no document-level horizontal overflow. No production customer, job, appointment or email was created during QA. Structured Google address parsing is deployed, but production continues to show manual entry because the Sites environment has no approved address-provider endpoint or credential; environment revision 14 was retained.
- Guided quick invoicing and quarter-hour appointment repair were published as Sites version 178 from implementation commit `4968a64d1f947d18da5e4417d6371f84b282082d` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5bcee8a55c819187773e30657b9d76`. The browser-native validation overlay is replaced by guided inline errors and the minimum appointment time shares the same quarter-hour grid as the control. The sixth Invoice step can be skipped or can snapshot saved and custom fixed fees, GST and due date into one durable TLink invoice before the existing deliberate email delivery. Accounting and payment connection prompts appear only when no supported provider is connected. Signed-in live QA reached the enabled final action with a `$250.00` plus GST draft at desktop and 390 px without saving a job or sending customer email. The canonical health route returned HTTP 200, production environment revision 14 was retained and no TLink route failure appeared in the post-release worker-error review.
- Quick-invoice provider handoff was published as Sites version 179 from implementation commit `9200141d521bf3119d7e08fbeb36f82a3de2b05f` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5c133b0a908191bd1e5d4b01d88943`. A saved TLink quick invoice can deliberately create one draft in Xero, MYOB or QuickBooks, or one full-total checkout through Stripe or Square, without re-entering customer, line or total data. Existing owner-scoped accounting and payment ledgers enforce one external document per source and purpose. TLink remains authoritative, accounting totals and GST must match exact TLink cents, verified invoice-payment callbacks mark only the quick invoice paid, and deposit callbacks retain their existing accepted-quote behavior. Signed-in desktop and 390 px QA confirmed the current job and Invoice workspaces without document-level horizontal overflow. No production provider transaction was created because the account has no connected sandbox provider or disposable invoice. Production environment revision 14 was retained, and Sites worker logs contained no new application errors after release.
- Guided-dispatch reliability and the invoice correction ledger were published as Sites version 180 from implementation commit `c75d555c669a9fecbf5dd639488675346cb01c23` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5c1c06195c8191a2a941766c595420`. Entered custom fees now enable the final quick-invoice action without a second Add click. Guided evidence delivery checks the provider result, appointment email includes the scheduled window, and the shared date dialog provides click-select quarter-hour times without native browser validation overlays. Schedule has an explicit week selector, previous and next controls, internal horizontal and vertical scrolling and day-plus-time drag placement. Issued quick invoices use append-only revision, credit and payment-allocation records with exact integer-cent outstanding balances; draft correction remains revision protected and provider credit or refund authority is not implied. Signed-in desktop and 390 px QA confirmed the enabled final action for an entered `$100.00` fee without saving the QA job, the time dialog, valid quarter-hour input, week navigation and bounded schedule scrolling. The production Resend email channel was enabled after administrator health confirmed its credentials and callbacks were ready. Job `TLJ-00000804` then recorded its initial photo-request email as `Sent | sent` at 19 July 2026, 10:49:46 am, with the UI confirming provider acceptance. This is not represented as mailbox delivery until an authenticated callback records that state. No production quick invoice, credit, refund or provider transaction was created. The canonical `/api/health` route returned HTTP 200, Sites worker logs contained no recent errors, and production environment revision 14 was retained.
- Customer photo upload and appointment preparation were published as Sites version 182 from implementation commits `1f96c09feb8f988dc9aabf2a40a125f7293086b3` and `e5934780a77151b5fcd53b23a9eaa42badb5faac` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5c29873ce08191b009b5676db7d869`. Production worker evidence confirmed the reported Android failure was an HTTP 413 edge rejection after the browser produced requests of about 1.4 MB and 1.7 MB. Customer JPEG, PNG and WebP selections are now converted to metadata-free JPEGs below 640 KB, staged visibly, limited to three per requested section and sent through one final batch action. Successful items leave the queue immediately so a later failure cannot duplicate them. The private page shows the appointment and one-tap Google Calendar action without the customer address. Photo-request email now carries a standard invitation with stable job UID, Australian timezone, organiser, recipient attendee and resend sequence. Live Gmail QA for job `TLJ-00000804` showed the 11:00 am to 1:00 pm invitation, Yes, Maybe and No controls, View on Google Calendar and the calendar attachment. Gmail required a one-time first-sender trust action before automatically adding current and future invitations; TLink does not bypass customer Google-account consent. Signed-in desktop and 390 px customer-page QA found no document-level horizontal overflow, and no QA photo was uploaded. Complete validation, fresh D1 replay, production build, canonical HTTP 200 health and zero recent Sites worker errors were confirmed. Production environment revision 14 was retained.
- Customer-upload review alerts and protected previews were published as Sites version 183 from implementation commit `dddedb71ca9af3b3274f4276504cb68cd73d3307` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5ca8e07ea4819194a59a66edc8c2bb`. Customer photo-request completions now produce owner-scoped or assigned-technician job notifications with durable per-user read receipts and exact Field work deep links. Job images and PDFs open through a protected inline preview with optional download, while guided and saved quick invoices require a preview confirmation before sending. The electricity comparison page now uses a local system font stack instead of remote heading typography. Live signed-in QA showed two unread upload events for `TLJ-00000804` and rendered the first of three customer files inside the job preview without marking an alert read or mutating production data. The account has no production invoice draft, so invoice-preview behavior was covered by focused contracts without creating or sending an invoice. Live comparison headings and brand used Arial at 2560 px without document-level horizontal overflow. Complete validation, focused 12-test follow-up, ESLint and diff checks passed. Production environment revision 14 was retained.
- Fast evidence review and saved-price quoting were published as Sites version 184 from implementation commit `0365fb8bd0f20ef35689f10f3350ce3072b49416` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5cb6c1ef58819191a40c7378e0d260`. Owners, managers and coordinators can approve a supplied customer-photo requirement or request a retake from Field work. The retake remains append-only and revision-bound, preserves the original evidence, uses one fixed privacy-safe reason and reuses the current secure link through the existing consent-aware delivery ledger. Quick quotes now put the business's saved price-book items before manual lines; linked description, type, unit price and GST remain authoritative and are resolved again server side, while quantity and the customer section remain editable. Compact Jobs, Customers and Price book shortcuts sit under Work in the installer rail. Signed-in desktop and 390 px QA confirmed the live controls on `TLJ-00000804`, the masked email retake preview, the empty-state Quote to Price book handoff, the price-item editor and no document-level overflow. No review, customer message, price item or quote was created or changed during QA. Complete validation, fresh D1 replay, production build, canonical HTTP 200 health and zero recent Sites worker errors passed. The only browser console entries were Chrome extension message-channel closures with no application stack. Production environment revision 14 was retained.
- In-place evidence review and direct quote preview-and-send were published as Sites version 185 from implementation commit `0f4f0cf7698a830d73b107d9b5b3c2915033062c` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5d67145e0881919a8d19209871ecc7`. Job detail refreshes now update authoritative data without remounting the focused job, so Field work remains selected after Approve or Retake and the reviewer can continue through the remaining requirements. Quote creation keeps Save draft as an optional secondary action and adds one Preview and send path that validates and previews the recipient, itemised scope, GST, total and recorded terms before sequentially saving, issuing and emailing through the existing server boundaries. A partially completed sequence remains recoverable and never claims an email was sent after delivery failure. Signed-in live QA on `TLJ-00000804` confirmed the authorised email, the issued-quote next-version preview, exact `$4,444.00` total and the disabled final send until consent. Desktop and 390 px inspection found no document-level or modal overflow. No evidence decision, quote version or customer email was created during QA. Complete validation passed with 29 integration tests and 648 passing tests plus two intentional skips, fresh D1 migration replay and production build. Canonical `/api/health` returned HTTP 200. The only browser console entries were Chrome extension message-channel closures with no application stack. Production environment revision 14 was retained.
- Central customer and field activity notifications were published as Sites version 186 from implementation commit `506198847fe44ee4f929cd78e429f18de0e1ad95` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5d81214f7481919e006da26f5e81de`. The installer bell projects authorised events from the existing quote, question, photo-request, appointment, payment, work-order and sign-off ledgers without adding a second activity source. Accepted and declined quotes, customer questions and opens, photo completions, reschedule requests, payment outcomes, meaningful field progress, completed tasks and forms, and sign-offs now share one owner or assigned-technician review queue with Customer or Field team labels and exact job-tab targets. Unanswered quote questions appear directly below the quote header. Signed-in live QA showed four unread updates including the existing accepted quote and `Does this work` customer question for `TLJ-00000803`, plus customer quote-open and technician sign-off activity. The same job showed the question and Answer action at the top of its accepted quote. Desktop and 390 px inspection found no document-level overflow. Focused tests, TypeScript, ESLint, diff checks and complete `npm.cmd run validate` passed, including 29 integration tests, fresh D1 migration replay and production build. Canonical `/api/health` returned HTTP 200. The browser reported only Chrome extension message-channel closures with no application stack; the Sites error filter contained one client-cancelled notification poll during navigation and no application exception or 5xx. Production environment revision 14 was retained. The OAuth adapters remain deployed but correctly unavailable because the external Google, Microsoft, Xero, MYOB, QuickBooks, Stripe and Square provider applications have not supplied their required client credentials.
- Installer-owned provider connection handoff was published as Sites version 187 from implementation commit `b29e28245c0090f730c779b2f094607284d9686b` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5d8b4e01948191ba5d5a75df4f3441`. Installer-facing integration cards no longer expose administrator setup, callback URLs or client-credential instructions. Each provider now has one tenant-safe Connected, Available to connect or TLink setup in progress state, and OAuth callback results reopen the correct Integrations or Schedule workspace, verify authoritative provider state before claiming success and remove consumed query parameters. Xero uses its current granular invoice scope, Stripe Connect no longer falls back to an unrelated referral key, and payment providers require their callback verification configuration before a tenant can initiate a connection or payment request. Signed-in production QA confirmed all seven provider cards, the failed-callback result and URL cleanup, no application console errors, no recent Sites worker errors and no desktop document overflow. The canonical `/api/health` route returned HTTP 200. Automated responsive contracts cover the narrow layout; no provider consent redirect was claimed because the central provider registrations and production credentials remain absent. Production environment revision 14 was retained.
- Rolling dispatch and the action dashboard were published as Sites version 195 from implementation commit `e830abb0e3cb8cd189b8d26795010f867b9625b3` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5e2583216c8191995fcedb921b4d15`. Schedule now keeps eight weeks in one bounded planner, supports horizontal and vertical drag with edge scrolling, and shows customer, assigned person and suburb before time while keeping the global TLink job reference inside the accessible detail dialog. Direct-customer identity remains owner scoped and AEA protected work stays redacted. My day now exposes actionable schedule and work-status counts, source-backed four-week booked-work and current-stage charts, and one-click routes into the existing operational workspaces. Signed-in desktop and phone-width QA confirmed the rolling planner, appointment popup, dashboard charts and no document-level horizontal overflow without mutating production records. Complete validation passed twice, including TypeScript, ESLint, integration and full tests, clean D1 replay and production build. The canonical `/api/health` route returned HTTP 200. The browser showed only Chrome extension message-channel closures without an application stack, and the Sites error filter contained one notification request cancelled when the QA tab was released with no application exception or 5xx. Production environment revision 18 was retained.
- Operational indexes and dispatch clarity were published as Sites version 196 from implementation commit `3b206e5dac836fcf343144495dd36e166cf21f46` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5eab70a03481919dff44aa07f926dd`. Both Schedule entry points now use the same eight-week dispatch calendar with a prominent Today summary, current day and time markers, and customer, installer and suburb prioritised on each booking. Jobs and Customers are compact owner-scoped indexes with deliberate focused-record navigation, operational columns and filters; Templates, Reports, Import data, Integrations and Team are visible without a More menu. Future jobs use opaque collision-free `TLJ-X???????` references while every existing reference remains stable. Stale requests are cancelled, owner-scoped aggregates replace repeated job lookups and heavy workspaces are split into on-demand client chunks. Complete validation passed with TypeScript, ESLint, 33 integration tests, 682 full-suite tests, clean replay of all 79 production migrations and a production build. Signed-in desktop and phone-width QA confirmed the live calendar, index and focused-record flows without production mutations or document-level overflow. The canonical `/api/health` route returned HTTP 200, the browser console was clean, and the Sites error filter contained only two notification polls cancelled during QA navigation with no application exception or 5xx. Production environment revision 18 was retained.
- Stable weekly dispatch navigation was published as Sites version 197 from implementation commit `91fec624c3b01609f0f8eab476b18941a3f5478f` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5eb9c2304c819186d592042b2b69f1`. Ordinary timetable scrolling no longer rotates and replaces the fetched date range. Previous week, Today, Next week and Go to week now change one explicit Monday-to-Sunday week; phone swipes change one week; and a 600 ms drag-edge hold exposes the adjacent buffered week without unmounting the source appointment or saving before drop. Clock position is preserved across the edge transition, while failed moves restore the authoritative source appointment and week. TLink remains authoritative and the existing revision-guarded PATCH and protected one-way calendar mirror boundaries are unchanged. Complete validation passed with 29 focused tests, TypeScript, ESLint, 33 integration tests, 684 full-suite tests with no failures, clean replay of all 79 production migrations and a production build. Signed-in production QA moved current-next-current and observed two, zero and two active appointment cards, opened and closed the bounded appointment preview, and found no document-level desktop overflow without changing a production record. The canonical `/api/health` route returned HTTP 200, Sites worker logs contained no recent errors and production environment revision 18 was retained.
- Saved job and customer index views were published as Sites version 198 from implementation commit `f05995b5834316ae65a6e82a37b7519d965fa24f` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5ec07753d48191b96efde0d9ce8f8b`. Each signed-in trade user can keep up to 12 owner-scoped named views per Jobs or Customers index. A view restores bounded filters, sort, page size and visible-column order, resets paging and stale cursors on application, and cannot be read or mutated across owners or indexes. Both directories use one selected column order for displayed headers, rows and formula-safe visible-page CSV export. Signed-in production QA confirmed the controls, all five live job rows, all three live customer rows, nine job columns and ten customer columns at desktop and phone width without changing production data or causing document-level overflow. Complete validation passed with TypeScript, ESLint, 33 integration tests, 686 passing full-suite tests with two intentional skips, clean replay of all 79 production migrations and a production build. The canonical `/api/health` route returned HTTP 200, Sites worker logs contained no recent errors and production environment revision 18 was retained.
- The protected owner database console was published as Sites version 199 from implementation commit `4a5cd19dda6f86896cfc751f5a42aa07f9b4eff5` on `codex/sites-custom-domain-migration`, deployment `appgdep_6a5f78c0b3cc81919214c0deb5a3a8f3`. The owner-only Operations Database workspace browses 145 ordinary application tables through live SQLite metadata, exact counts and bounded pages while excluding internal tables, redacting protected credential material and clipping large values. Single-row insert and delete are default-deny and limited to `workspace_list_views`, `trade_team_working_hours` and `trade_team_unavailability`, with server-generated IDs and timestamps, table-specific validation, recent owner authentication, exact row-specific typed confirmation and atomic privacy-safe audit. Raw SQL, updates, DDL and bulk changes are not exposed. Complete validation passed with 25 focused tests, TypeScript, ESLint, 33 integration tests, 697 passing full-suite tests with two intentional skips, clean replay of all 79 production migrations and a production build. Signed-in production QA browsed real rows, confirmed four protected integration credential cells were redacted, opened the guarded add flow, and found no document-level overflow at desktop or 390 px. All three writable production tables were empty, so deletion remained covered by executable tests rather than creating a disposable live row. No production data was changed. The canonical `/api/health` route returned HTTP 200, Sites worker logs contained no recent errors and production environment revision 18 was retained.
- Live verification after P6-2C observed HTTPS, HSTS, `Permissions-Policy`, `Referrer-Policy`, `X-Content-Type-Options` and `X-Frame-Options` on the canonical custom domain. No CSP header was observed.
- Apple and Google developer accounts, signing credentials, mobile Firebase files, OAuth client IDs and APNs or FCM release credentials are required for store distribution.
- Historical shared D1 databases may already contain explicit synthetic demo records from migrations 0033 to 0038. They remain marked as synthetic and filterable. Fresh databases no longer receive them automatically.

## Release policy

- Preserve the compatibility electricity comparator until the native stability and parity gate passes.
- Publish validated commits to GitHub and Sites only.
- Never publish generated account credentials, benchmark outputs, secrets or customer data.
- Do not rewrite migration history already applied to a shared database. Compatibility identifiers can remain no-op while new corrective migrations move state forward.
